--- include/unistd.h
+++ include/unistd.h
@@ -12,6 +12,16 @@
 int pipe2(int [2], int);
 #endif
 
+#ifndef HAVE_MKOSTEMP
+/*
+ * mkostemp(3) is conventionally declared in <stdlib.h>, but every
+ * caller in the tree already includes <unistd.h>, so declaring it
+ * here keeps the fallback effective without having to patch each
+ * new call site as they show up.
+ */
+int mkostemp(char *, int);
+#endif
+
 #ifndef HAVE_PLEDGE
 int pledge(const char *, const char *);
 
