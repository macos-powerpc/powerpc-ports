--- source3/lib/sysquotas_nfs.c	2026-02-06 22:12:26.000000000 +0800
+++ source3/lib/sysquotas_nfs.c	2026-06-24 06:21:37.000000000 +0800
@@ -275,7 +275,11 @@
 out:
 	if (clnt) {
 		if (clnt->cl_auth) {
+#ifdef __APPLE__
+			((void (*)(AUTH *))clnt->cl_auth->ah_ops->ah_destroy)(clnt->cl_auth);
+#else
 			auth_destroy(clnt->cl_auth);
+#endif
 		}
 		clnt_destroy(clnt);
 	}
