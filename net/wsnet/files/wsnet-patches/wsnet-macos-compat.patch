diff --git a/CMakeLists.txt b/CMakeLists.txt
index 167e7a8..290c743 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,7 +1,9 @@
 cmake_minimum_required(VERSION 3.21)
 
-set(CMAKE_OSX_DEPLOYMENT_TARGET "12" CACHE STRING "Minimum OS X deployment version")
-set(X_VCPKG_APPLOCAL_DEPS_INSTALL ON)
+# vcpkg is only required for Windows, Android, and iOS
+if (WIN32 OR ANDROID OR IOS OR VCPKG_TARGET_ANDROID)
+    set(X_VCPKG_APPLOCAL_DEPS_INSTALL ON)
+endif()
 
 if (VCPKG_TARGET_ANDROID)
     include("cmake/vcpkg_android.cmake")
@@ -50,22 +52,41 @@ project(wsnet
 
 configure_file(cmake/wsnet_version.h.in ${PROJECT_BINARY_DIR}/wsnet_version.h)
 
-find_package(c-ares CONFIG REQUIRED)
-find_package(OpenSSL REQUIRED)
-find_package(CURL CONFIG REQUIRED)
-find_package(spdlog CONFIG REQUIRED)
-find_package(RapidJSON CONFIG REQUIRED)
-find_package(skyr-url CONFIG REQUIRED)
-find_package(GTest CONFIG REQUIRED)
-find_package(CMakeRC)
-find_path(CPP_BASE64_INCLUDE_DIRS "cpp-base64/base64.cpp")
-
-find_package(Boost REQUIRED COMPONENTS filesystem)
-if(Boost_FOUND)
-    include_directories(${Boost_INCLUDE_DIRS})
+# Use pkg-config for Unix-like systems, vcpkg for Windows/Mobile
+if (UNIX AND NOT ANDROID AND NOT IOS)
+    find_package(PkgConfig REQUIRED)
+    pkg_check_modules(CARES REQUIRED libcares)
+    pkg_check_modules(CURL REQUIRED libcurl)
+    find_package(OpenSSL REQUIRED)
+    pkg_check_modules(SPDLOG REQUIRED spdlog)
+    find_package(RapidJSON REQUIRED)
+    pkg_check_modules(SKYR_URL REQUIRED skyr-url)
+    if (DEFINED IS_BUILD_TESTS)
+        find_package(GTest REQUIRED)
+    endif()
+    find_package(Boost REQUIRED COMPONENTS filesystem)
+    find_package(CMakeRC)
+    find_path(CPP_BASE64_INCLUDE_DIRS "cpp-base64/base64.cpp")
 else()
-    message(STATUS "Boost NOT Found !")
-endif(Boost_FOUND)
+    # vcpkg-based dependency resolution for Windows/Android/iOS
+    find_package(c-ares CONFIG REQUIRED)
+    find_package(OpenSSL REQUIRED)
+    find_package(PkgConfig REQUIRED)
+    pkg_check_modules(CURL REQUIRED libcurl)
+    find_package(spdlog CONFIG REQUIRED)
+    find_package(RapidJSON CONFIG REQUIRED)
+    find_package(skyr-url CONFIG REQUIRED)
+    find_package(GTest CONFIG REQUIRED)
+    find_package(CMakeRC)
+    find_path(CPP_BASE64_INCLUDE_DIRS "cpp-base64/base64.cpp")
+
+    find_package(Boost REQUIRED COMPONENTS filesystem)
+    if(Boost_FOUND)
+        include_directories(${Boost_INCLUDE_DIRS})
+    else()
+        message(STATUS "Boost NOT Found !")
+    endif(Boost_FOUND)
+endif()
 
 find_path(ADVOBFUSCATOR_INCLUDE_DIRS "Lib/Indexes.h")
 
@@ -103,10 +124,45 @@ endif()
 # Set platform specific dependencies
 if (APPLE)
     set(OS_SPECIFIC_LIBRARIES "-framework Foundation" "-framework CFNetwork")
-    target_compile_options(wsnet PRIVATE "-fobjc-arc")
+    # Only use ARC with Clang, not with GCC
+    if (CMAKE_CXX_COMPILER_ID MATCHES "Clang" OR CMAKE_CXX_COMPILER_ID MATCHES "AppleClang")
+        target_compile_options(wsnet PRIVATE "-fobjc-arc")
+    endif()
+endif()
+
+# Link libraries - use pkg-config variables for Unix, vcpkg targets for others
+if (UNIX AND NOT ANDROID AND NOT IOS)
+    target_link_libraries(wsnet PRIVATE
+        ${CARES_LIBRARIES}
+        ${CURL_LIBRARIES}
+        ${SPDLOG_LIBRARIES}
+        OpenSSL::SSL
+        wsnet::rc
+        Boost::filesystem
+        ${OS_SPECIFIC_LIBRARIES}
+    )
+    target_include_directories(wsnet PRIVATE
+        ${CARES_INCLUDE_DIRS}
+        ${CURL_INCLUDE_DIRS}
+        ${SPDLOG_INCLUDE_DIRS}
+        ${SKYR_URL_INCLUDE_DIRS}
+        ${RapidJSON_INCLUDE_DIRS}
+    )
+else()
+    target_link_libraries(wsnet PRIVATE
+        c-ares::cares
+        ${CURL_LIBRARIES}
+        spdlog::spdlog
+        rapidjson
+        skyr::skyr-url
+        OpenSSL::SSL
+        wsnet::rc
+        Boost::filesystem
+        ${OS_SPECIFIC_LIBRARIES}
+    )
+    target_include_directories(wsnet PRIVATE ${CURL_INCLUDE_DIRS})
 endif()
 
-target_link_libraries(wsnet PRIVATE c-ares::cares CURL::libcurl spdlog::spdlog rapidjson skyr::skyr-url OpenSSL::SSL wsnet::rc Boost::filesystem ${OS_SPECIFIC_LIBRARIES})
 target_include_directories(wsnet PRIVATE
     ${PROJECT_BINARY_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/include/wsnet ${CMAKE_CURRENT_SOURCE_DIR}/src
     ${CPP_BASE64_INCLUDE_DIRS} ${ADVOBFUSCATOR_INCLUDE_DIRS}
diff --git a/src/pingmanager/apple/pingmanager_apple.mm b/src/pingmanager/apple/pingmanager_apple.mm
index cbb48b3..7acc2f2 100644
--- a/src/pingmanager/apple/pingmanager_apple.mm
+++ b/src/pingmanager/apple/pingmanager_apple.mm
@@ -2,6 +2,20 @@
 #import "simple_ping.h"
 #include "utils/utils.h"
 
+// Compatibility macros for OS X 10.6+
+#ifndef __bridge
+    #define __bridge
+#endif
+#ifndef __bridge_retained
+    #define __bridge_retained
+#endif
+#ifndef __bridge_transfer
+    #define __bridge_transfer
+#endif
+#ifndef nullable
+    #define nullable
+#endif
+
 // wrapper for SimplePing
 // SimplePing it's Apple's recommended class for pinging: https://developer.apple.com/library/archive/samplecode/SimplePing
 @interface PingHelper : NSObject <SimplePingDelegate>
diff --git a/src/pingmanager/apple/simple_ping.h b/src/pingmanager/apple/simple_ping.h
index 4ca9588..32dea10 100644
--- a/src/pingmanager/apple/simple_ping.h
+++ b/src/pingmanager/apple/simple_ping.h
@@ -11,6 +11,15 @@
 #include <AssertMacros.h>           // for __Check_Compile_Time
 #include <sys/socket.h>
 
+// Compatibility macros for OS X 10.6+
+#ifndef NS_ASSUME_NONNULL_BEGIN
+    #define NS_ASSUME_NONNULL_BEGIN
+    #define NS_ASSUME_NONNULL_END
+    #define nullable
+    #define NS_UNAVAILABLE
+    #define NS_DESIGNATED_INITIALIZER
+#endif
+
 NS_ASSUME_NONNULL_BEGIN
 
 @protocol SimplePingDelegate;
diff --git a/src/pingmanager/apple/simple_ping.mm b/src/pingmanager/apple/simple_ping.mm
index ffe0385..361cc84 100644
--- a/src/pingmanager/apple/simple_ping.mm
+++ b/src/pingmanager/apple/simple_ping.mm
@@ -1,7 +1,7 @@
 /*
     Copyright (C) 2016 Apple Inc. All Rights Reserved.
-    See LICENSE.txt for this sample’s licensing information
-    
+    See LICENSE.txt for this sample's licensing information
+
     Abstract:
     An object wrapper around the low-level BSD Sockets ping function.
  */
@@ -12,6 +12,26 @@
 #include <netinet/in.h>
 #include <errno.h>
 
+// Compatibility macros for OS X 10.6+
+#ifndef __bridge
+    #define __bridge
+#endif
+#ifndef __bridge_retained
+    #define __bridge_retained
+#endif
+#ifndef __bridge_transfer
+    #define __bridge_transfer
+#endif
+
+// CFAutorelease is not available on OS X 10.6, use manual memory management
+#if !defined(__MAC_10_7) || MAC_OS_X_VERSION_MIN_REQUIRED < __MAC_10_7
+    #define WSNET_CFAutorelease(obj) (obj)
+    #define WSNET_NEEDS_MANUAL_RELEASE 1
+#else
+    #define WSNET_CFAutorelease(obj) CFAutorelease(obj)
+    #define WSNET_NEEDS_MANUAL_RELEASE 0
+#endif
+
 #pragma mark * IPv4 and ICMPv4 On-The-Wire Format
 
 /*! Describes the on-the-wire header format for an IPv4 packet.
@@ -162,14 +182,15 @@ static uint16_t in_cksum(const void *buffer, size_t bufferLen) {
     
     assert(error != nil);
     
-    // We retain ourselves temporarily because it's common for the delegate method 
-    // to release its last reference to us, which causes -dealloc to be called here. 
-    // If we then reference self on the return path, things go badly.  I don't think 
-    // that happens currently, but I've got into the habit of doing this as a 
+    // We retain ourselves temporarily because it's common for the delegate method
+    // to release its last reference to us, which causes -dealloc to be called here.
+    // If we then reference self on the return path, things go badly.  I don't think
+    // that happens currently, but I've got into the habit of doing this as a
     // defensive measure.
-    
-    CFAutorelease( CFBridgingRetain( self ));
-    
+
+    CFTypeRef selfRef = WSNET_CFAutorelease( (__bridge CFTypeRef)self );
+    (void)selfRef; // Suppress unused variable warning
+
     [self stop];
     strongDelegate = self.delegate;
     if ( (strongDelegate != nil) && [strongDelegate respondsToSelector:@selector(simplePing:didFailWithError:)] ) {
@@ -616,10 +637,10 @@ static void SocketReadCallback(CFSocketRef s, CFSocketCallBackType type, CFDataR
         CFSocketContext         context = {0, (__bridge void *)(self), NULL, NULL, NULL};
         CFRunLoopSourceRef      rls;
         id<SimplePingDelegate>  strongDelegate;
-        
+
         // Wrap it in a CFSocket and schedule it on the runloop.
-        
-        self.socket = (CFSocketRef) CFAutorelease( CFSocketCreateWithNative(NULL, fd, kCFSocketReadCallBack, SocketReadCallback, &context) );
+
+        self.socket = (CFSocketRef) WSNET_CFAutorelease( CFSocketCreateWithNative(NULL, fd, kCFSocketReadCallBack, SocketReadCallback, &context) );
         assert(self.socket != NULL);
         
         // The socket will now take care of cleaning up our file descriptor.
@@ -730,11 +751,11 @@ static void HostResolveCallback(CFHostRef theHost, CFHostInfoType typeInfo, cons
     Boolean             success;
     CFHostClientContext context = {0, (__bridge void *)(self), NULL, NULL, NULL};
     CFStreamError       streamError;
-    
+
     assert(self.host == NULL);
     assert(self.hostAddress == nil);
 
-    self.host = (CFHostRef) CFAutorelease( CFHostCreateWithName(NULL, (__bridge CFStringRef) self.hostName) );
+    self.host = (CFHostRef) WSNET_CFAutorelease( CFHostCreateWithName(NULL, (__bridge CFStringRef) self.hostName) );
     assert(self.host != NULL);
     
     CFHostSetClient(self.host, HostResolveCallback, &context); // NOLINT(clang-analyzer-nullability.NullablePassedToNonnull)
