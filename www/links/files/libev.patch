--- config.h.in.orig	2024-07-27 21:25:08.000000000 +0800
+++ config.h.in	2026-06-11 06:17:48.000000000 +0800
@@ -596,8 +596,8 @@
 /* Define if you have the <dirent.h> header file.  */
 #undef HAVE_DIRENT_H
 
-/* Define if you have the <ev-event.h> header file.  */
-#undef HAVE_EV_EVENT_H
+/* Define if you have the <ev.h> header file.  */
+#undef HAVE_EV_H
 
 /* Define if you have the <event.h> header file.  */
 #undef HAVE_EVENT_H

--- links.h.orig	2023-03-22 02:26:59.000000000 +0800
+++ links.h	2026-06-11 06:18:04.000000000 +0800
@@ -285,7 +285,7 @@
 #if defined(HAVE_EVENT_H)
 #include <event.h>
 #else
-#include <ev-event.h>
+#include <ev.h>
 #endif
 #define USE_LIBEVENT
 #endif

--- configure.orig	2026-06-11 05:49:36.000000000 +0800
+++ configure	2026-06-11 06:18:51.000000000 +0800
@@ -6173,7 +6173,7 @@
 
 cf_have_libevent=no
 if test "$disable_libevent" != yes; then
-	for ac_hdr in event.h ev-event.h
+	for ac_hdr in event.h ev.h
 do
 ac_safe=`echo "$ac_hdr" | sed 'y%./+-%__p_%'`
 echo $ac_n "checking for $ac_hdr""... $ac_c" 1>&6
@@ -6266,7 +6266,7 @@
 		fi
 	fi
 	if test "$cf_have_libevent" = no; then
-		if test "$ac_cv_header_event_h" = yes -o "$ac_cv_header_ev_event_h" = yes; then
+		if test "$ac_cv_header_event_h" = yes -o "$ac_cv_header_ev_h" = yes; then
 			echo $ac_n "checking for event_loop in -lev""... $ac_c" 1>&6
 echo "configure:6269: checking for event_loop in -lev" >&5
 ac_lib_var=`echo ev'_'event_loop | sed 'y%./+-%__p_%'`
