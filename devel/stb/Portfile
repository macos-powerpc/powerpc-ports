# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0

github.setup        nothings stb fede005abaf93d9d7f3a679d1999b2db341b360f
version             20250908
revision            0

categories          devel
platforms           any
supported_archs     noarch
license             {public-domain MIT}
maintainers         {judaew @judaew} openmaintainer

description         Single-file public domain (or MIT licensed) libraries for C/C++
long_description    {*}${description}

checksums           rmd160  e0ced717e5a5f8d939c6097226188a82b8715edf \
                    sha256  3c45e9ab123933274ad782e19fc4029444bfe26e0a5eea483c30a82b880b8a42 \
                    size    1513545
github.tarball_from archive

use_configure       no
build               {}

destroot {
    xinstall -d ${destroot}${prefix}/include/${name}
    xinstall -m 0644 {*}[glob ${worksrcpath}/*.h] \
        ${destroot}${prefix}/include/${name}
    xinstall -m 0644 {*}[glob ${worksrcpath}/*.c] \
        ${destroot}${prefix}/include/${name}

    xinstall -d ${destroot}${prefix}/share/doc/${name}
    xinstall -m 0644 {*}[glob ${worksrcpath}/docs/*] \
        ${destroot}${prefix}/share/doc/${name}
}
