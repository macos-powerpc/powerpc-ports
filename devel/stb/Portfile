# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0

github.setup        nothings stb f58f558c120e9b32c217290b80bad1a0729fbb2c
version             20250526
revision            0

categories          devel
platforms           any
supported_archs     noarch
license             {public-domain MIT}
maintainers         {judaew @judaew} openmaintainer

description         Single-file public domain (or MIT licensed) libraries for C/C++
long_description    {*}${description}

checksums           rmd160  42557f0f4f7a4f61da0601df2c2a24111f3ebd9e \
                    sha256  be1a5dad9ac38f42ef8abf00bd7db776101661b77603f832fcc0bae843d82846 \
                    size    1513418
github.tarball_from archive

use_configure       no
build               {}

destroot {
    xinstall -d ${destroot}${prefix}/include/${name}
    xinstall -m 0644 {*}[glob ${worksrcpath}/*.h] \
        ${destroot}${prefix}/include/${name}
    xinstall -m 0644 {*}[glob ${worksrcpath}/*.c] \
        ${destroot}${prefix}/include/${name}

    xinstall -d ${destroot}${prefix}/share/doc/${name}
    xinstall -m 0644 {*}[glob ${worksrcpath}/docs/*] \
        ${destroot}${prefix}/share/doc/${name}
}
