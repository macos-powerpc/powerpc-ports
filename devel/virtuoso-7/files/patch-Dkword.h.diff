--- libsrc/Dk/Dkword.h.orig	2023-03-01 18:18:32.000000000 +0800
+++ libsrc/Dk/Dkword.h	2025-09-08 17:05:51.000000000 +0800
@@ -79,7 +79,12 @@
 #ifdef WORDS_BIGENDIAN
 
 #define memcmp_8(p1, p2, l, neq) \
-  {if (memcmp (p1, p2, l)) goto neq;}
+  { if (memcmp (p1, p2, l)) goto neq; }
+
+#define memcmp_8l(a1, a2, len, neq, lret)       \
+  { \
+    if (memcmp((a1), (a2), (len))) { lret = 0; goto neq; } \
+  }
 #else
 
 #define memcmp_8(a1, a2, len, neq)				\
