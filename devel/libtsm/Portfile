# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           cmake 1.1
PortGroup           github 1.0

github.setup        Aetf libtsm 4.0.2 v
revision            0
categories          devel
license             MIT
maintainers         nomaintainer
description         Terminal-emulator state machine
long_description    ${name} is a state machine for DEC VT100-VT520-compatible \
                    terminal emulators. It tries to support all common standards \
                    while keeping compatibility to existing emulators like xterm, \
                    gnome-terminal, konsole etc. ${name} itself does not provide \
                    any rendering or window management, it is a pure state machine \
                    without any external dependencies.
checksums           rmd160  310a3a78c60f1ac9cd031cb8d4cc912d52f431bf \
                    sha256  ce51be564872d3e6a35fadab1f74b8804f72d8a92c0f378ca384155134d154e5 \
                    size    148135
github.tarball_from archive

cmake.build_type    Release

compiler.c_standard 2011

patchfiles          patch-cmake-CompileOptions.cmake.diff \
                    patch-src-shared-CMakeLists.txt.diff \
                    patch-src-shared-shl-macro.h.diff \
                    patch-src-tsm-CMakeLists.txt.diff

configure.args-append \
                    -DBUILD_GTKTSM=OFF \
                    -DBUILD_SHARED_LIBS=ON \
                    -DBUILD_TESTING=OFF
