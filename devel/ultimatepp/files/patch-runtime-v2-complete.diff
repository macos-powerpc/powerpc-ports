diff --git a/uppsrc/CtrlCore/CocoApp.mm b/uppsrc/CtrlCore/CocoApp.mm
index 1be5b2f07..9fd0ad371 100644
--- a/uppsrc/CtrlCore/CocoApp.mm
+++ b/uppsrc/CtrlCore/CocoApp.mm
@@ -4,6 +4,11 @@
 
 #ifdef GUI_COCOA
 
+// Carbon compatibility for macOS 10.6 PowerPC
+#if defined(__ppc__) || defined(__ppc64__)
+#include <Carbon/Carbon.h>
+#endif
+
 @interface AppDelegate : NSObject<NSApplicationDelegate>
 {
 }
@@ -46,10 +51,21 @@ void CocoInit(int argc, const char **argv, const char **envptr)
 {
 	Ctrl::GlobalBackBuffer();
 	main_coco_pool = [NSAutoreleasePool new];
-	
+
+#if defined(__ppc__) || defined(__ppc64__)
+	// On PowerPC macOS 10.6, use Carbon to transform process type before NSApp
+	ProcessSerialNumber psn;
+	if (GetCurrentProcess(&psn) == noErr) {
+		TransformProcessType(&psn, kProcessTransformToForegroundApplication);
+	}
+#endif
+
 	[NSApplication sharedApplication];
 
+#if !defined(__ppc__) && !defined(__ppc64__)
+	// setActivationPolicy: may not work reliably on PowerPC
 	[NSApp setActivationPolicy:NSApplicationActivationPolicyRegular];
+#endif
 
 	menubar = [[NSMenu new] autorelease];
 	id appMenuItem = [[NSMenuItem new] autorelease];
@@ -261,8 +277,12 @@ Rect MakeScreenRect(NSScreen *screen, CGRect r)
 void Ctrl::GetWorkArea(Array<Rect>& rc)
 {
 	Upp::GuiLock __;
-	NSArray *screens = [NSScreen screens]; for(NSUInteger i = 0; i < [screens count]; i++) { NSScreen *screen = [screens objectAtIndex:i];
-		rc.Add(MakeScreenRect(screen, [screen visibleFrame])); }
+	NSArray *screens = [NSScreen screens];
+	for(NSUInteger i = 0; i < [screens count]; i++) {
+		NSScreen *screen = [screens objectAtIndex:i];
+		NSRect nsframe = [screen visibleFrame];
+		rc.Add(MakeScreenRect(screen, NSRectToCGRect(nsframe)));
+	}
 }
 
 
@@ -283,8 +303,11 @@ Rect Ctrl::GetVirtualScreenArea()
 {
 	bool first = true;
 	Rect r(0, 0, 0, 0);
-	NSArray *screens = [NSScreen screens]; for(NSUInteger i = 0; i < [screens count]; i++) { NSScreen *screen = [screens objectAtIndex:i];
-		Rect sr = MakeScreenRect(screen, [screen frame]);
+	NSArray *screens = [NSScreen screens];
+	for(NSUInteger i = 0; i < [screens count]; i++) {
+		NSScreen *screen = [screens objectAtIndex:i];
+		NSRect nsframe = [screen frame];
+		Rect sr = MakeScreenRect(screen, NSRectToCGRect(nsframe));
 		if(first)
 			r = sr;
 		else
@@ -304,8 +327,11 @@ Rect Ctrl::GetPrimaryWorkArea()
 Rect Ctrl::GetScreenArea(Point pt)
 {
 	Upp::GuiLock __;
-	NSArray *screens = [NSScreen screens]; for(NSUInteger i = 0; i < [screens count]; i++) { NSScreen *screen = [screens objectAtIndex:i];
-		Rect rc = MakeScreenRect(screen, [screen frame]);
+	NSArray *screens = [NSScreen screens];
+	for(NSUInteger i = 0; i < [screens count]; i++) {
+		NSScreen *screen = [screens objectAtIndex:i];
+		NSRect nsframe = [screen frame];
+		Rect rc = MakeScreenRect(screen, NSRectToCGRect(nsframe));
 		if(rc.Contains(pt))
 			return rc;
 	}
@@ -314,8 +340,12 @@ Rect Ctrl::GetScreenArea(Point pt)
 
 Rect Ctrl::GetPrimaryScreenArea()
 {
-	NSArray *screens = [NSScreen screens]; for(NSUInteger i = 0; i < [screens count]; i++) { NSScreen *screen = [screens objectAtIndex:i];
-		return MakeScreenRect(screen, [screen frame]);
+	NSArray *screens = [NSScreen screens];
+	for(NSUInteger i = 0; i < [screens count]; i++) {
+		NSScreen *screen = [screens objectAtIndex:i];
+		NSRect nsframe = [screen frame];
+		return MakeScreenRect(screen, NSRectToCGRect(nsframe));
+	}
 	return Rect(0, 0, 1024, 768);
 }
 
@@ -346,7 +376,10 @@ void Ctrl::GuiPlatformGetTopRect(Rect& r) const
 void MMCtrl::SyncRect(CocoView *view)
 {
 	NSWindow *win = [view window];
-	view->ctrl->SetWndRect(MakeScreenRect([win screen], [win contentRectForFrameRect: [win frame]]));
+	NSScreen *screen = [win screen];
+	NSRect winFrame = [win frame];
+	NSRect contentRect = [win contentRectForFrameRect:winFrame];
+	view->ctrl->SetWndRect(MakeScreenRect(screen, NSRectToCGRect(contentRect)));
 }
 
 TopFrameDraw::TopFrameDraw(Ctrl *ctrl, const Rect& r)
diff --git a/uppsrc/CtrlLib/ChCocoMM.mm b/uppsrc/CtrlLib/ChCocoMM.mm
index f14b7ee93..c571e03fe 100644
--- a/uppsrc/CtrlLib/ChCocoMM.mm
+++ b/uppsrc/CtrlLib/ChCocoMM.mm
@@ -18,6 +18,24 @@
 #include <CtrlCore/CocoMM.h>
 #include "ChCocoMM.h"
 
+// Forward declare methods for 10.6 SDK compatibility
+@interface NSGraphicsContext (Compatibility_10_6_ChCoco)
++ (NSGraphicsContext *)graphicsContextWithCGContext:(CGContextRef)context flipped:(BOOL)flag;
+@end
+
+@interface NSImage (Compatibility_10_6_ChCoco)
+- (void)drawInRect:(NSRect)rect;
+@end
+
+@interface NSScroller (Compatibility_10_6_ChCoco)
+- (void)setKnobStyle:(NSInteger)style;
+- (void)setScrollerStyle:(NSInteger)style;
+@end
+
+@interface NSButton (Compatibility_10_6_ChCoco)
+- (void)setControlSize:(NSInteger)size;
+@end
+
 // SDK compatibility defines for 10.6
 #if MAC_OS_X_VERSION_MAX_ALLOWED < 1070
 typedef NSInteger NSScrollerStyle;
@@ -50,9 +68,22 @@ typedef NSInteger NSControlStateValue;
 // CGColor property added in 10.8, so we need manual conversion
 static CGColorRef NSColorToCGColor(NSColor *nscolor)
 {
+	// Try to convert to RGB colorspace - system colors need this
 	NSColor *rgbColor = [nscolor colorUsingColorSpaceName:NSCalibratedRGBColorSpace];
+
+	// If that fails, try device RGB
 	if(!rgbColor)
-		rgbColor = nscolor;
+		rgbColor = [nscolor colorUsingColorSpaceName:NSDeviceRGBColorSpace];
+
+	// If still no conversion, use a safe default (white)
+	if(!rgbColor) {
+		CGFloat components[4] = {1.0, 1.0, 1.0, 1.0};
+		CGColorSpaceRef colorSpace = CGColorSpaceCreateWithName(kCGColorSpaceGenericRGB);
+		CGColorRef cgColor = CGColorCreate(colorSpace, components);
+		CGColorSpaceRelease(colorSpace);
+		return cgColor;
+	}
+
 	CGFloat components[4];
 	[rgbColor getRed:&components[0] green:&components[1] blue:&components[2] alpha:&components[3]];
 	CGColorSpaceRef colorSpace = CGColorSpaceCreateWithName(kCGColorSpaceGenericRGB);
@@ -63,27 +94,32 @@ static CGColorRef NSColorToCGColor(NSColor *nscolor)
 
 void Coco_PaintCh(void *cgcontext, int type, int value, int state)
 {
-	auto dopaint = [&] {
-		auto cg = (CGContextRef) cgcontext;
-		if(Upp::IsUHDMode())
-			CGContextScaleCTM(cg, 2, 2);
+	// Lambda removed - GCC 14 PowerPC has issues with lambdas in ObjC++ context
+	auto cg = (CGContextRef) cgcontext;
+
+	// Check for valid context - can be NULL on initialization errors
+	if(!cg)
+		return;
+
+	if(Upp::IsUHDMode())
+		CGContextScaleCTM(cg, 2, 2);
 		NSRect cr = NSMakeRect(0, 0, 140, 140);
 		if(type == COCO_NSCOLOR) {
 			CGContextSaveGState(cg);
-			CGColorRef color = NSColorToCGColor(Upp::decode(value,
-				COCO_PAPER, [NSColor textBackgroundColor],
-				COCO_SELECTEDTEXT, [NSColor selectedTextColor],
-				COCO_SELECTEDPAPER, [NSColor selectedTextBackgroundColor],
-				COCO_DISABLED, [NSColor disabledControlTextColor],
-				COCO_WINDOW, [NSColor windowBackgroundColor],
-				COCO_SELECTEDMENUTEXT, [NSColor selectedMenuItemTextColor],
-				[NSColor textColor]
-			));
-			CGContextSetFillColorWithColor(cg, color);
-			CGColorRelease(color);
-			CGContextFillRect(cg, NSRectToCGRect(cr));
-			CGContextRestoreGState(cg);
-		}
+			CGColorRef color = NSColorToCGColor(Upp::decode(value,
+				COCO_PAPER, [NSColor textBackgroundColor],
+				COCO_SELECTEDTEXT, [NSColor selectedTextColor],
+				COCO_SELECTEDPAPER, [NSColor selectedTextBackgroundColor],
+				COCO_DISABLED, [NSColor disabledControlTextColor],
+				COCO_WINDOW, [NSColor windowBackgroundColor],
+				COCO_SELECTEDMENUTEXT, [NSColor selectedMenuItemTextColor],
+				[NSColor textColor]
+			));
+			CGContextSetFillColorWithColor(cg, color);
+			CGColorRelease(color);
+			CGContextFillRect(cg, NSRectToCGRect(cr));
+			CGContextRestoreGState(cg);
+		}
 		else
 		if(type == COCO_NSIMAGE) {
 #if MAC_OS_X_VERSION_MAX_ALLOWED >= 1070
@@ -152,8 +188,6 @@ void Coco_PaintCh(void *cgcontext, int type, int value, int state)
 		    [NSGraphicsContext restoreGraphicsState];
 		    CGContextRestoreGState(cg);
 		}
-	};
-	dopaint();
 }
 
 #endif
diff --git a/uppsrc/ide/Builders/Cocoa.cpp b/uppsrc/ide/Builders/Cocoa.cpp
index 375c64129..16777c1a4 100644
--- a/uppsrc/ide/Builders/Cocoa.cpp
+++ b/uppsrc/ide/Builders/Cocoa.cpp
@@ -68,7 +68,7 @@ void GccBuilder::CocoaAppBundle()
 			<< "    <key>NSHighResolutionCapable</key>\n"
 		    << "    <string>True</string>\n"
 		    << "	<key>LSMinimumSystemVersion</key>\n"
-			<< "    <string>10.13.0</string>\n"
+			<< "    <string>10.6.0</string>\n"
 		;
 		if(imgs.GetCount())
 			Info_plist
