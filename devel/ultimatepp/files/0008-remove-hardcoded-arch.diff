diff --git a/uppsrc/ide/Builders/Install.cpp b/uppsrc/ide/Builders/Install.cpp
index 87138c0fb..e5bcef763 100644
--- a/uppsrc/ide/Builders/Install.cpp
+++ b/uppsrc/ide/Builders/Install.cpp
@@ -162,12 +162,9 @@ void CreateBuildMethods()
 		
 		Path("$INCLUDE$", "/opt/local/include;/usr/include;/usr/local/include;/opt/homebrew/include;/opt/homebrew/opt/openssl/include");
 		Path("$LIB$", "/opt/local/lib;/usr/lib;/usr/local/lib;/opt/homebrew/lib;/opt/homebrew/opt/openssl/lib");
-		
-		String common;
-	#ifdef CPU_ARM
-		common = "-arch arm64";
-	#endif
-		bm.Replace("$COMMON$", common);
+
+		// No hardcoded architecture flags - let compiler use native architecture
+		bm.Replace("$COMMON$", "");
 
 		SaveFile(bm_path, bm);
 	}
@@ -179,11 +176,9 @@ void CreateBuildMethods()
 			r.Replace("INCLUDE = \"\";", "INCLUDE = \"/usr/local/opt/openssl/include\";");
 			r.Replace("LIB = \"\";", "LIB = \"/usr/local/opt/openssl/lib\";");
 		}
-		String common;
-	#ifdef CPU_X86
-		common = "-mpopcnt";
-	#endif
-		r.Replace("$COMMON$", common);
+		// No hardcoded architecture flags for non-macOS POSIX
+		// -mpopcnt requires SSE4.2, not available on all x86 CPUs
+		r.Replace("$COMMON$", "");
 		return r;
 	};
 
