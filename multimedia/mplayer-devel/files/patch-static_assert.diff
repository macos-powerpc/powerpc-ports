--- ffmpeg/libavcodec/ccaption_dec.c.orig	2026-09-13 03:42:11.000000000 +0800
+++ ffmpeg/libavcodec/ccaption_dec.c	2026-09-13 03:40:29.000000000 +0800
@@ -182,7 +182,7 @@
 #define EMPTY_START(IDX)
 #define EMPTY_END
 #define ASSERT_ENTRY(IDX, str)                                    \
-    static_assert(sizeof(str) <= sizeof(charset_overrides[0][0]), \
+    _Static_assert(sizeof(str) <= sizeof(charset_overrides[0][0]), \
                   "'" str "' string takes too much space");
 CHARSET_OVERRIDE_LIST(EMPTY_START, ASSERT_ENTRY, EMPTY_END)
 
--- ffmpeg/libavutil/hash.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libavutil/hash.c	2026-09-13 04:00:02.000000000 +0800
@@ -77,7 +77,7 @@
     HASHES(HASH_MAX_SIZE)
     MAX_HASH_SIZE
 };
-static_assert(AV_HASH_MAX_SIZE >= MAX_HASH_SIZE, "AV_HASH_MAX_SIZE needs to be updated!");
+_Static_assert(AV_HASH_MAX_SIZE >= MAX_HASH_SIZE, "AV_HASH_MAX_SIZE needs to be updated!");
 
 #define HASH_MAX_NAME_SIZE(TYPE, NAME, SIZE) \
     HASH_MAX_NAME_SIZE_BEFORE_ ## TYPE,      \
--- ffmpeg/libavformat/img2.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libavformat/img2.c	2026-09-13 03:39:22.000000000 +0800
@@ -98,7 +98,7 @@
     TAG(NONE,                     )
 
 #define LENGTH_CHECK(CODECID, STR) \
-    static_assert(sizeof(#STR) <= sizeof(ff_img_tags->str), #STR " does not fit into IdStrMap.str\n");
+    _Static_assert(sizeof(#STR) <= sizeof(ff_img_tags->str), #STR " does not fit into IdStrMap.str\n");
 IMG_TAGS(LENGTH_CHECK)
 
 const IdStrMap ff_img_tags[] = {
--- ffmpeg/libavformat/mov_chan.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libavformat/mov_chan.c	2026-09-13 03:55:36.000000000 +0800
@@ -226,7 +226,7 @@
     CHLIST16( MOV_CH_LAYOUT_TMH_10_2_STD,         L,   R,   C,    Vhc,  Lsd,  Rsd,   Ls,   Rs,   Vhl,  Vhr,  Lw,  Rw,  Csd,  Cs,  LFE1,  LFE2  )\
     CHLIST21( MOV_CH_LAYOUT_TMH_10_2_FULL,        L,   R,   C,    Vhc,  Lsd,  Rsd,   Ls,   Rs,   Vhl,  Vhr,  Lw,  Rw,  Csd,  Cs,  LFE1,  LFE2,  Lc,  Rc,  HI,  VI,  Haptic  )\
 
-#define CHLIST(_tag, _cnt, ...)    static_assert((_tag & 0xffff) == _cnt, "Channel count of " #_tag " is not " #_cnt);
+#define CHLIST(_tag, _cnt, ...)    _Static_assert((_tag & 0xffff) == _cnt, "Channel count of " #_tag " is not " #_cnt);
 MOV_CH_LAYOUT_MAP
 #undef CHLIST
 #define CHLIST(_tag, _cnt, ...)    TAG(_tag, _cnt), __VA_ARGS__,
--- ffmpeg/libavcodec/version.c.orig	2026-09-13 03:08:07.000000000 +0800
+++ ffmpeg/libavcodec/version.c	2026-09-13 03:27:27.000000000 +0800
@@ -31,7 +31,7 @@
 
 unsigned avcodec_version(void)
 {
-    static_assert(AV_CODEC_ID_LEAD         ==   269 &&
+    _Static_assert(AV_CODEC_ID_LEAD        ==   269 &&
                   AV_CODEC_ID_PCM_SGA      == 65572 &&
                   AV_CODEC_ID_ADPCM_XMD    == 69683 &&
                   AV_CODEC_ID_CBD2_DPCM    == 81928 &&
@@ -39,7 +39,7 @@
                   AV_CODEC_ID_ARIB_CAPTION == 94233 &&
                   AV_CODEC_ID_SMPTE_2038   == 98315,
                   "Don't insert new codec ids in the middle of a list");
-    static_assert(LIBAVCODEC_VERSION_MICRO >= 100, "micro version starts at 100");
+    _Static_assert(LIBAVCODEC_VERSION_MICRO >= 100, "micro version starts at 100");
 
     return LIBAVCODEC_VERSION_INT;
 }
--- ffmpeg/libavdevice/version.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libavdevice/version.c	2026-09-13 03:29:09.000000000 +0800
@@ -30,7 +30,7 @@
 
 unsigned avdevice_version(void)
 {
-    static_assert(LIBAVDEVICE_VERSION_MICRO >= 100, "micro version starts at 100");
+    _Static_assert(LIBAVDEVICE_VERSION_MICRO >= 100, "micro version starts at 100");
     return LIBAVDEVICE_VERSION_INT;
 }
 
--- ffmpeg/libavfilter/version.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libavfilter/version.c	2026-09-13 03:32:07.000000000 +0800
@@ -29,7 +29,7 @@
 
 unsigned avfilter_version(void)
 {
-    static_assert(LIBAVFILTER_VERSION_MICRO >= 100, "micro version starts at 100");
+    _Static_assert(LIBAVFILTER_VERSION_MICRO >= 100, "micro version starts at 100");
     return LIBAVFILTER_VERSION_INT;
 }
 
--- ffmpeg/libavformat/version.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libavformat/version.c	2026-09-13 03:28:27.000000000 +0800
@@ -30,7 +30,7 @@
 
 unsigned avformat_version(void)
 {
-    static_assert(LIBAVFORMAT_VERSION_MICRO >= 100, "micro version starts at 100");
+    _Static_assert(LIBAVFORMAT_VERSION_MICRO >= 100, "micro version starts at 100");
     return LIBAVFORMAT_VERSION_INT;
 }
 
--- ffmpeg/libavutil/version.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libavutil/version.c	2026-09-13 03:27:36.000000000 +0800
@@ -35,11 +35,11 @@
 
 unsigned avutil_version(void)
 {
-    static_assert(AV_SAMPLE_FMT_S64P      == 11 &&
+    _Static_assert(AV_SAMPLE_FMT_S64P     == 11 &&
                   AVMEDIA_TYPE_ATTACHMENT ==  4 &&
                   AV_PICTURE_TYPE_BI      ==  7,
                   "Don't insert new sample/media/picture types in the middle of the list");
-    static_assert(LIBAVUTIL_VERSION_MICRO >= 100, "micro version starts at 100");
+    _Static_assert(LIBAVUTIL_VERSION_MICRO >= 100, "micro version starts at 100");
 
     return LIBAVUTIL_VERSION_INT;
 }
--- ffmpeg/libpostproc/version.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libpostproc/version.c	2026-09-13 03:31:14.000000000 +0800
@@ -29,7 +29,7 @@
 
 unsigned postproc_version(void)
 {
-    static_assert(LIBPOSTPROC_VERSION_MICRO >= 100, "micro version starts at 100");
+    _Static_assert(LIBPOSTPROC_VERSION_MICRO >= 100, "micro version starts at 100");
     return LIBPOSTPROC_VERSION_INT;
 }
 
--- ffmpeg/libswresample/version.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libswresample/version.c	2026-09-13 03:30:00.000000000 +0800
@@ -29,7 +29,7 @@
 
 unsigned swresample_version(void)
 {
-    static_assert(LIBSWRESAMPLE_VERSION_MICRO >= 100, "micro version starts at 100");
+    _Static_assert(LIBSWRESAMPLE_VERSION_MICRO >= 100, "micro version starts at 100");
     return LIBSWRESAMPLE_VERSION_INT;
 }
 
--- ffmpeg/libswscale/version.c.orig	2026-09-13 03:08:08.000000000 +0800
+++ ffmpeg/libswscale/version.c	2026-09-13 03:30:37.000000000 +0800
@@ -26,7 +26,7 @@
 
 unsigned swscale_version(void)
 {
-    static_assert(LIBSWSCALE_VERSION_MICRO >= 100, "micro version starts at 100");
+    _Static_assert(LIBSWSCALE_VERSION_MICRO >= 100, "micro version starts at 100");
     return LIBSWSCALE_VERSION_INT;
 }
 
