diff --git a/configure.orig b/configure
index 39e7f03..3cb9721 100755
--- configure..in.orig
+++ configure.in
@@ -1938,7 +1938,7 @@ OS=`uname`
 OS="${OS}"

 MACHINE=$(uname -m)
-CPU="CPU_INTEL"
+CPU="CPU_ARM"
 if test "$MACHINE" = "armv5l"; then
   CPU="CPU_ARM"
 fi
diff --git a/libfind2.c.orig b/libfind2.c
index d5c8c32..c0df20e 100644
--- libfind2.c.orig
+++ libfind2.c
@@ -240,10 +240,9 @@ findmac:;
       return 0;
       }
     }
-  if(argv[2][i  ] == 'i' && 
-     argv[2][i+1] == '3' && 
-     argv[2][i+2] == '8' && 
-     argv[2][i+3] == '6')
+  if(argv[2][i  ] == 'p' && 
+     argv[2][i+1] == 'p' && 
+     argv[2][i+2] == 'c')
     {  
     if(argv[1][0] == '0')
       {  
