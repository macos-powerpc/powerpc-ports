--- RScriptEditorTextStorage.m.orig
+++ RScriptEditorTextStorage.m
@@ -38,6 +38,11 @@
 #import "FoldingSignTextAttachmentCell.h"
 #import "RGUI.h"
 
+#if !defined(MAC_OS_X_VERSION_10_11) || MAC_OS_X_VERSION_MAX_ALLOWED < MAC_OS_X_VERSION_10_11
+/* NSTextStorageEditActions (an NSUInteger-based NS_OPTIONS) was introduced with the 10.11 SDK */
+typedef NSUInteger NSTextStorageEditActions;
+#endif
+
 @implementation RScriptEditorTextStorage
 
 static NSTextAttachment *sharedAttachment = nil;
