--- RController.m.orig
+++ RController.m
@@ -1908,7 +1908,9 @@
 		}
 
     // there are bugs in Sonoma, so to be safe disable focus switching in macOS 12.0 and higher
-    if (@available(macOS 12, *))
+    // (checked via NSAppKitVersionNumber rather than @available so that this also
+    // compiles with gcc and pre-Xcode 9 clang; 2113 == NSAppKitVersionNumber12_0)
+    if (floor(NSAppKitVersionNumber) >= 2113)
         return;
 
 	// Get all windows
