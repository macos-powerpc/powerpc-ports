--- zlibrary/core/src/unix/iconv/IConvEncodingConverter.cpp
+++ zlibrary/core/src/unix/iconv/IConvEncodingConverter.cpp
@@ -89,7 +89,7 @@
 #ifdef DO_ICONV_CAST
 	iconv(myIConverter, (const char**)&in, &inSize, &out, &outSize);
 #else // DO_ICONV_CAST
-	iconv(myIConverter, &in, &inSize, &out, &outSize);
+	iconv(myIConverter, (const char**)&in, &inSize, &out, &outSize);
 #endif // DO_ICONV_CAST
 	if (inSize != 0) {
 		if (myBuffer.empty()) {
@@ -132,7 +132,7 @@
 #ifdef DO_ICONV_CAST
 		iconv(myIConverter, (const char**)&in, &inSize, &out, &outSize);
 #else // DO_ICONV_CAST
-		iconv(myIConverter, &in, &inSize, &out, &outSize);
+		iconv(myIConverter, (const char**)&in, &inSize, &out, &outSize);
 #endif // DO_ICONV_CAST
 		if (inSize == 0) {
 			ZLUnicodeUtil::Ucs4Char ch;
--- fbreader/macosx/info.plist
+++ fbreader/macosx/info.plist
@@ -46,6 +46,6 @@
 	<key>CFBundleVersion</key>
 	<string>1</string>
 	<key>LSMinimumSystemVersion</key>
-	<string>10.5</string>
+	<string>10.4</string>
 </dict>
 </plist>
