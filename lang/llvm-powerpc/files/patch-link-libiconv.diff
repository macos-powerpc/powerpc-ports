--- a/llvm/tools/llvm-shlib/CMakeLists.txt	2018-08-03 18:15:36.000000000 +0800
+++ b/llvm/tools/llvm-shlib/CMakeLists.txt	2026-08-19 19:48:31.000000000 +0800
@@ -57,6 +57,10 @@
   set(LIB_NAMES -Wl,-all_load ${LIB_NAMES})
 endif()
 
+if(APPLE AND ICONV_LIBRARY_PATH)
+  list(APPEND LIB_NAMES ${ICONV_LIBRARY_PATH})
+endif()
+
 target_link_libraries(LLVM PRIVATE ${LIB_NAMES})
 
 if (APPLE)
