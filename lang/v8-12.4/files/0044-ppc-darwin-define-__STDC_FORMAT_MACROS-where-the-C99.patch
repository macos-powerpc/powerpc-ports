From 860d206df4af585f06a19b262f8e2e587e156819 Mon Sep 17 00:00:00 2001
From: barracuda156 <barracuda@macos-powerpc.org>
Date: Tue, 1 Sep 2026 22:49:33 +0000
Subject: [PATCH 44/52] ppc-darwin: define __STDC_FORMAT_MACROS where the C99
 format macros are used

G5 build error #6 of the 12.4 port (v8_base, incremental-marking.cc:
PRIi64 undeclared).  The PowerPC-era Mac OS X SDK gates the C99 PRI*
macros for C++ behind __STDC_FORMAT_MACROS, and the define only works
if it precedes the first - possibly transitive - inclusion of
<inttypes.h>; a bare #include <inttypes.h> (which the failing file
even has) exposes nothing.  Files that go through libstdc++'s
<cinttypes> are fine (the 9.4-era convention carried in patch 0025,
G5-validated across whole builds), which the sweep confirms
empirically: every already-compiled PRI user in this build has a
direct <cinttypes>.

Class sweep of the whole tree (grep for non-V8PRI* C99 macro uses,
filtered to TUs compiled on this target): the unprotected set is
incremental-marking.cc, the embedded-file-writer base and win TUs
(the win writer compiles on every host), wasm-code-manager.cc and
wasm-objects.cc, plus two wasm headers - function-body-decoder-impl.h
and module-decoder-impl.h - whose PRI uses compile inside their
includers, so the whole consumer closure (via wasm-disassembler-impl,
constant-expression-interface and turboshaft-graph-interface) gets
the guard too: factory, module-decoder, constant-expression{,-interface},
{function-body,graph-builder}-decoder/interface, function-compiler,
liftoff-compiler, turboshaft-graph-interface, wasm-disassembler,
wrappers and compiler/pipeline.  module-instantiate keeps its 0025
<cinttypes>.  Non-issues verified: V8PRIxPTR/V8PRIdPTR are V8's own
string-literal macros (deoptimizer, translated-state, assembler-ppc -
all already compiled); disassembler.cc's uses are ENABLE_DISASSEMBLER-
gated; simulator/other-arch/gdb-server TUs are not compiled; the absl
closure's two PRI users built long ago.

One uniform guarded define block at the top of each file, before any
include - a no-op on modern SDKs and glibc, which define the macros
unconditionally.
---
 v8/src/compiler/pipeline.cc                                | 7 +++++++
 v8/src/heap/factory.cc                                     | 7 +++++++
 v8/src/heap/incremental-marking.cc                         | 7 +++++++
 .../embedded/platform-embedded-file-writer-base.cc         | 7 +++++++
 .../snapshot/embedded/platform-embedded-file-writer-win.cc | 7 +++++++
 v8/src/wasm/baseline/liftoff-compiler.cc                   | 7 +++++++
 v8/src/wasm/constant-expression-interface.cc               | 7 +++++++
 v8/src/wasm/constant-expression.cc                         | 7 +++++++
 v8/src/wasm/function-body-decoder-impl.h                   | 7 +++++++
 v8/src/wasm/function-body-decoder.cc                       | 7 +++++++
 v8/src/wasm/function-compiler.cc                           | 7 +++++++
 v8/src/wasm/graph-builder-interface.cc                     | 7 +++++++
 v8/src/wasm/module-decoder-impl.h                          | 7 +++++++
 v8/src/wasm/module-decoder.cc                              | 7 +++++++
 v8/src/wasm/turboshaft-graph-interface.cc                  | 7 +++++++
 v8/src/wasm/wasm-code-manager.cc                           | 7 +++++++
 v8/src/wasm/wasm-disassembler.cc                           | 7 +++++++
 v8/src/wasm/wasm-objects.cc                                | 7 +++++++
 v8/src/wasm/wrappers.cc                                    | 7 +++++++
 19 files changed, 133 insertions(+)

diff --git a/v8/src/compiler/pipeline.cc b/v8/src/compiler/pipeline.cc
index 0181588..aad5780 100644
--- a/v8/src/compiler/pipeline.cc
+++ b/v8/src/compiler/pipeline.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/compiler/pipeline.h"
 
 #include <fstream>
diff --git a/v8/src/heap/factory.cc b/v8/src/heap/factory.cc
index d076477..3589d5d 100644
--- a/v8/src/heap/factory.cc
+++ b/v8/src/heap/factory.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/heap/factory.h"
 
 #include <algorithm>  // For copy
diff --git a/v8/src/heap/incremental-marking.cc b/v8/src/heap/incremental-marking.cc
index fdccd90..a47ef23 100644
--- a/v8/src/heap/incremental-marking.cc
+++ b/v8/src/heap/incremental-marking.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/heap/incremental-marking.h"
 
 #include <inttypes.h>
diff --git a/v8/src/snapshot/embedded/platform-embedded-file-writer-base.cc b/v8/src/snapshot/embedded/platform-embedded-file-writer-base.cc
index e4ca7bf..0d7ce98 100644
--- a/v8/src/snapshot/embedded/platform-embedded-file-writer-base.cc
+++ b/v8/src/snapshot/embedded/platform-embedded-file-writer-base.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/snapshot/embedded/platform-embedded-file-writer-base.h"
 
 #include <string>
diff --git a/v8/src/snapshot/embedded/platform-embedded-file-writer-win.cc b/v8/src/snapshot/embedded/platform-embedded-file-writer-win.cc
index 39a9935..211d4fb 100644
--- a/v8/src/snapshot/embedded/platform-embedded-file-writer-win.cc
+++ b/v8/src/snapshot/embedded/platform-embedded-file-writer-win.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/snapshot/embedded/platform-embedded-file-writer-win.h"
 
 #include <algorithm>
diff --git a/v8/src/wasm/baseline/liftoff-compiler.cc b/v8/src/wasm/baseline/liftoff-compiler.cc
index f0238c2..0001603 100644
--- a/v8/src/wasm/baseline/liftoff-compiler.cc
+++ b/v8/src/wasm/baseline/liftoff-compiler.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/baseline/liftoff-compiler.h"
 
 #include "src/base/enum-set.h"
diff --git a/v8/src/wasm/constant-expression-interface.cc b/v8/src/wasm/constant-expression-interface.cc
index da2db67..9ad9b56 100644
--- a/v8/src/wasm/constant-expression-interface.cc
+++ b/v8/src/wasm/constant-expression-interface.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/constant-expression-interface.h"
 
 #include "src/base/overflowing-math.h"
diff --git a/v8/src/wasm/constant-expression.cc b/v8/src/wasm/constant-expression.cc
index 046ceed..c012c95 100644
--- a/v8/src/wasm/constant-expression.cc
+++ b/v8/src/wasm/constant-expression.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/constant-expression.h"
 
 #include "src/handles/handles.h"
diff --git a/v8/src/wasm/function-body-decoder-impl.h b/v8/src/wasm/function-body-decoder-impl.h
index 2906601..ef64cb4 100644
--- a/v8/src/wasm/function-body-decoder-impl.h
+++ b/v8/src/wasm/function-body-decoder-impl.h
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #if !V8_ENABLE_WEBASSEMBLY
 #error This header should only be included if WebAssembly is enabled.
 #endif  // !V8_ENABLE_WEBASSEMBLY
diff --git a/v8/src/wasm/function-body-decoder.cc b/v8/src/wasm/function-body-decoder.cc
index 7b08db3..17b15d0 100644
--- a/v8/src/wasm/function-body-decoder.cc
+++ b/v8/src/wasm/function-body-decoder.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/function-body-decoder.h"
 
 #include "src/utils/ostreams.h"
diff --git a/v8/src/wasm/function-compiler.cc b/v8/src/wasm/function-compiler.cc
index 8687e59..24d7599 100644
--- a/v8/src/wasm/function-compiler.cc
+++ b/v8/src/wasm/function-compiler.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/function-compiler.h"
 
 #include "src/codegen/compiler.h"
diff --git a/v8/src/wasm/graph-builder-interface.cc b/v8/src/wasm/graph-builder-interface.cc
index 0e98427..2f585f1 100644
--- a/v8/src/wasm/graph-builder-interface.cc
+++ b/v8/src/wasm/graph-builder-interface.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/graph-builder-interface.h"
 
 #include "src/base/vector.h"
diff --git a/v8/src/wasm/module-decoder-impl.h b/v8/src/wasm/module-decoder-impl.h
index 6b70645..d847a88 100644
--- a/v8/src/wasm/module-decoder-impl.h
+++ b/v8/src/wasm/module-decoder-impl.h
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #if !V8_ENABLE_WEBASSEMBLY
 #error This header should only be included if WebAssembly is enabled.
 #endif  // !V8_ENABLE_WEBASSEMBLY
diff --git a/v8/src/wasm/module-decoder.cc b/v8/src/wasm/module-decoder.cc
index d7006f0..5e7c7af 100644
--- a/v8/src/wasm/module-decoder.cc
+++ b/v8/src/wasm/module-decoder.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/module-decoder.h"
 
 #include "src/logging/metrics.h"
diff --git a/v8/src/wasm/turboshaft-graph-interface.cc b/v8/src/wasm/turboshaft-graph-interface.cc
index f431b0f..3e29fc9 100644
--- a/v8/src/wasm/turboshaft-graph-interface.cc
+++ b/v8/src/wasm/turboshaft-graph-interface.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/turboshaft-graph-interface.h"
 
 #include "include/v8-fast-api-calls.h"
diff --git a/v8/src/wasm/wasm-code-manager.cc b/v8/src/wasm/wasm-code-manager.cc
index 7572437..e3e55a9 100644
--- a/v8/src/wasm/wasm-code-manager.cc
+++ b/v8/src/wasm/wasm-code-manager.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/wasm-code-manager.h"
 
 #include <algorithm>
diff --git a/v8/src/wasm/wasm-disassembler.cc b/v8/src/wasm/wasm-disassembler.cc
index 1690ca0..12a0beb 100644
--- a/v8/src/wasm/wasm-disassembler.cc
+++ b/v8/src/wasm/wasm-disassembler.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/wasm-disassembler.h"
 
 #include <iomanip>
diff --git a/v8/src/wasm/wasm-objects.cc b/v8/src/wasm/wasm-objects.cc
index 771fd4d..4e7e216 100644
--- a/v8/src/wasm/wasm-objects.cc
+++ b/v8/src/wasm/wasm-objects.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/wasm/wasm-objects.h"
 
 #include "src/base/iterator.h"
diff --git a/v8/src/wasm/wrappers.cc b/v8/src/wasm/wrappers.cc
index 9746edd..84ab3b0 100644
--- a/v8/src/wasm/wrappers.cc
+++ b/v8/src/wasm/wrappers.cc
@@ -2,6 +2,13 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+// The PowerPC-era Mac OS X SDK gates the C99 format macros for C++
+// behind __STDC_FORMAT_MACROS, which must be defined before the first
+// (possibly transitive) inclusion of <inttypes.h>.
+#ifndef __STDC_FORMAT_MACROS
+#define __STDC_FORMAT_MACROS 1
+#endif
+
 #include "src/codegen/bailout-reason.h"
 #include "src/compiler/linkage.h"
 #include "src/compiler/turboshaft/index.h"
-- 
2.43.0

