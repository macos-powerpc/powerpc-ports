--- erts/emulator/drivers/common/inet_drv.c	2026-02-27 01:14:09.000000000 +0800
+++ erts/emulator/drivers/common/inet_drv.c	2026-03-01 18:27:54.000000000 +0800
@@ -14496,7 +14496,9 @@
         switch(buf[2]) {
         case INET_PROTO_DEFAULT: protocol = 0; break;
         case INET_PROTO_UDP: protocol = IPPROTO_UDP; break;
+#ifdef HAVE_SCTP
         case INET_PROTO_SCTP: protocol = IPPROTO_SCTP; break;
+#endif
         default:
             return ctl_xerror(str_eprotonosupport, rbuf, rsize);
         }
