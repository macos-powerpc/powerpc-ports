--- src/include/main/database.h	2026-09-02 06:12:38.000000000 +0800
+++ src/include/main/database.h	2026-09-10 12:36:40.000000000 +0800
@@ -7,9 +7,12 @@
 #include <string_view>
 #include <vector>
 
-#if defined(__APPLE__)
+#if defined(__APPLE__) && defined(QOS_CLASS_DEFAULT)
+#define LB_HAS_QOS
+#ifdef LB_HAS_QOS
 #include <pthread/qos.h>
 #endif
+#endif
 
 #include "common/api.h"
 #include "common/database_lifecycle_manager.h"
@@ -80,7 +83,7 @@
         bool forceCheckpointOnClose = true, bool throwOnWalReplayFailure = false,
         bool enableChecksums = true, bool enableMultiWrites = false,
         bool enableDefaultHashIndex = true
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
         ,
         uint32_t threadQos = QOS_CLASS_DEFAULT
 #endif
@@ -98,7 +101,7 @@
     bool enableChecksums;
     bool enableMultiWrites;
     bool enableDefaultHashIndex;
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     uint32_t threadQos;
 #endif
 };

--- src/c_api/database.cpp	2026-09-02 06:12:38.000000000 +0800
+++ src/c_api/database.cpp	2026-09-10 13:02:10.000000000 +0800
@@ -17,7 +17,7 @@
             config.checkpoint_threshold, true, config.throw_on_wal_replay_failure,
             config.enable_checksums, config.enable_multi_writes, config.enable_default_hash_index);
 
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
         systemConfig.threadQos = config.thread_qos;
 #endif
         out_database->_database = new Database(database_path_str, systemConfig);
@@ -54,7 +54,7 @@
     cSystemConfig.enable_checksums = config.enableChecksums;
     cSystemConfig.enable_multi_writes = config.enableMultiWrites;
     cSystemConfig.enable_default_hash_index = config.enableDefaultHashIndex;
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     cSystemConfig.thread_qos = config.threadQos;
 #endif
     return cSystemConfig;

--- src/include/common/task_system/task_scheduler.h	2026-09-02 06:12:38.000000000 +0800
+++ src/include/common/task_system/task_scheduler.h	2026-09-10 13:17:02.000000000 +0800
@@ -6,6 +6,12 @@
 #include <thread>
 #endif
 
+#ifdef __APPLE__
+#ifdef QOS_CLASS_DEFAULT
+#define LB_HAS_QOS
+#endif
+#endif
+
 #include "common/task_system/task.h"
 #include "processor/execution_context.h"
 
@@ -42,7 +48,7 @@
 #ifndef __SINGLE_THREADED__
 class LBUG_API TaskScheduler {
 public:
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     explicit TaskScheduler(uint64_t numWorkerThreads, uint32_t threadQos);
 #else
     explicit TaskScheduler(uint64_t numWorkerThreads);
@@ -79,7 +85,7 @@
     std::mutex taskSchedulerMtx;
     std::condition_variable cv;
     uint64_t nextScheduledTaskID;
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     uint32_t threadQos; // Thread quality of service for worker threads.
 #endif
 };

--- src/common/task_system/task_scheduler.cpp	2026-09-02 06:12:38.000000000 +0800
+++ src/common/task_system/task_scheduler.cpp	2026-09-10 13:15:28.000000000 +0800
@@ -6,22 +6,23 @@
 
 #if defined(__APPLE__)
 #include <pthread.h>
-
+#ifdef LB_HAS_QOS
 #include <pthread/qos.h>
 #endif
+#endif
 
 namespace lbug {
 namespace common {
 
 #ifndef __SINGLE_THREADED__
 
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
 TaskScheduler::TaskScheduler(uint64_t numWorkerThreads, uint32_t threadQos)
 #else
 TaskScheduler::TaskScheduler(uint64_t numWorkerThreads)
 #endif
     : stopWorkerThreads{false}, nextScheduledTaskID{0} {
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     this->threadQos = threadQos;
 #endif
     for (auto n = 0u; n < numWorkerThreads; ++n) {
@@ -104,7 +105,7 @@
 }
 
 void TaskScheduler::runWorkerThread() {
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     qos_class_t qosClass = (qos_class_t)threadQos;
     if (qosClass != QOS_CLASS_DEFAULT && qosClass != QOS_CLASS_UNSPECIFIED) {
         auto pthreadQosStatus = pthread_set_qos_class_self_np(qosClass, 0);

--- src/main/database.cpp	2026-09-02 06:12:38.000000000 +0800
+++ src/main/database.cpp	2026-09-10 14:03:56.000000000 +0800
@@ -11,6 +11,8 @@
 
 #if defined(_WIN32)
 #include <windows.h>
+#elif defined(__APPLE__)
+#include <sys/sysctl.h>
 #else
 #include <unistd.h>
 #endif
@@ -36,7 +38,7 @@
     bool readOnly, uint64_t maxDBSize, bool autoCheckpoint, uint64_t checkpointThreshold,
     bool forceCheckpointOnClose, bool throwOnWalReplayFailure, bool enableChecksums,
     bool enableMultiWrites, bool enableDefaultHashIndex
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     ,
     uint32_t threadQos
 #endif
@@ -46,7 +48,7 @@
       forceCheckpointOnClose{forceCheckpointOnClose},
       throwOnWalReplayFailure(throwOnWalReplayFailure), enableChecksums(enableChecksums),
       enableMultiWrites{enableMultiWrites}, enableDefaultHashIndex{enableDefaultHashIndex} {
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     this->threadQos = threadQos;
 #endif
     if (bufferPoolSize_ == -1u || bufferPoolSize_ == 0) {
@@ -55,6 +57,10 @@
         status.dwLength = sizeof(status);
         GlobalMemoryStatusEx(&status);
         auto systemMemSize = (std::uint64_t)status.ullTotalPhys;
+#elif defined(__APPLE__)
+        std::uint64_t systemMemSize = 0;
+        size_t len = sizeof(systemMemSize);
+        sysctlbyname("hw.memsize", &systemMemSize, &len, nullptr, 0);
 #else
         auto systemMemSize = static_cast<std::uint64_t>(sysconf(_SC_PHYS_PAGES)) *
                              static_cast<std::uint64_t>(sysconf(_SC_PAGESIZE));
@@ -116,7 +122,7 @@
 
     bufferManager = initBmFunc(*this);
     memoryManager = std::make_unique<MemoryManager>(bufferManager.get(), vfs.get());
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     queryProcessor =
         std::make_unique<processor::QueryProcessor>(dbConfig->maxNumThreads, dbConfig->threadQos);
 #else

--- src/main/db_config.cpp	2026-09-02 06:12:38.000000000 +0800
+++ src/main/db_config.cpp	2026-09-10 14:07:40.000000000 +0800
@@ -38,7 +38,7 @@
       throwOnWalReplayFailure(systemConfig.throwOnWalReplayFailure),
       enableChecksums(systemConfig.enableChecksums),
       enableDefaultHashIndex{systemConfig.enableDefaultHashIndex}, enableSpillingToDisk{true} {
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     this->threadQos = systemConfig.threadQos;
 #endif
 }

--- src/include/processor/processor.h	2026-09-02 06:12:38.000000000 +0800
+++ src/include/processor/processor.h	2026-09-10 13:53:25.000000000 +0800
@@ -13,7 +13,7 @@
 class QueryProcessor {
 
 public:
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
     explicit QueryProcessor(uint64_t numThreads, uint32_t threadQos);
 #else
     explicit QueryProcessor(uint64_t numThreads);

--- src/processor/processor.cpp	2026-09-02 06:12:38.000000000 +0800
+++ src/processor/processor.cpp	2026-09-10 14:19:31.000000000 +0800
@@ -13,7 +13,7 @@
 namespace lbug {
 namespace processor {
 
-#if defined(__APPLE__)
+#if defined(LB_HAS_QOS)
 QueryProcessor::QueryProcessor(uint64_t numThreads, uint32_t threadQos) {
     taskScheduler = std::make_unique<TaskScheduler>(numThreads, threadQos);
 }

--- src/storage/buffer_manager/vm_region.cpp	2026-09-02 06:12:38.000000000 +0800
+++ src/storage/buffer_manager/vm_region.cpp	2026-09-10 14:42:05.000000000 +0800
@@ -22,6 +22,10 @@
 #define MAP_NORESERVE 0
 #endif
 
+#ifndef MAP_ANONYMOUS
+#define MAP_ANONYMOUS MAP_ANON
+#endif
+
 using namespace lbug::common;
 
 namespace lbug {
