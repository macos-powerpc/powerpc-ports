diff --git a/3rd/bee.lua/bee/filewatch/filewatch.h b/3rd/bee.lua/bee/filewatch/filewatch.h
index e7befd9..feeee01 100644
--- a/3rd/bee.lua/bee/filewatch/filewatch.h
+++ b/3rd/bee.lua/bee/filewatch/filewatch.h
@@ -8,6 +8,15 @@
 #if defined(_WIN32)
 #    include <list>
 #elif defined(__APPLE__)
+// Apple's AssertMacros.h (pulled in transitively by CoreServices.h) defines
+// bare `check`/`require`/`verify` macros by default, which collide with
+// unrelated identifiers of the same name elsewhere in this codebase (e.g.
+// fmt's `detail::is_map::check` template, reached from bee/nonstd/print.h
+// when <print> isn't available). Nothing here uses Apple's macros, so
+// suppress the underscore-less names.
+#    ifndef __ASSERT_MACROS_DEFINE_VERSIONS_WITHOUT_UNDERSCORES
+#        define __ASSERT_MACROS_DEFINE_VERSIONS_WITHOUT_UNDERSCORES 0
+#    endif
 #    include <CoreServices/CoreServices.h>
 
 #    include <mutex>
