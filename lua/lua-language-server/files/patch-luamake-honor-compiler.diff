diff --git a/3rd/luamake/compile/ninja/android.ninja b/3rd/luamake/compile/ninja/android.ninja
index 552ed2f..a76a3af 100644
--- a/3rd/luamake/compile/ninja/android.ninja
+++ b/3rd/luamake/compile/ninja/android.ninja
@@ -2,7 +2,7 @@ ninja_required_version = 1.7
 builddir = build/android
 bin = $builddir/bin
 obj = $builddir/obj
-cc = clang
+cc = $$CC
 ar = ar
 luamake = luamake
 rule c_source_lua
diff --git a/3rd/luamake/compile/ninja/freebsd.ninja b/3rd/luamake/compile/ninja/freebsd.ninja
index 8f15d49..60be7d0 100644
--- a/3rd/luamake/compile/ninja/freebsd.ninja
+++ b/3rd/luamake/compile/ninja/freebsd.ninja
@@ -2,7 +2,7 @@ ninja_required_version = 1.7
 builddir = build/freebsd
 bin = $builddir/bin
 obj = $builddir/obj
-cc = clang
+cc = $$CC
 ar = ar
 luamake = luamake
 rule c_source_lua
diff --git a/3rd/luamake/compile/ninja/linux.ninja b/3rd/luamake/compile/ninja/linux.ninja
index b6a8d67..909e3e3 100644
--- a/3rd/luamake/compile/ninja/linux.ninja
+++ b/3rd/luamake/compile/ninja/linux.ninja
@@ -2,7 +2,7 @@ ninja_required_version = 1.7
 builddir = build/linux
 bin = $builddir/bin
 obj = $builddir/obj
-cc = gcc
+cc = $$CC
 ar = ar
 luamake = luamake
 rule c_source_lua
diff --git a/3rd/luamake/compile/ninja/macos.ninja b/3rd/luamake/compile/ninja/macos.ninja
index 2958a47..430f649 100644
--- a/3rd/luamake/compile/ninja/macos.ninja
+++ b/3rd/luamake/compile/ninja/macos.ninja
@@ -2,38 +2,38 @@ ninja_required_version = 1.7
 builddir = build/macos
 bin = $builddir/bin
 obj = $builddir/obj
-cc = clang
+cc = $$CC
+cxx = $$CXX
 ar = ar
 luamake = luamake
 rule c_source_lua
   command = $cc -MMD -MT $out -MF $out.d -std=c11 -O2 -Wall $
-    -mmacosx-version-min=10.15 -Ibee.lua/3rd/lua55 -DMAKE_LIB $
-    -DLUA_USE_MACOSX -DNDEBUG -Wunguarded-availability -o $out -c $in
+    -Ibee.lua/3rd/lua55 -DMAKE_LIB $
+    -DLUA_USE_MACOSX -DNDEBUG $${CPPFLAGS} $${CFLAGS} -o $out -c $in
   description = Compile C   $out
   deps = gcc
   depfile = $out.d
 build $obj/source_lua/onelua.obj: c_source_lua bee.lua/3rd/lua55/onelua.c
 rule cxx_source_bee
-  command = $cc -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
-    -fvisibility=hidden -mmacosx-version-min=10.15 -Ibee.lua/3rd/lua55 $
-    -DNDEBUG -Wunguarded-availability -o $out -c $in
+  command = $cxx -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
+    -fvisibility=hidden -Ibee.lua/3rd/lua55 $
+    -DNDEBUG $${CPPFLAGS} $${CXXFLAGS} -o $out -c $in
   description = Compile C++ $out
   deps = gcc
   depfile = $out.d
 build $obj/source_bee/lua-seri.obj: cxx_source_bee $
     bee.lua/3rd/lua-seri/lua-seri.cpp
 rule cxx_source_bee_1
-  command = $cc -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
-    -fvisibility=hidden -mmacosx-version-min=10.15 -DNDEBUG $
-    -Wunguarded-availability -o $out -c $in
+  command = $cxx -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
+    -fvisibility=hidden -DNDEBUG $${CPPFLAGS} $${CXXFLAGS} -o $out -c $in
   description = Compile C++ $out
   deps = gcc
   depfile = $out.d
 build $obj/source_bee/format.obj: cxx_source_bee_1 bee.lua/3rd/fmt/format.cc
 rule cxx_source_bee_2
-  command = $cc -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
-    -fvisibility=hidden -mmacosx-version-min=10.15 -Ibee.lua $
-    -Ibee.lua/3rd/lua55 -DNDEBUG -Wunguarded-availability -o $out -c $in
+  command = $cxx -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
+    -fvisibility=hidden -Ibee.lua $
+    -Ibee.lua/3rd/lua55 -DNDEBUG $${CPPFLAGS} $${CXXFLAGS} -o $out -c $in
   description = Compile C++ $out
   deps = gcc
   depfile = $out.d
@@ -75,19 +75,17 @@ build $obj/source_bee/spinlock.obj: cxx_source_bee_2 $
 build $obj/source_bee/version.obj: cxx_source_bee_2 bee.lua/bee/version.cpp
 rule c_source_bee
   command = $cc -MMD -MT $out -MF $out.d -std=c11 -O2 -Wall $
-    -fvisibility=hidden -mmacosx-version-min=10.15 -Ibee.lua $
-    -Ibee.lua/3rd/lua55 -DBEE_STATIC -DNDEBUG -Wunguarded-availability -o $
-    $out -c $in
+    -fvisibility=hidden -Ibee.lua $
+    -Ibee.lua/3rd/lua55 -DBEE_STATIC -DNDEBUG $${CPPFLAGS} $${CFLAGS} -o $out -c $in
   description = Compile C   $out
   deps = gcc
   depfile = $out.d
 build $obj/source_bee/bee_newstate.obj: c_source_bee $
     bee.lua/3rd/lua-patch/bee_newstate.c
 rule cxx_source_bee_3
-  command = $cc -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
-    -fvisibility=hidden -mmacosx-version-min=10.15 -Ibee.lua $
-    -Ibee.lua/3rd/lua55 -DBEE_STATIC -DNDEBUG -Wunguarded-availability -o $
-    $out -c $in
+  command = $cxx -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
+    -fvisibility=hidden -Ibee.lua $
+    -Ibee.lua/3rd/lua55 -DBEE_STATIC -DNDEBUG $${CPPFLAGS} $${CXXFLAGS} -o $out -c $in
   description = Compile C++ $out
   deps = gcc
   depfile = $out.d
@@ -120,10 +118,9 @@ build $obj/source_bee/lua_thread.obj: cxx_source_bee_3 $
 build $obj/source_bee/lua_time.obj: cxx_source_bee_3 $
     bee.lua/binding/lua_time.cpp
 rule cxx_source_bootstrap
-  command = $cc -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
-    -fvisibility=hidden -mmacosx-version-min=10.15 -Ibee.lua $
-    -Ibee.lua/3rd/lua55 -DLUA_USE_MACOSX -DNDEBUG -Wunguarded-availability $
-    -o $out -c $in
+  command = $cxx -MMD -MT $out -MF $out.d -std=c++17 -fno-rtti -O2 -Wall $
+    -fvisibility=hidden -Ibee.lua $
+    -Ibee.lua/3rd/lua55 -DLUA_USE_MACOSX -DNDEBUG $${CPPFLAGS} $${CXXFLAGS} -o $out -c $in
   description = Compile C++ $out
   deps = gcc
   depfile = $out.d
@@ -132,8 +129,8 @@ build $obj/source_bootstrap/bootstrap_init.obj: cxx_source_bootstrap $
 build $obj/source_bootstrap/main.obj: cxx_source_bootstrap $
     bee.lua/bootstrap/main.cpp
 rule link_bootstrap
-  command = $cc $in -o $out -framework Foundation -framework CoreFoundation $
-    -framework CoreServices -lm -ldl -lstdc++ -Wl,-S,-x
+  command = $cxx $in -o $out -framework Foundation -framework CoreFoundation $
+    -framework CoreServices -lm -ldl -lstdc++ -Wl,-S,-x $${LDFLAGS}
   description = Link    Exe $out
 rule copy
   command = ditto $in$input $out 1>/dev/null
diff --git a/3rd/luamake/compile/ninja/netbsd.ninja b/3rd/luamake/compile/ninja/netbsd.ninja
index cbc13f7..2b9553a 100644
--- a/3rd/luamake/compile/ninja/netbsd.ninja
+++ b/3rd/luamake/compile/ninja/netbsd.ninja
@@ -2,7 +2,7 @@ ninja_required_version = 1.7
 builddir = build/netbsd
 bin = $builddir/bin
 obj = $builddir/obj
-cc = gcc
+cc = $$CC
 ar = ar
 luamake = luamake
 rule c_source_lua
diff --git a/3rd/luamake/compile/ninja/openbsd.ninja b/3rd/luamake/compile/ninja/openbsd.ninja
index 4a1b546..3f44c67 100644
--- a/3rd/luamake/compile/ninja/openbsd.ninja
+++ b/3rd/luamake/compile/ninja/openbsd.ninja
@@ -2,7 +2,7 @@ ninja_required_version = 1.7
 builddir = build/openbsd
 bin = $builddir/bin
 obj = $builddir/obj
-cc = clang
+cc = $$CC
 ar = ar
 luamake = luamake
 rule c_source_lua
diff --git a/3rd/luamake/scripts/compiler/gcc.lua b/3rd/luamake/scripts/compiler/gcc.lua
index 50cd320..d449e30 100644
--- a/3rd/luamake/scripts/compiler/gcc.lua
+++ b/3rd/luamake/scripts/compiler/gcc.lua
@@ -18,6 +18,17 @@ function gcc.update_flags(flags, cflags, cxxflags, attribute, name)
 end
 
 function gcc.update_ldflags(ldflags, attribute)
+    -- Only ever populated on Apple targets (see bee.lua/compile/common.lua);
+    -- clang.lua has always handled this, but gcc flavor was previously
+    -- never actually selectable on macOS (globals.lua forced clang there),
+    -- so this path was simply never exercised until now. GCC's Darwin
+    -- driver understands -framework the same way clang's does.
+    if attribute.frameworks then
+        for _, framework in ipairs(attribute.frameworks) do
+            ldflags[#ldflags+1] = "-framework"
+            ldflags[#ldflags+1] = framework
+        end
+    end
     if attribute.crt == "dynamic" then
         ldflags[#ldflags+1] = "-lstdc++"
     else
diff --git a/3rd/luamake/scripts/globals.lua b/3rd/luamake/scripts/globals.lua
index 97a02bb..2598867 100644
--- a/3rd/luamake/scripts/globals.lua
+++ b/3rd/luamake/scripts/globals.lua
@@ -19,6 +19,10 @@ globals.hostshell = globals.hostshell or (function ()
     return "sh"
 end)()
 globals.os = globals.os or globals.hostos
+if globals.os ~= "windows" then
+    globals.cc = globals.cc or os.getenv "CC"
+    globals.cxx = globals.cxx or os.getenv "CXX"
+end
 globals.compiler = globals.compiler or (function ()
     if globals.os == "windows" then
         if globals.hostshell == "cmd" then
@@ -33,6 +37,12 @@ globals.compiler = globals.compiler or (function ()
     if globals.os == "emscripten" then
         return "emcc"
     end
+    -- Honor an explicitly configured (or environment-provided) compiler
+    -- instead of always assuming clang is available/desired.
+    local cc = globals.cc or globals.cxx
+    if cc and (cc:match "gcc" or cc:match "g%+%+") then
+        return "gcc"
+    end
     return "clang"
 end)()
 
diff --git a/3rd/luamake/scripts/writer.lua b/3rd/luamake/scripts/writer.lua
index 72fadba..f82f43b 100644
--- a/3rd/luamake/scripts/writer.lua
+++ b/3rd/luamake/scripts/writer.lua
@@ -128,6 +128,19 @@ local function reslove_args(args)
     return table.concat(command, " ")
 end
 
+-- Let CPPFLAGS/CFLAGS/CXXFLAGS/LDFLAGS from the environment augment (not
+-- replace) whatever luamake would otherwise generate, the way a normal
+-- ./configure-based build honors them. This is how a build system such as
+-- MacPorts' passes in flags a PortGroup adds (e.g. extra -isystem/-L/-l
+-- flags for a compatibility shim library) without luamake needing to know
+-- about it.
+local function env_flags(name)
+    local v = os.getenv(name)
+    if v and v ~= "" then
+        return v
+    end
+end
+
 local function array_remove(t, k)
     for pos, v in ipairs(t) do
         if v == k then
@@ -153,6 +166,8 @@ local function update_flags(flags, cflags, cxxflags, attribute, name, rule)
         end
     end
     cc.update_flags(flags, cflags, cxxflags, attribute, name, rule)
+    cflags[#cflags+1] = env_flags "CFLAGS"
+    cxxflags[#cxxflags+1] = env_flags "CXXFLAGS"
 
     if attribute.includes then
         for _, inc in ipairs(attribute.includes) do
@@ -196,6 +211,7 @@ local function update_flags(flags, cflags, cxxflags, attribute, name, rule)
     if attribute.flags then
         tbl_append(flags, attribute.flags)
     end
+    flags[#flags+1] = env_flags "CPPFLAGS"
 end
 
 local function update_ldflags(ldflags, attribute, name)
@@ -226,6 +242,7 @@ local function update_ldflags(ldflags, attribute, name)
     end
 
     cc.update_ldflags(ldflags, attribute, name)
+    ldflags[#ldflags+1] = env_flags "LDFLAGS"
 end
 
 local enum_onoff <const> = { on = true, off = true }
@@ -345,7 +362,14 @@ local function generate(rule, attribute, name)
         if attribute.ldflags then
             tbl_append(ldflags, attribute.ldflags)
         end
-        if globals.compiler == "clang" and attribute.frameworks then
+        -- attribute.frameworks is only ever set inside macos/ios platform
+        -- blocks (never gated by compiler in the source data itself), so
+        -- gating it here by `compiler == "clang"` was wrong on its own
+        -- terms even before gcc became a real macOS option: it silently
+        -- dropped every framework a source_set declared (e.g. bee.lua's
+        -- source_bee) whenever some other compiler was selected, with no
+        -- corresponding check anywhere else to catch the gap.
+        if attribute.frameworks then
             for _, framework in ipairs(attribute.frameworks) do
                 ldflags[#ldflags+1] = "-framework"
                 ldflags[#ldflags+1] = framework
