--- plugins/adb/CMakeLists.txt	2026-08-28 04:32:49.000000000 +0800
+++ plugins/adb/CMakeLists.txt	2026-09-11 17:46:18.000000000 +0800
@@ -15,7 +15,7 @@
 add_library(adb MODULE ${SOURCES})
 
 # Link with required libraries
-target_link_libraries(adb utils far2l)
+target_link_libraries(adb utils far2l atomic)
 
 # forkpty/openpty: libutil on Linux + BSDs (macOS has it in libSystem).
 # dladdr: libdl on Linux/glibc; BSDs and macOS have it in libc.

--- plugins/mtp/CMakeLists.txt	2026-08-28 04:32:49.000000000 +0800
+++ plugins/mtp/CMakeLists.txt	2026-09-11 17:48:16.000000000 +0800
@@ -18,7 +18,7 @@
 # libusb_static is also pulled transitively via vendored libmtp_static, but link it explicitly so
 # the plugin (which uses libusb directly — see ReadDeviceSerialViaLibusb, SharedLibusbContext) still
 # resolves when libmtp_static is an INTERFACE wrapper around system libmtp.
-target_link_libraries(mtp utils far2l libmtp_static libusb_static)
+target_link_libraries(mtp utils far2l libmtp_static libusb_static atomic)
 
 if(APPLE)
     # CoreFoundation: CFPreferences{SetValue,Synchronize} for icdd bypass.
