# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           cmake 1.1
PortGroup           github 1.0
PortGroup           legacysupport 1.1

# O_CLOEXEC
legacysupport.newest_darwin_requires_legacy 10

github.setup        contour-terminal contour 0.6.1.7494 v
revision            0
categories          sysutils
license             Apache-2
maintainers         {@barracuda156 macos-powerpc.org:barracuda} openmaintainer
description         ${name} – a modern & actually fast Terminal Emulator
long_description    {*}${description}
checksums           rmd160  0eff96cbb4301f39d9d7d2d1b218c9e0d5f251cc \
                    sha256  15090a08cf05124bf05074130cc6460d1d34c6d80f1058c63202effa032b8f6f \
                    size    10329146
github.tarball_from archive

depends_build-append \
                    port:boxed-cpp \
                    port:gslpp \
                    path:bin/pkg-config:pkgconfig \
                    port:reflection-cpp

depends_lib-append  port:freetype \
                    path:lib/pkgconfig/harfbuzz.pc:harfbuzz \
                    port:libunicode \
                    port:range-v3 \
                    port:yaml-cpp

depends_test-append port:catch2

# Fix CoreText APIs for 10.6:
patchfiles-append   patch-coretext.diff

# Workaroud this issue: https://github.com/ericniebler/range-v3/issues/1855
patchfiles-append   patch-fix-ObjC++.diff

compiler.cxx_standard   2020

configure.objcxxflags-append \
                    -std=c++20

configure.args-append \
                    -DCONTOUR_BUILD_NATIVE=OFF \
                    -DCONTOUR_BUILD_STATIC=OFF \
                    -DCONTOUR_BUILD_WITH_MIMALLOC=OFF \
                    -DCONTOUR_FRONTEND_GUI=OFF \
                    -DCONTOUR_INSTALL_TOOLS=ON \
                    -DCONTOUR_TESTING=OFF \
                    -DCONTOUR_USE_CPM=OFF

# GUI-less target does not work atm: https://github.com/contour-terminal/contour/issues/1780
# The port builds several libraries which are used by contour app, but either its GUI should
# also be fixed, or otherwise alternative solution found.
