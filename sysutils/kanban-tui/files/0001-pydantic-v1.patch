diff --git a/pyproject.toml b/pyproject.toml
index 4d98ff5..c288ce7 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -10,8 +10,7 @@ dependencies = [
     "textual-plotext>=1.0.0",
     "tzdata>=2025.2",
     "python-dateutil>=2.9.0.post0",
-    "pydantic>=2.11.7",
-    "pydantic-settings>=2.10.1",
+    "pydantic>=1.10.26",
     "tomli-w>=1.2.0",
     "xdg-base-dirs>=6.0.2",
     "textual-jumper>=0.2.0",
diff --git a/src/kanban_tui/backends/auth.py b/src/kanban_tui/backends/auth.py
index 70c27ed..f2a6748 100644
--- a/src/kanban_tui/backends/auth.py
+++ b/src/kanban_tui/backends/auth.py
@@ -1,13 +1,9 @@
 import os
+import tomllib
 from pathlib import Path
 
 import tomli_w
-from pydantic import BaseModel, Field
-from pydantic_settings import (
-    BaseSettings,
-    PydanticBaseSettingsSource,
-    TomlConfigSettingsSource,
-)
+from pydantic import BaseModel, BaseSettings, Field
 
 from kanban_tui.constants import AUTH_FILE
 
@@ -37,37 +33,24 @@ class AuthSettings(BaseSettings):
         if config_from_env:
             path = config_from_env
         with open(Path(path).resolve(), "w") as toml_file:
-            dumb = tomli_w.dumps(self.model_dump())
+            dumb = tomli_w.dumps(self.dict())
             toml_file.write(dumb)
 
-    @classmethod
-    def settings_customise_sources(
-        cls,
-        settings_cls: type[BaseSettings],
-        init_settings: PydanticBaseSettingsSource,
-        env_settings: PydanticBaseSettingsSource,
-        dotenv_settings: PydanticBaseSettingsSource,
-        file_secret_settings: PydanticBaseSettingsSource,
-    ) -> tuple[PydanticBaseSettingsSource, ...]:
-        config_from_env = os.getenv("KANBAN_TUI_AUTH_FILE")
-        conf_file = Path(config_from_env).resolve() if config_from_env else AUTH_FILE
-
-        default_sources = (
-            init_settings,
-            env_settings,
-            dotenv_settings,
-            file_secret_settings,
-        )
-
-        if conf_file.exists():
-            return (
-                init_settings,
-                TomlConfigSettingsSource(settings_cls, conf_file),
-                env_settings,
-                dotenv_settings,
-                file_secret_settings,
+    class Config:
+        @classmethod
+        def customise_sources(cls, init_settings, env_settings, file_secret_settings):
+            config_from_env = os.getenv("KANBAN_TUI_AUTH_FILE")
+            conf_file = (
+                Path(config_from_env).resolve() if config_from_env else AUTH_FILE
             )
-        return default_sources
+
+            def toml_settings(settings: BaseSettings) -> dict:
+                with conf_file.open("rb") as f:
+                    return tomllib.load(f)
+
+            if conf_file.exists():
+                return (init_settings, toml_settings, env_settings, file_secret_settings)
+            return (init_settings, env_settings, file_secret_settings)
 
 
 def init_auth_file(auth_file_path: str) -> str:
diff --git a/src/kanban_tui/classes/task.py b/src/kanban_tui/classes/task.py
index e085041..a64e948 100644
--- a/src/kanban_tui/classes/task.py
+++ b/src/kanban_tui/classes/task.py
@@ -1,10 +1,31 @@
 from datetime import datetime, timedelta
 from typing import Any
 
-from pydantic import BaseModel, Field, computed_field
-
-
-class Task(BaseModel):
+from pydantic import BaseModel, Field
+
+# pydantic v1 has no computed_field; these properties are added when dumping
+_COMPUTED_FIELDS = (
+    "days_left",
+    "days_since_creation",
+    "finished",
+    "is_blocked",
+    "has_dependents",
+)
+
+
+class _ComputedFieldsMixin:
+    # A mixin so the method name does not shadow builtin dict in Task's body
+    def dict(self, **kwargs):
+        data = super().dict(**kwargs)
+        for name in _COMPUTED_FIELDS:
+            value = getattr(self, name)
+            if value is None and kwargs.get("exclude_none"):
+                continue
+            data[name] = value
+        return data
+
+
+class Task(_ComputedFieldsMixin, BaseModel):
     task_id: int
     title: str
     column: int
@@ -17,7 +38,8 @@ class Task(BaseModel):
     description: str = ""
     blocked_by: list[int] = []  # Task IDs this task depends on
     blocking: list[int] = []  # Task IDs that depend on this task
-    metadata: dict[str, Any] = Field(default_factory=dict)  # Backend-specific extras
+    # plain default so exclude_defaults drops it like pydantic v2 does
+    metadata: dict[str, Any] = {}  # Backend-specific extras
 
     def get_days_since_creation(self) -> int:
         now = datetime.now().replace(microsecond=0)
@@ -80,28 +102,23 @@ class Task(BaseModel):
                 # Mark task as finished when moving to finish column
                 self.finish_task()
 
-    @computed_field  # type: ignore[prop-decorator]
     @property
     def days_left(self) -> int | None:
         return self.get_days_left_till_due()
 
-    @computed_field  # type: ignore[prop-decorator]
     @property
     def days_since_creation(self) -> int:
         return self.get_days_since_creation()
 
-    @computed_field  # type: ignore[prop-decorator]
     @property
     def finished(self) -> bool:
         return bool(self.finish_date)
 
-    @computed_field  # type: ignore[prop-decorator]
     @property
     def is_blocked(self) -> bool:
         """Returns True if this task has any dependencies (regardless of their status)."""
         return len(self.blocked_by) > 0
 
-    @computed_field  # type: ignore[prop-decorator]
     @property
     def has_dependents(self) -> bool:
         """Returns True if any other tasks depend on this one."""
diff --git a/src/kanban_tui/cli/board_commands.py b/src/kanban_tui/cli/board_commands.py
index 49046fc..b48c94d 100644
--- a/src/kanban_tui/cli/board_commands.py
+++ b/src/kanban_tui/cli/board_commands.py
@@ -1,7 +1,8 @@
 """CLI commands for kanban-tui board management"""
 
 import click
-from pydantic import TypeAdapter
+import json as jsonlib
+from pydantic.json import pydantic_encoder
 
 from kanban_tui.app import KanbanTui
 from kanban_tui.classes.board import Board
@@ -46,9 +47,10 @@ def list_boards(app: KanbanTui, json: bool):
             f"[red]--- Active Board has board_id = {active_board_id} ---[/]"
         )
         if json:
-            board_list = TypeAdapter(list[Board])
-            json_str = board_list.dump_json(boards, indent=4, exclude_none=True).decode(
-                "utf-8"
+            json_str = jsonlib.dumps(
+                [b.dict(exclude_none=True) for b in boards],
+                indent=4,
+                default=pydantic_encoder,
             )
             print_to_console(json_str)
         else:
diff --git a/src/kanban_tui/cli/category_commands.py b/src/kanban_tui/cli/category_commands.py
index ecd16a6..33b2c08 100644
--- a/src/kanban_tui/cli/category_commands.py
+++ b/src/kanban_tui/cli/category_commands.py
@@ -1,7 +1,8 @@
 """CLI commands for kanban-tui category management"""
 
 import click
-from pydantic import TypeAdapter
+import json as jsonlib
+from pydantic.json import pydantic_encoder
 from textual.color import Color, ColorParseError
 
 from kanban_tui.app import KanbanTui
@@ -43,10 +44,11 @@ def list_categories(app: KanbanTui, json: bool):
         print_to_console("No categories created yet.")
     else:
         if json:
-            category_list = TypeAdapter(list[Category])
-            json_str = category_list.dump_json(
-                categories, indent=4, exclude_none=True
-            ).decode("utf-8")
+            json_str = jsonlib.dumps(
+                [c.dict(exclude_none=True) for c in categories],
+                indent=4,
+                default=pydantic_encoder,
+            )
             print_to_console(json_str)
         else:
             for category in categories:
diff --git a/src/kanban_tui/cli/column_commands.py b/src/kanban_tui/cli/column_commands.py
index 3f3a8d5..3ee71f6 100644
--- a/src/kanban_tui/cli/column_commands.py
+++ b/src/kanban_tui/cli/column_commands.py
@@ -1,7 +1,8 @@
 """CLI commands for kanban-tui column management"""
 
 import click
-from pydantic import TypeAdapter
+import json as jsonlib
+from pydantic.json import pydantic_encoder
 
 from kanban_tui.app import KanbanTui
 from kanban_tui.classes.column import Column
@@ -53,9 +54,10 @@ def list_columns(app: KanbanTui, json: bool, board: None | int):
         print_to_console(f"There is no board with board_id = {board}.")
 
     if json:
-        column_list = TypeAdapter(list[Column])
-        json_str = column_list.dump_json(columns, indent=4, exclude_none=True).decode(
-            "utf-8"
+        json_str = jsonlib.dumps(
+            [c.dict(exclude_none=True) for c in columns],
+            indent=4,
+            default=pydantic_encoder,
         )
         print_to_console(json_str)
     else:
diff --git a/src/kanban_tui/cli/task_commands.py b/src/kanban_tui/cli/task_commands.py
index 2010500..17a73e2 100644
--- a/src/kanban_tui/cli/task_commands.py
+++ b/src/kanban_tui/cli/task_commands.py
@@ -3,7 +3,8 @@
 import datetime
 
 import click
-from pydantic import TypeAdapter
+import json as jsonlib
+from pydantic.json import pydantic_encoder
 
 from kanban_tui.app import KanbanTui
 from kanban_tui.classes.task import Task
@@ -107,10 +108,11 @@ def list_tasks(
 
     else:
         if json:
-            task_list = TypeAdapter(list[Task])
-            json_str = task_list.dump_json(
-                tasks, indent=4, exclude_none=True, exclude_defaults=True
-            ).decode("utf-8")
+            json_str = jsonlib.dumps(
+                [t.dict(exclude_none=True, exclude_defaults=True) for t in tasks],
+                indent=4,
+                default=pydantic_encoder,
+            )
             print_to_console(json_str)
         else:
             for task in tasks:
diff --git a/src/kanban_tui/config.py b/src/kanban_tui/config.py
index 6e581be..d72253e 100644
--- a/src/kanban_tui/config.py
+++ b/src/kanban_tui/config.py
@@ -1,17 +1,13 @@
 from __future__ import annotations
 
 import os
+import tomllib
 from contextvars import ContextVar
 from enum import StrEnum
 from pathlib import Path
 
 import tomli_w
-from pydantic import BaseModel, Field
-from pydantic_settings import (
-    BaseSettings,
-    PydanticBaseSettingsSource,
-    TomlConfigSettingsSource,
-)
+from pydantic import BaseModel, BaseSettings, Field
 
 from kanban_tui.constants import (
     AUTH_FILE,
@@ -150,37 +146,24 @@ class Settings(BaseSettings):
         config_from_env = os.getenv("KANBAN_TUI_CONFIG_FILE")
         if config_from_env:
             path = config_from_env
-        dump = tomli_w.dumps(self.model_dump())
+        dump = tomli_w.dumps(self.dict())
         Path(path).resolve().write_text(dump, encoding="utf-8")
 
-    @classmethod
-    def settings_customise_sources(
-        cls,
-        settings_cls: type[BaseSettings],
-        init_settings: PydanticBaseSettingsSource,
-        env_settings: PydanticBaseSettingsSource,
-        dotenv_settings: PydanticBaseSettingsSource,
-        file_secret_settings: PydanticBaseSettingsSource,
-    ) -> tuple[PydanticBaseSettingsSource, ...]:
-        config_from_env = os.getenv("KANBAN_TUI_CONFIG_FILE")
-        conf_file = Path(config_from_env).resolve() if config_from_env else CONFIG_FILE
-
-        default_sources = (
-            init_settings,
-            env_settings,
-            dotenv_settings,
-            file_secret_settings,
-        )
-
-        if conf_file.exists():
-            return (
-                init_settings,
-                TomlConfigSettingsSource(settings_cls, conf_file),
-                env_settings,
-                dotenv_settings,
-                file_secret_settings,
+    class Config:
+        @classmethod
+        def customise_sources(cls, init_settings, env_settings, file_secret_settings):
+            config_from_env = os.getenv("KANBAN_TUI_CONFIG_FILE")
+            conf_file = (
+                Path(config_from_env).resolve() if config_from_env else CONFIG_FILE
             )
-        return default_sources
+
+            def toml_settings(settings: BaseSettings) -> dict:
+                with conf_file.open("rb") as f:
+                    return tomllib.load(f)
+
+            if conf_file.exists():
+                return (init_settings, toml_settings, env_settings, file_secret_settings)
+            return (init_settings, env_settings, file_secret_settings)
 
 
 def init_config(config_path: str, database: str) -> str:
