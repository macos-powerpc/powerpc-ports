From b30241d24173c5f07f67d0c74c0339993c5989af Mon Sep 17 00:00:00 2001
From: Sergey Fedorov <barracuda@macos-powerpc.org>
Date: Tue, 1 Jul 2025 06:33:12 +0800
Subject: [PATCH 2/2] Follow-up

---
 src/bookmarkswidget.h  | 11 +++++------
 src/fontdialog.h       |  3 +--
 src/mainwindow.h       | 19 ++++++++++---------
 src/propertiesdialog.h | 17 ++++++++---------
 src/tab-switcher.h     |  7 ++++---
 src/termwidget.h       |  6 +++---
 src/termwidgetholder.h |  6 ++++--
 7 files changed, 35 insertions(+), 34 deletions(-)

diff --git a/src/bookmarkswidget.h b/src/bookmarkswidget.h
index a1838b0..b6edc8c 100644
--- a/src/bookmarkswidget.h
+++ b/src/bookmarkswidget.h
@@ -34,18 +34,17 @@ public:
 
     void setup();
 
-public slots:
-    void handleCommand(const QModelIndex& index);
-    void filter(const QString& str);
-    void clearFilterEdit();
-
 signals:
     void callCommand(const QString &cmd);
 
 private:
     BookmarksModel *m_model;
-};
 
+private slots:
+    void handleCommand(const QModelIndex& index);
+    void filter(const QString& str);
+    void clearFilterEdit();
+};
 
 class BookmarksModel : public QAbstractItemModel
 {
diff --git a/src/fontdialog.h b/src/fontdialog.h
index d90db56..8247a4c 100644
--- a/src/fontdialog.h
+++ b/src/fontdialog.h
@@ -29,10 +29,9 @@ public:
     explicit FontDialog(const QFont &f);
     QFont getFont();
 
-public slots:
+private slots:
     void setFontSample(const QFont &f);
     void setFontSize();
-
 };
 
 #endif
diff --git a/src/mainwindow.h b/src/mainwindow.h
index 16bc8ab..0c53ed5 100644
--- a/src/mainwindow.h
+++ b/src/mainwindow.h
@@ -97,27 +97,28 @@ private:
 public slots:
     void showHide();
     void updateDisabledActions();
-    void bookmarksWidget_callCommand(const QString& cmd);
-    void bookmarksDock_visibilityChanged(bool visible);
+
+private slots:
+    void on_consoleTabulator_currentChanged(int);
+    void propertiesChanged();
     void actAbout_triggered();
-    void onCurrentTitleChanged(int index);
+    void actProperties_triggered();
     void updateActionGroup(QAction *);
-    void propertiesChanged();
-    void toggleBorderless();
     void toggleBookmarks();
+    void toggleBorderless();
     void toggleTabBar();
     void toggleMenu();
-    void showFullscreen(bool fullscreen);
 
-private slots:
-    void on_consoleTabulator_currentChanged(int);
-    void actProperties_triggered();
+    void showFullscreen(bool fullscreen);
     void setKeepOpen(bool value);
     void find();
 
     void newTerminalWindow();
+    void bookmarksWidget_callCommand(const QString&);
+    void bookmarksDock_visibilityChanged(bool visible);
 
     void addNewTab(TerminalConfig cfg = TerminalConfig());
+    void onCurrentTitleChanged(int index);
 
     void handleHistory();
 };
diff --git a/src/propertiesdialog.h b/src/propertiesdialog.h
index ac830bf..2368646 100644
--- a/src/propertiesdialog.h
+++ b/src/propertiesdialog.h
@@ -66,15 +66,6 @@ class PropertiesDialog : public QDialog, Ui::PropertiesDialog
 
         bool eventFilter(QObject *object, QEvent *event) override;
 
-    public slots:
-        void changeFontButton_clicked();
-        void chooseBackgroundImageButton_clicked();
-        void bookmarksPathEdited();
-        void bookmarksButton_clicked();
-        void saveSizeOnExitStateChanged(int state);
-        void getCurrentSizeClicked();
-        void onRejected();
-
     signals:
         void propertiesChanged();
 
@@ -90,6 +81,14 @@ class PropertiesDialog : public QDialog, Ui::PropertiesDialog
         void apply();
         void accept() override;
 
+        void changeFontButton_clicked();
+        void chooseBackgroundImageButton_clicked();
+        void bookmarksPathEdited();
+        void bookmarksButton_clicked();
+        void saveSizeOnExitStateChanged(int state);
+        void getCurrentSizeClicked();
+        void onRejected();
+
     protected:
         void setupShortcuts();
         void saveShortcuts();
diff --git a/src/tab-switcher.h b/src/tab-switcher.h
index 10fd25b..d7162b6 100644
--- a/src/tab-switcher.h
+++ b/src/tab-switcher.h
@@ -10,6 +10,7 @@ class TabWidget;
 class AppModel : public QAbstractListModel
 {
     Q_OBJECT
+
 public:
     AppModel(QObject* parent, TabWidget* tabs);
 
@@ -40,9 +41,6 @@ public:
 signals:
     void activateTab(int index) const;
 
-public slots:
-    void timer();
-
 protected:
     void keyReleaseEvent(QKeyEvent *event) override;
     void closeEvent(QCloseEvent *) override;
@@ -50,6 +48,9 @@ protected:
 private:
     void showSwitcher();
 
+private slots:
+    void timer();
+
 private:
     QTimer *m_timer;
     TabWidget* m_tabs;
diff --git a/src/termwidget.h b/src/termwidget.h
index 1f12075..3300bf3 100644
--- a/src/termwidget.h
+++ b/src/termwidget.h
@@ -50,6 +50,8 @@ class TermWidgetImpl : public QTermWidget
         void zoomOut();
         void zoomReset();
         void customContextMenuCall(const QPoint & pos);
+
+   private slots:
         void activateUrl(const QUrl& url, bool fromContextMenu);
         void bell();
 
@@ -85,9 +87,6 @@ class TermWidget : public QWidget, public DBusAddressable
 
         bool eventFilter(QObject * obj, QEvent * evt) override;
 
-    public slots:
-        void onTitleChanged();
-
     signals:
         void finished();
         void renameSession();
@@ -108,6 +107,7 @@ class TermWidget : public QWidget, public DBusAddressable
     private slots:
         void term_termGetFocus();
         void term_termLostFocus();
+        void onTitleChanged();
 };
 
 #endif
diff --git a/src/termwidgetholder.h b/src/termwidgetholder.h
index 3db6f7a..7e97bcc 100644
--- a/src/termwidgetholder.h
+++ b/src/termwidgetholder.h
@@ -78,8 +78,6 @@ class TermWidgetHolder : public QWidget
         void directionalNavigation(NavigationDirection dir);
         void clearActiveTerminal();
         void onTermTitleChanged(QString title, QString icon) const;
-        void setCurrentTerminal(TermWidget* term);
-        void handle_finished();
 
     signals:
         void finished();
@@ -95,6 +93,10 @@ class TermWidgetHolder : public QWidget
 
         void split(TermWidget * term, Qt::Orientation orientation);
         TermWidget * newTerm(TerminalConfig &cfg);
+
+    private slots:
+        void setCurrentTerminal(TermWidget* term);
+        void handle_finished();
 };
 
 #endif
-- 
2.49.0

