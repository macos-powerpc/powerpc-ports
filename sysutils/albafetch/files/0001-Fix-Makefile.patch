--- Makefile	2025-10-02 06:44:21.000000000 +0800
+++ Makefile	2025-10-03 17:15:53.000000000 +0800
@@ -1,7 +1,6 @@
 .PHONY: all compile clean run debug deb install uninstall
 
-OS := $(shell uname -o 2> /dev/null)
-KERNEL := $(shell uname -s 2> /dev/null)
+OS := $(shell uname -s 2> /dev/null)
 
 INSTALLPATH := /usr/local/bin
 CONFIGPATH := /etc/xdg
@@ -14,11 +13,11 @@
 	CONFIGPATH := $(PREFIX)/etc/xdg
 endif
 
-ifeq ($(KERNEL),Darwin)
+ifeq ($(OS),Darwin)
 	INSTALLFLAGS := -m755
 	CONFIGFLAGS := -m644
-	INSTALLPATH := /usr/local/bin
-	CONFIGPATH := ~/.config/
+	INSTALLPATH := @DESTDIR@/bin
+	CONFIGPATH := @DESTDIR@/etc
 endif
 
 all: compile
