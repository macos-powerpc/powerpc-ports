--- src/osx/btop_collect.cpp.orig
+++ src/osx/btop_collect.cpp
@@ -18,6 +18,20 @@ tab-size = 4
 #include <Availability.h>
 #include <CoreFoundation/CoreFoundation.h>
 #include <IOKit/IOKitLib.h>
+
+// macOS 10.6 SDK compatibility: kIOMainPortDefault added in macOS 12.0
+// Earlier SDKs use kIOMasterPortDefault
+#if __MAC_OS_X_VERSION_MIN_REQUIRED < 120000
+#ifndef kIOMainPortDefault
+#define kIOMainPortDefault kIOMasterPortDefault
+#endif
+#endif
+
+// GPU_SUPPORT requires Apple Silicon IOReport APIs (macOS 11+)
+// Disable for older macOS versions (Intel/PPC don't have Apple Silicon GPUs)
+#if defined(GPU_SUPPORT) && __MAC_OS_X_VERSION_MIN_REQUIRED < 110000
+#undef GPU_SUPPORT
+#endif
 #include <arpa/inet.h>
 #include <libproc.h>
 #include <mach/mach.h>
@@ -100,10 +114,6 @@ extern "C" {
 }
 #endif // GPU_SUPPORT

-#if __MAC_OS_X_VERSION_MIN_REQUIRED < 120000
-#define kIOMainPortDefault kIOMasterPortDefault
-#endif
-
 using std::clamp, std::string_literals::operator""s, std::cmp_equal, std::cmp_less, std::cmp_greater;
 using std::ifstream, std::numeric_limits, std::streamsize, std::round, std::max, std::min;
 namespace fs = std::filesystem;
@@ -1062,6 +1072,20 @@ namespace Mem {
 		auto &mem = current_mem;
 		static bool snapped = (getenv("BTOP_SNAPPED") != nullptr);

+// macOS 10.6/10.7 SDK may not have HOST_VM_INFO64 - use 32-bit version
+#if __MAC_OS_X_VERSION_MIN_REQUIRED < 1080
+		vm_statistics p;
+		mach_msg_type_number_t info_size = HOST_VM_INFO_COUNT;
+		if (host_statistics(mach_host_self(), HOST_VM_INFO, (host_info_t)&p, &info_size) == 0) {
+			mem.stats.at("free") = p.free_count * Shared::pageSize;
+			// external_page_count not available in 32-bit struct, use inactive as cached approximation
+			mem.stats.at("cached") = p.inactive_count * Shared::pageSize;
+			mem.stats.at("used") = (p.active_count + p.wire_count) * Shared::pageSize;
+			mem.stats.at("available") = Shared::totalMem - mem.stats.at("used");
+		}
+#else
 		vm_statistics64 p;
 		mach_msg_type_number_t info_size = HOST_VM_INFO64_COUNT;
 		if (host_statistics64(mach_host_self(), HOST_VM_INFO64, (host_info64_t)&p, &info_size) == 0) {
@@ -1070,6 +1094,7 @@ namespace Mem {
 			mem.stats.at("used") = (p.active_count + p.wire_count) * Shared::pageSize;
 			mem.stats.at("available") = Shared::totalMem - mem.stats.at("used");
 		}
+#endif

 		int mib[2] = {CTL_VM, VM_SWAPUSAGE};
