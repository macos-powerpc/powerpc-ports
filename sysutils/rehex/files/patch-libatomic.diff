--- Makefile
+++ Makefile	2026-01-13 15:42:34.000000000 +0800
@@ -133,7 +133,7 @@
 	LDLIBS += -liconv
 endif
 ifeq ($(uname_S),Darwin)
-	LDLIBS += -liconv
+	LDLIBS += -liconv -latomic
 endif
 
 LDLIBS := -lunistring $(WX_LIBS) $(GTK_LIBS) $(BOTAN_LIBS) $(CAPSTONE_LIBS) $(JANSSON_LIBS) $(LUA_LIBS) $(LDLIBS)
