--- src/monitor.h.orig	2026-06-09 21:15:57.000000000 +0800
+++ src/monitor.h	2026-06-09 21:16:16.000000000 +0800
@@ -4,7 +4,7 @@
 
 #ifdef __linux
 #include <pty.h>
-#elif defined(__OpenBSD__) || defined(__NetBSD__)
+#elif defined(__OpenBSD__) || defined(__NetBSD__) || defined(__APPLE__)
 #include <termios.h>
 #include <util.h>
 #elif defined(__FreeBSD__) || defined(__DragonFly__)
@@ -12,7 +12,7 @@
 #include <termios.h>
 #endif
 
-#include <poll.h>
+#include <sys/select.h>
 
 #include "util.h"
 
@@ -22,17 +22,16 @@
 
 #define MONITOR_WRITE_WOULD_BLOCK (-1)
 
-#define CHILD_FD_IDX 0
-#define EXTRA_FD_IDX 1
-
 typedef struct
 {
-    int           child_fd, parent_fd, extra_fd;
-    struct pollfd pollfds[2];
-    bool          read_info_up_to_date;
-    pid_t         child_pid;
-    bool          child_is_dead;
-    char          input_buffer[MONITOR_INPUT_BUFFER_SZ];
+    int  child_fd, parent_fd;
+    int  extra_fd;        /* -1 = not set */
+    bool child_fd_ready;
+    bool extra_fd_ready;
+    bool read_info_up_to_date;
+    pid_t child_pid;
+    bool  child_is_dead;
+    char  input_buffer[MONITOR_INPUT_BUFFER_SZ];
 
     struct MonitorCallbacks
     {
@@ -74,5 +73,5 @@
  * Check read can be performed on the 'extra' fd */
 static bool Monitor_are_window_system_events_pending(Monitor* self)
 {
-    return self->pollfds[EXTRA_FD_IDX].revents & POLLIN;
+    return self->extra_fd_ready;
 }

--- src/monitor.c.orig	2026-06-06 00:15:05.000000000 +0800
+++ src/monitor.c	2026-06-09 21:16:46.000000000 +0800
@@ -65,7 +65,7 @@
         instances             = Vector_new_MonitorInfo();
     }
     Monitor self       = { 0 };
-    self.extra_fd      = 0;
+    self.extra_fd      = -1;   /* -1 means "not set" */
     self.child_is_dead = true;
 
     return self;
@@ -123,17 +123,40 @@
 
 bool Monitor_wait(Monitor* self, int timeout)
 {
-    memset(self->pollfds, 0, sizeof(self->pollfds));
-    self->pollfds[CHILD_FD_IDX].fd     = self->child_fd;
-    self->pollfds[CHILD_FD_IDX].events = POLLIN;
-    self->pollfds[EXTRA_FD_IDX].fd     = self->extra_fd;
-    self->pollfds[EXTRA_FD_IDX].events = POLLIN;
+    fd_set  readfds;
+    FD_ZERO(&readfds);
+    FD_SET(self->child_fd, &readfds);
+    int nfds = self->child_fd + 1;
+
+    if (self->extra_fd >= 0) {
+        FD_SET(self->extra_fd, &readfds);
+        if (self->extra_fd >= nfds) {
+            nfds = self->extra_fd + 1;
+        }
+    }
+
+    /* poll timeout is ms; select uses struct timeval (NULL = block forever) */
+    struct timeval  tv;
+    struct timeval* tvp;
+    if (timeout < 0) {
+        tvp = NULL;
+    } else {
+        tv.tv_sec  = timeout / 1000;
+        tv.tv_usec = (timeout % 1000) * 1000L;
+        tvp        = &tv;
+    }
 
     errno = 0;
-    if (poll(self->pollfds, 2, timeout) < 0) {
+    int ret = select(nfds, &readfds, NULL, NULL, tvp);
+    if (ret < 0) {
         if (errno != EINTR && errno != EAGAIN) {
-            ERR("poll failed %s", strerror(errno));
+            ERR("select failed %s", strerror(errno));
         }
+        self->child_fd_ready = false;
+        self->extra_fd_ready = false;
+    } else {
+        self->child_fd_ready = FD_ISSET(self->child_fd, &readfds);
+        self->extra_fd_ready = (self->extra_fd >= 0) && FD_ISSET(self->extra_fd, &readfds);
     }
 
     self->read_info_up_to_date = true;
@@ -147,18 +170,24 @@
     }
 
     if (!self->read_info_up_to_date) {
-        memset(self->pollfds, 0, sizeof(self->pollfds[0]));
-        self->pollfds[CHILD_FD_IDX].fd     = self->child_fd;
-        self->pollfds[CHILD_FD_IDX].events = POLLIN;
-        if (poll(self->pollfds, 1, 0) < 0) {
+        fd_set         readfds;
+        struct timeval tv = { .tv_sec = 0, .tv_usec = 0 };
+        FD_ZERO(&readfds);
+        FD_SET(self->child_fd, &readfds);
+
+        int ret = select(self->child_fd + 1, &readfds, NULL, NULL, &tv);
+        if (ret < 0) {
             if (errno != EINTR && errno != EAGAIN) {
-                ERR("poll failed %s\n", strerror(errno));
+                ERR("select failed %s\n", strerror(errno));
             }
+            self->child_fd_ready = false;
+        } else {
+            self->child_fd_ready = FD_ISSET(self->child_fd, &readfds);
         }
         self->read_info_up_to_date = true;
     }
 
-    if (self->pollfds[CHILD_FD_IDX].revents & POLLIN) {
+    if (self->child_fd_ready) {
         size_t  to_read = unlikely(settings.debug_vt) ? 1 : sizeof(self->input_buffer);
         ssize_t rd      = read(self->child_fd, self->input_buffer, to_read);
         if (rd < 0) {
