# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0
PortGroup           legacysupport 1.1
PortGroup           makefile 1.0

# strnlen, _clock_gettime
legacysupport.newest_darwin_requires_legacy 15

github.setup        91861 wayst 51a1c97b04125f58bedc54df6dbd98a8da0939f8
version             20260606
revision            0
categories          sysutils x11
license             MIT
maintainers         {@barracuda156 macos-powerpc.org:barracuda} openmaintainer
description         Simple terminal emulator
long_description    {*}${description}
checksums           rmd160  3a09c90d648744f9ca2adbeabba738a4b56636ea \
                    sha256  fd45aa805a0037d23b6331ee816979f5588cebe6cb0d96a36a2d4f4f80d61d85 \
                    size    624358
github.tarball_from archive

installs_libs       no

legacysupport.redirect_bins ${name}

patchfiles          patch-Makefile.diff

# src/colors.h:18:10: fatal error: threads.h: No such file or directory
patchfiles-append   patch-colors.h.diff

# Replace poll with select:
patchfiles-append   patch-monitor.c.diff

# https://github.com/91861/wayst/issues/127
patchfiles-append   patch-uchar.diff

# https://github.com/91861/wayst/issues/128
patchfiles-append   patch-vt.h.diff

post-patch {
    # Remove invalid flag:
    reinplace "s|-mtune=generic||g" ${worksrcpath}/Makefile
}

depends_build-append \
                    path:bin/pkg-config:pkgconfig

depends_lib-append  path:lib/pkgconfig/fontconfig.pc:fontconfig \
                    path:lib/pkgconfig/freetype2.pc:freetype \
                    port:libutf8proc \
                    port:mesa \
                    port:xorg-libX11 \
                    port:xorg-libXrandr \
                    port:xrender

if {${os.platform} eq "darwin" && ${os.major} < 16} {
    build.cmd-prepend \
                    LDLIBS=-lMacportsLegacySupport
}

build.args-append   window_protocol=x11

compiler.c_standard 2018
