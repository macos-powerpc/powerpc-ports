diff --git a/addons/kate/search/SearchDiskFiles.cpp b/addons/kate/search/SearchDiskFiles.cpp
index fc9f593e21..693cc4280c 100644
--- a/addons/kate/search/SearchDiskFiles.cpp
+++ b/addons/kate/search/SearchDiskFiles.cpp
@@ -22,10 +22,13 @@
 #include "SearchDiskFiles.moc"
 #include <kdebug.h>
 
+#include "kateregexp.h"
+
 #include <QDir>
 #include <QTextStream>
 
 SearchDiskFiles::SearchDiskFiles(QObject *parent) : QThread(parent)
+,m_regExp(0)
 ,m_cancelSearch(true)
 ,m_matchCount(0)
 {}
@@ -34,10 +37,11 @@ SearchDiskFiles::~SearchDiskFiles()
 {
     m_cancelSearch = true;
     wait();
+    delete m_regExp;
 }
 
 void SearchDiskFiles::startSearch(const QStringList &files,
-                               const QRegExp &regexp)
+                               const KateRegExp &regexp)
 {
     if (files.size() == 0) {
         emit searchDone();
@@ -45,7 +49,8 @@ void SearchDiskFiles::startSearch(const QStringList &files,
     }
     m_cancelSearch = false;
     m_files = files;
-    m_regExp = regexp;
+    delete m_regExp;
+    m_regExp = new KateRegExp(regexp);
     m_matchCount = 0;
     m_statusTime.restart();
     start();
@@ -63,7 +68,7 @@ void SearchDiskFiles::run()
             emit searching(fileName);
         }
 
-        if (m_regExp.pattern().contains("\\n")) {
+        if (m_regExp->isMultiLine()) {
             searchMultiLineRegExp(fileName);
         }
         else {
@@ -98,13 +103,13 @@ void SearchDiskFiles::searchSingleLineRegExp(const QString &fileName)
     int column;
     while (!(line=stream.readLine()).isNull()) {
         if (m_cancelSearch) break;
-        column = m_regExp.indexIn(line);
+        column = m_regExp->indexIn(line, 0, line.length());
         while (column != -1) {
-            if (m_regExp.cap().isEmpty()) break;
+            if (m_regExp->cap().isEmpty()) break;
             // limit line length
             if (line.length() > 512) line = line.left(512);
-            emit matchFound(fileName, fileName, i, column, line, m_regExp.matchedLength());
-            column = m_regExp.indexIn(line, column + m_regExp.cap().size());
+            emit matchFound(fileName, fileName, i, column, line, m_regExp->matchedLength());
+            column = m_regExp->indexIn(line, column + m_regExp->cap().size(), line.length());
             m_matchCount++;
             // NOTE: This sleep is here so that the main thread will get a chance to
             // handle any stop button clicks if there are a lot of matches
@@ -121,7 +126,6 @@ void SearchDiskFiles::searchMultiLineRegExp(const QString &fileName)
     int line = 0;
     static QString fullDoc;
     static QVector<int> lineStart;
-    QRegExp tmpRegExp = m_regExp;
 
     if (!file.open(QFile::ReadOnly)) {
         return;
@@ -138,17 +142,16 @@ void SearchDiskFiles::searchMultiLineRegExp(const QString &fileName)
             lineStart << i+1;
         }
     }
-    if (tmpRegExp.pattern().endsWith("$")) {
-        fullDoc += '\n';
-        QString newPatern = tmpRegExp.pattern();
-        newPatern.replace("$", "(?=\\n)");
-        tmpRegExp.setPattern(newPatern);
-    }
+    // no "$" -> "(?=\\n)" rewriting needed: repairPattern() already put the
+    // regex in KatePcre::MultiLine mode when it still spans lines, and in
+    // that mode "$" matches at the end of the subject and right before every
+    // '\n' - i.e. at the end of the last line whether or not the file ends
+    // with a trailing newline.
 
-    column = tmpRegExp.indexIn(fullDoc, column);
+    column = m_regExp->indexIn(fullDoc, column, fullDoc.length());
     while (column != -1) {
         if (m_cancelSearch) break;
-        if (tmpRegExp.cap().isEmpty()) break;
+        if (m_regExp->cap().isEmpty()) break;
         // search for the line number of the match
         int i;
         line = -1;
@@ -164,9 +167,9 @@ void SearchDiskFiles::searchMultiLineRegExp(const QString &fileName)
         emit matchFound(fileName,fileName,
                         line,
                         (column - lineStart[line]),
-                        fullDoc.mid(lineStart[line], column - lineStart[line])+tmpRegExp.cap(),
-                        tmpRegExp.matchedLength());
-        column = tmpRegExp.indexIn(fullDoc, column + tmpRegExp.matchedLength());
+                        fullDoc.mid(lineStart[line], column - lineStart[line])+m_regExp->cap(),
+                        m_regExp->matchedLength());
+        column = m_regExp->indexIn(fullDoc, column + m_regExp->matchedLength(), fullDoc.length());
         m_matchCount++;
         // NOTE: This sleep is here so that the main thread will get a chance to
         // handle any stop button clicks if there are a lot of matches
diff --git a/addons/kate/search/SearchDiskFiles.h b/addons/kate/search/SearchDiskFiles.h
index de46f7432c..669dfb66b9 100644
--- a/addons/kate/search/SearchDiskFiles.h
+++ b/addons/kate/search/SearchDiskFiles.h
@@ -22,13 +22,14 @@
 #define SearchDiskFiles_h
 
 #include <QThread>
-#include <QRegExp>
 #include <QFileInfo>
 #include <QVector>
 #include <QMutex>
 #include <QStringList>
 #include <QTime>
 
+class KateRegExp;
+
 class SearchDiskFiles: public QThread
 {
     Q_OBJECT
@@ -38,7 +39,7 @@ public:
     ~SearchDiskFiles();
 
     void startSearch(const QStringList &iles,
-                     const QRegExp &regexp);
+                     const KateRegExp &regexp);
     void run();
 
     bool searching();
@@ -57,7 +58,10 @@ Q_SIGNALS:
     void searching(const QString &file);
 
 private:
-    QRegExp          m_regExp;
+    // owned copy for this search run, so the worker thread never touches the
+    // GUI thread's KateRegExp (see kateregexp.h: copies have independent
+    // match state, sharing only the read-only compiled pattern)
+    KateRegExp      *m_regExp;
     QStringList      m_files;
     bool             m_cancelSearch;
     int              m_matchCount;
diff --git a/addons/kate/search/plugin_search.cpp b/addons/kate/search/plugin_search.cpp
index c8a621a736..879ef6d8e2 100644
--- a/addons/kate/search/plugin_search.cpp
+++ b/addons/kate/search/plugin_search.cpp
@@ -45,6 +45,12 @@
 #include <klineedit.h>
 #include <kcolorscheme.h>
 
+#include "kateregexp.h"
+#include "katepcre.h"
+
+#include <kconfiggroup.h>
+
+#include <QDir>
 #include <QKeyEvent>
 #include <QClipboard>
 #include <QMenu>
@@ -52,6 +58,13 @@
 #include <QTextDocument>
 #include <QScrollBar>
 
+// uic only sees "ui_search.h" / "ui_results.h" through plugin_search.h (it
+// scans .cpp files, not headers they include) - repeat the includes here so
+// tooling that looks at this translation unit's own text finds them too;
+// harmless, both are include-guarded.
+#include "ui_search.h"
+#include "ui_results.h"
+
 static QAction *menuEntry(QMenu *menu,
                           const QString &before, const QString &after, const QString &desc,
                           QString menuBefore = QString(), QString menuAfter = QString());
@@ -98,13 +111,33 @@ private:
     }
 };
 
-Results::Results(QWidget *parent): QWidget(parent), matches(0)
+Results::Results(QWidget *parent): QWidget(parent), matches(0), regExp(0), caseSensitive(false), useRegExp(false)
 {
     setupUi(this);
 
     tree->setItemDelegate(new SPHtmlDelegate(tree));
 }
 
+Results::~Results()
+{
+    delete regExp;
+}
+
+// Builds the KateRegExp a search run uses: for a plain-text (non-regex)
+// search, escape() turns the typed text into a pattern that matches it
+// literally (KatePcre has no separate "fixed string" mode, unlike QRegExp).
+// repairPattern() is called exactly once here, on the regex every worker
+// (SearchOpenFiles, SearchDiskFiles) then receives its own copy of - see
+// kateregexpsearch.cpp for the same repairPattern()-then-branch-on-multiline
+// sequence this mirrors.
+static KateRegExp *buildSearchRegExp(const QString &pattern, bool useRegExp, Qt::CaseSensitivity cs)
+{
+    const QString actualPattern = useRegExp ? pattern : KatePcre::escape(pattern);
+    KateRegExp *regExp = new KateRegExp(actualPattern, cs);
+    bool stillMultiLine;
+    regExp->repairPattern(stillMultiLine);
+    return regExp;
+}
 
 K_PLUGIN_FACTORY(KatePluginSearchFactory, registerPlugin<KatePluginSearch>();)
 K_EXPORT_PLUGIN(KatePluginSearchFactory(KAboutData("katesearch","katesearch",ki18n("Search & Replace"), "0.1", ki18n("Search & replace in files"))))
@@ -585,13 +618,13 @@ void KatePluginSearchView::folderFileListChanged()
     // earliest after first event loop.
     // The DiskFile might finish immediately
     if (openList.size() > 0) {
-        m_searchOpenFiles.startSearch(openList, m_curResults->regExp);
+        m_searchOpenFiles.startSearch(openList, *m_curResults->regExp);
     }
     else {
         m_searchOpenFilesDone = true;
     }
 
-    m_searchDiskFiles.startSearch(fileList, m_curResults->regExp);
+    m_searchDiskFiles.startSearch(fileList, *m_curResults->regExp);
 }
 
 
@@ -707,17 +740,14 @@ void KatePluginSearchView::addMatchMark(KTextEditor::Document* doc, int line, in
 
     KTextEditor::Range range(line, column, endLine, endColumn);
 
-    if (m_curResults && !replace) {
-        // special handling for "(?=\\n)" in multi-line search
-        QRegExp tmpReg = m_curResults->regExp;
-        if (m_curResults->regExp.pattern().endsWith("(?=\\n)")) {
-            QString newPatern = tmpReg.pattern();
-            newPatern.replace("(?=\\n)", "$");
-            tmpReg.setPattern(newPatern);
-        }
-
-        if (tmpReg.indexIn(doc->text(range)) != 0) {
-            kDebug() << doc->text(range) << "Does not match" << m_curResults->regExp.pattern();
+    if (m_curResults && !replace && m_curResults->regExp) {
+        // KatePcre's MultiLine option (set by repairPattern() for a pattern
+        // that still spans lines) makes "^"/"$" match at line boundaries
+        // directly, so unlike the old QRegExp-based code there is no
+        // "(?=\\n)" rewrite to special-case here.
+        const QString matchText = doc->text(range);
+        if (m_curResults->regExp->indexIn(matchText, 0, matchText.length()) != 0) {
+            kDebug() << matchText << "Does not match" << m_curResults->regExp->pattern();
             return;
         }
     }
@@ -865,10 +895,12 @@ void KatePluginSearchView::startSearch()
     m_ui.replaceCombo->setDisabled(true);
 
 
-    QRegExp reg(m_ui.searchCombo->currentText(),
-                m_ui.matchCase->isChecked() ? Qt::CaseSensitive : Qt::CaseInsensitive,
-                m_ui.useRegExp->isChecked() ? QRegExp::RegExp : QRegExp::FixedString);
-    m_curResults->regExp = reg;
+    delete m_curResults->regExp;
+    m_curResults->regExp = buildSearchRegExp(m_ui.searchCombo->currentText(),
+                                             m_ui.useRegExp->isChecked(),
+                                             m_ui.matchCase->isChecked() ? Qt::CaseSensitive : Qt::CaseInsensitive);
+    m_curResults->caseSensitive = m_ui.matchCase->isChecked();
+    m_curResults->useRegExp = m_ui.useRegExp->isChecked();
 
     clearMarks();
     m_curResults->tree->clear();
@@ -886,7 +918,7 @@ void KatePluginSearchView::startSearch()
         m_resultBaseDir.clear();
         const QList<KTextEditor::Document*> & documents = m_kateApp->documentManager()->documents();
         addHeaderItem();
-        m_searchOpenFiles.startSearch(documents, reg);
+        m_searchOpenFiles.startSearch(documents, *m_curResults->regExp);
     }
     else if (m_ui.searchPlaceCombo->currentIndex() == 1) {
         m_resultBaseDir = m_ui.folderRequester->text();
@@ -929,12 +961,12 @@ void KatePluginSearchView::startSearch()
         // earliest after first event loop.
         // The DiskFile might finish immediately
         if (openList.size() > 0) {
-            m_searchOpenFiles.startSearch(openList, m_curResults->regExp);
+            m_searchOpenFiles.startSearch(openList, *m_curResults->regExp);
         }
         else {
             m_searchOpenFilesDone = true;
         }
-        m_searchDiskFiles.startSearch(files, reg);
+        m_searchDiskFiles.startSearch(files, *m_curResults->regExp);
     }
 }
 
@@ -961,11 +993,12 @@ void KatePluginSearchView::startSearchWhileTyping()
     m_ui.replaceButton->setDisabled(true);
     m_ui.nextButton->setDisabled(true);
 
-    QRegExp reg(m_ui.searchCombo->currentText(),
-                m_ui.matchCase->isChecked() ? Qt::CaseSensitive : Qt::CaseInsensitive,
-                m_ui.useRegExp->isChecked() ? QRegExp::RegExp : QRegExp::FixedString);
-
-    m_curResults->regExp = reg;
+    delete m_curResults->regExp;
+    m_curResults->regExp = buildSearchRegExp(m_ui.searchCombo->currentText(),
+                                             m_ui.useRegExp->isChecked(),
+                                             m_ui.matchCase->isChecked() ? Qt::CaseSensitive : Qt::CaseInsensitive);
+    m_curResults->caseSensitive = m_ui.matchCase->isChecked();
+    m_curResults->useRegExp = m_ui.useRegExp->isChecked();
 
     clearMarks();
     m_curResults->tree->clear();
@@ -982,7 +1015,7 @@ void KatePluginSearchView::startSearchWhileTyping()
     item->setFlags(item->flags() | Qt::ItemIsTristate);
 
     if (m_ui.searchCombo->currentText().length() >= 2) {
-        m_searchOpenFiles.searchOpenFile(doc, reg, 0);
+        m_searchOpenFiles.searchOpenFile(doc, *m_curResults->regExp, 0);
     }
     searchWhileTypingDone();
 }
@@ -1188,8 +1221,9 @@ void KatePluginSearchView::replaceSingleMatch()
         return;
     }
 
-    if (!res->regExp.exactMatch(doc->text(m_matchRanges[i]->toRange()))) {
-        kDebug() << doc->text(m_matchRanges[i]->toRange()) << "Does not match" << res->regExp.pattern();
+    const QString matchedText = doc->text(m_matchRanges[i]->toRange());
+    if (!res->regExp || !res->regExp->exactMatch(matchedText)) {
+        kDebug() << matchedText << "Does not match" << (res->regExp ? res->regExp->pattern() : QString());
         goToNextMatch();
         return;
     }
@@ -1202,8 +1236,8 @@ void KatePluginSearchView::replaceSingleMatch()
 
     QString replaceText = m_ui.replaceCombo->currentText();
     replaceText.replace("\\\\", "¤Search&Replace¤");
-    for (int j=1; j<=res->regExp.captureCount(); j++) {
-        replaceText.replace(QString("\\%1").arg(j), res->regExp.cap(j));
+    for (int j=1; j<=res->regExp->numCaptures(); j++) {
+        replaceText.replace(QString("\\%1").arg(j), res->regExp->cap(j));
     }
     replaceText.replace("\\n", "\n");
     replaceText.replace("¤Search&Replace¤", "\\\\");
@@ -1254,8 +1288,11 @@ void KatePluginSearchView::replaceChecked()
 
     m_curResults->replace = m_ui.replaceCombo->currentText();
 
+    if (!m_curResults->regExp) {
+        return;
+    }
     m_replacer.replaceChecked(m_curResults->tree,
-                              m_curResults->regExp,
+                              *m_curResults->regExp,
                               m_curResults->replace);
 }
 
@@ -1568,8 +1605,8 @@ void KatePluginSearchView::resultTabChanged(int index)
     m_ui.matchCase->blockSignals(true);
     m_ui.useRegExp->blockSignals(true);
     m_ui.searchCombo->lineEdit()->setText(m_ui.resultTabWidget->tabText(index));
-    m_ui.matchCase->setChecked(res->regExp.caseSensitivity() == Qt::CaseSensitive);
-    m_ui.useRegExp->setChecked(res->regExp.patternSyntax() != QRegExp::FixedString);
+    m_ui.matchCase->setChecked(res->caseSensitive);
+    m_ui.useRegExp->setChecked(res->useRegExp);
     m_ui.searchCombo->blockSignals(false);
     m_ui.matchCase->blockSignals(false);
     m_ui.useRegExp->blockSignals(false);
diff --git a/addons/kate/search/plugin_search.h b/addons/kate/search/plugin_search.h
index 9821cce998..4c8f0e7388 100644
--- a/addons/kate/search/plugin_search.h
+++ b/addons/kate/search/plugin_search.h
@@ -38,6 +38,7 @@
 #include "replace_matches.h"
 
 class KateSearchCommand;
+class KateRegExp;
 namespace KTextEditor{
     class MovingRange;
 }
@@ -47,9 +48,19 @@ class Results: public QWidget, public Ui::Results
     Q_OBJECT
 public:
     Results(QWidget *parent = 0);
-    int     matches;
-    QRegExp regExp;
-    QString replace;
+    ~Results();
+    int         matches;
+    // owned; rebuilt (delete + new) for every search run against this tab.
+    // Not default-constructible (KateRegExp always needs a pattern), so it
+    // is a pointer rather than a value member; 0 until the first search.
+    KateRegExp *regExp;
+    // caseSensitive/useRegExp mirror the matchCase/useRegExp checkboxes that
+    // produced regExp: KateRegExp (unlike QRegExp) does not expose its case
+    // sensitivity or whether it is a literal/regex pattern, so resultTabChanged()
+    // needs them kept alongside it to restore the checkboxes for this tab.
+    bool        caseSensitive;
+    bool        useRegExp;
+    QString     replace;
 };
 
 // This class keeps the focus inside the S&R plugin when pressing tab/shift+tab by overriding focusNextPrevChild()
diff --git a/addons/kate/search/replace_matches.cpp b/addons/kate/search/replace_matches.cpp
index 67902e1e08..8ce57bbc72 100644
--- a/addons/kate/search/replace_matches.cpp
+++ b/addons/kate/search/replace_matches.cpp
@@ -25,22 +25,31 @@
 #include <ktexteditor/movingrange.h>
 #include <klocale.h>
 
+#include "kateregexp.h"
+
 ReplaceMatches::ReplaceMatches(QObject *parent) : QObject(parent),
 m_manager(0),
 m_tree(0),
-m_rootIndex(-1)
+m_rootIndex(-1),
+m_regExp(0)
 {
     connect(this, SIGNAL(replaceNextMatch()), this, SLOT(doReplaceNextMatch()), Qt::QueuedConnection);
 }
 
-void ReplaceMatches::replaceChecked(QTreeWidget *tree, const QRegExp &regexp, const QString &replace)
+ReplaceMatches::~ReplaceMatches()
+{
+    delete m_regExp;
+}
+
+void ReplaceMatches::replaceChecked(QTreeWidget *tree, const KateRegExp &regexp, const QString &replace)
 {
     if (m_manager == 0) return;
     if (m_rootIndex != -1) return;
-    
+
     m_tree = tree;
     m_rootIndex = 0;
-    m_regExp = regexp;
+    delete m_regExp;
+    m_regExp = new KateRegExp(regexp);
     m_replaceText = replace;
     m_cancelReplace = false;
     emit replaceNextMatch();
@@ -144,15 +153,15 @@ void ReplaceMatches::doReplaceNextMatch()
             matchLines+= '\n' + doc->line(endLine);
         }
 
-        if (m_regExp.indexIn(matchLines) != 0) {
+        if (m_regExp->indexIn(matchLines, 0, matchLines.length()) != 0) {
             kDebug() << "expression does not match";
             continue;
         }
 
         QString replaceText = m_replaceText;
         replaceText.replace("\\\\", "¤Search&Replace¤");
-        for (int j=1; j<=m_regExp.captureCount(); j++) {
-            replaceText.replace(QString("\\%1").arg(j), m_regExp.cap(j));
+        for (int j=1; j<=m_regExp->numCaptures(); j++) {
+            replaceText.replace(QString("\\%1").arg(j), m_regExp->cap(j));
         }
         replaceText.replace("\\n", "\n");
         replaceText.replace("¤Search&Replace¤", "\\\\");
diff --git a/addons/kate/search/replace_matches.h b/addons/kate/search/replace_matches.h
index c015e53894..e805c6998d 100644
--- a/addons/kate/search/replace_matches.h
+++ b/addons/kate/search/replace_matches.h
@@ -22,11 +22,12 @@
 #define _REPLACE_MATCHES_H_
 
 #include <QObject>
-#include <QRegExp>
 #include <QTreeWidget>
 #include <ktexteditor/document.h>
 #include <kate/documentmanager.h>
 
+class KateRegExp;
+
 class ReplaceMatches: public QObject
 {
     Q_OBJECT
@@ -44,9 +45,10 @@ public:
     };
 
     ReplaceMatches(QObject *parent = 0);
+    ~ReplaceMatches();
     void setDocumentManager(Kate::DocumentManager *manager);
 
-    void replaceChecked(QTreeWidget *tree, const QRegExp &regexp, const QString &replace);
+    void replaceChecked(QTreeWidget *tree, const KateRegExp &regexp, const QString &replace);
 
     KTextEditor::Document *findNamed(const QString &name);
 
@@ -65,7 +67,9 @@ private:
     Kate::DocumentManager        *m_manager;
     QTreeWidget                  *m_tree;
     int                           m_rootIndex;
-    QRegExp                       m_regExp;
+    // owned copy; see kateregexp.h and SearchDiskFiles for why m_regExp is
+    // a pointer (no default constructor) rather than a value member
+    KateRegExp                   *m_regExp;
     QString                       m_replaceText;
     bool                          m_cancelReplace;
 };
diff --git a/addons/kate/search/search_open_files.cpp b/addons/kate/search/search_open_files.cpp
index 00af3a49fc..bfcb991633 100644
--- a/addons/kate/search/search_open_files.cpp
+++ b/addons/kate/search/search_open_files.cpp
@@ -21,22 +21,30 @@
 #include "search_open_files.h"
 #include "search_open_files.moc"
 
+#include "kateregexp.h"
+
 #include <QTime>
 
-SearchOpenFiles::SearchOpenFiles(QObject *parent) : QObject(parent), m_nextIndex(-1), m_cancelSearch(true)
+SearchOpenFiles::SearchOpenFiles(QObject *parent) : QObject(parent), m_nextIndex(-1), m_regExp(0), m_cancelSearch(true)
 {
     connect(this, SIGNAL(searchNextFile(int)), this, SLOT(doSearchNextFile(int)), Qt::QueuedConnection);
 }
 
+SearchOpenFiles::~SearchOpenFiles()
+{
+    delete m_regExp;
+}
+
 bool SearchOpenFiles::searching() { return !m_cancelSearch; }
 
-void SearchOpenFiles::startSearch(const QList<KTextEditor::Document*> &list, const QRegExp &regexp)
+void SearchOpenFiles::startSearch(const QList<KTextEditor::Document*> &list, const KateRegExp &regexp)
 {
     if (m_nextIndex != -1) return;
 
     m_docList = list;
     m_nextIndex = 0;
-    m_regExp = regexp;
+    delete m_regExp;
+    m_regExp = new KateRegExp(regexp);
     m_cancelSearch = false;
     m_statusTime.restart();
     emit searchNextFile(0);
@@ -58,7 +66,7 @@ void SearchOpenFiles::doSearchNextFile(int startLine)
 
     // NOTE The document managers signal documentWillBeDeleted() must be connected to
     // cancelSearch(). A closed file could lead to a crash if it is not handled.
-    int line = searchOpenFile(m_docList[m_nextIndex], m_regExp, startLine);
+    int line = searchOpenFile(m_docList[m_nextIndex], *m_regExp, startLine);
     if (line == 0) {
         // file searched go to next
         m_nextIndex++;
@@ -76,21 +84,21 @@ void SearchOpenFiles::doSearchNextFile(int startLine)
     }
 }
 
-int SearchOpenFiles::searchOpenFile(KTextEditor::Document *doc, const QRegExp &regExp, int startLine)
+int SearchOpenFiles::searchOpenFile(KTextEditor::Document *doc, const KateRegExp &regExp, int startLine)
 {
     if (m_statusTime.elapsed() > 100) {
         m_statusTime.restart();
         emit searching(doc->url().pathOrUrl());
     }
 
-    if (regExp.pattern().contains("\\n")) {
+    if (regExp.isMultiLine()) {
         return searchMultiLineRegExp(doc, regExp, startLine);
     }
 
     return searchSingleLineRegExp(doc, regExp, startLine);
 }
 
-int SearchOpenFiles::searchSingleLineRegExp(KTextEditor::Document *doc, const QRegExp &regExp, int startLine)
+int SearchOpenFiles::searchSingleLineRegExp(KTextEditor::Document *doc, const KateRegExp &regExp, int startLine)
 {
     int column;
     QTime time;
@@ -101,24 +109,24 @@ int SearchOpenFiles::searchSingleLineRegExp(KTextEditor::Document *doc, const QR
             kDebug() << "Search time exceeded" << time.elapsed() << line;
             return line;
         }
-        column = regExp.indexIn(doc->line(line));
+        const QString lineText = doc->line(line);
+        column = regExp.indexIn(lineText, 0, lineText.length());
         while (column != -1) {
             if (regExp.cap().isEmpty()) break;
             emit matchFound(doc->url().pathOrUrl(), doc->documentName(), line, column,
-                            doc->line(line), regExp.matchedLength());
-            column = regExp.indexIn(doc->line(line), column + regExp.cap().size());
+                            lineText, regExp.matchedLength());
+            column = regExp.indexIn(lineText, column + regExp.cap().size(), lineText.length());
         }
     }
     return 0;
 }
 
-int SearchOpenFiles::searchMultiLineRegExp(KTextEditor::Document *doc, const QRegExp &regExp, int startLine)
+int SearchOpenFiles::searchMultiLineRegExp(KTextEditor::Document *doc, const KateRegExp &regExp, int startLine)
 {
     int column = 0;
     int line = 0;
     QTime time;
     time.start();
-    QRegExp tmpRegExp = regExp;
 
     if (startLine == 0) {
         // Copy the whole file to a temporary buffer to be able to search newlines
@@ -129,11 +137,10 @@ int SearchOpenFiles::searchMultiLineRegExp(KTextEditor::Document *doc, const QRe
             m_fullDoc += doc->line(i) + '\n';
             m_lineStart << m_fullDoc.size();
         }
-        if (!regExp.pattern().endsWith("$")) {
-            // if regExp ends with '$' leave the extra newline at the end as
-            // '$' will be replaced with (?=\\n), which needs the extra newline
-            m_fullDoc.remove(m_fullDoc.size()-1, 1);
-        }
+        // no trailing-newline juggling needed: with KatePcre::MultiLine (set
+        // automatically by repairPattern() for a still-multi-line pattern)
+        // "$" matches both at the end of the subject and right before every
+        // '\n', so it lands at the end of the last line either way.
     }
     else {
         if (startLine>0 && startLine<m_lineStart.size()) {
@@ -145,15 +152,9 @@ int SearchOpenFiles::searchMultiLineRegExp(KTextEditor::Document *doc, const QRe
         }
     }
 
-    if (regExp.pattern().endsWith("$")) {
-        QString newPatern = tmpRegExp.pattern();
-        newPatern.replace("$", "(?=\\n)");
-        tmpRegExp.setPattern(newPatern);
-    }
-
-    column = tmpRegExp.indexIn(m_fullDoc, column);
+    column = regExp.indexIn(m_fullDoc, column, m_fullDoc.length());
     while (column != -1) {
-        if (tmpRegExp.cap().isEmpty()) break;
+        if (regExp.cap().isEmpty()) break;
         // search for the line number of the match
         int i;
         line = -1;
@@ -169,9 +170,9 @@ int SearchOpenFiles::searchMultiLineRegExp(KTextEditor::Document *doc, const QRe
         emit matchFound(doc->url().pathOrUrl(), doc->documentName(),
                         line,
                         (column - m_lineStart[line]),
-                        doc->line(line).left(column - m_lineStart[line])+tmpRegExp.cap(),
-                        tmpRegExp.matchedLength());
-        column = tmpRegExp.indexIn(m_fullDoc, column + tmpRegExp.matchedLength());
+                        doc->line(line).left(column - m_lineStart[line])+regExp.cap(),
+                        regExp.matchedLength());
+        column = regExp.indexIn(m_fullDoc, column + regExp.matchedLength(), m_fullDoc.length());
 
         if (time.elapsed() > 100) {
             //kDebug() << "Search time exceeded" << time.elapsed() << line;
diff --git a/addons/kate/search/search_open_files.h b/addons/kate/search/search_open_files.h
index 547e9c5486..1fa6c3edf9 100644
--- a/addons/kate/search/search_open_files.h
+++ b/addons/kate/search/search_open_files.h
@@ -22,32 +22,34 @@
 #define _SEARCH_OPEN_FILES_H_
 
 #include <QObject>
-#include <QRegExp>
 #include <QTime>
 #include <ktexteditor/document.h>
 
+class KateRegExp;
+
 class SearchOpenFiles: public QObject
 {
     Q_OBJECT
 
 public:
     SearchOpenFiles(QObject *parent = 0);
+    ~SearchOpenFiles();
 
-    void startSearch(const QList<KTextEditor::Document*> &list,const QRegExp &regexp);
+    void startSearch(const QList<KTextEditor::Document*> &list, const KateRegExp &regexp);
     bool searching();
 
 public Q_SLOTS:
     void cancelSearch();
 
     /// return 0 on success or a line number where we stopped.
-    int searchOpenFile(KTextEditor::Document *doc, const QRegExp &regExp, int startLine);
+    int searchOpenFile(KTextEditor::Document *doc, const KateRegExp &regExp, int startLine);
 
 private Q_SLOTS:
     void doSearchNextFile(int startLine);
 
 private:
-    int searchSingleLineRegExp(KTextEditor::Document *doc, const QRegExp &regExp, int startLine);
-    int searchMultiLineRegExp(KTextEditor::Document *doc, const QRegExp &regExp, int startLine);
+    int searchSingleLineRegExp(KTextEditor::Document *doc, const KateRegExp &regExp, int startLine);
+    int searchMultiLineRegExp(KTextEditor::Document *doc, const KateRegExp &regExp, int startLine);
 
 Q_SIGNALS:
     void searchNextFile(int startLine);
@@ -58,7 +60,9 @@ Q_SIGNALS:
 private:
     QList<KTextEditor::Document*> m_docList;
     int                           m_nextIndex;
-    QRegExp                       m_regExp;
+    // owned copy for this search run; a fresh KateRegExp per startSearch()
+    // (see kateregexp.h: copies share the compiled pattern, cheap to make)
+    KateRegExp                   *m_regExp;
     bool                          m_cancelSearch;
     QString                       m_fullDoc;
     QVector<int>                  m_lineStart;
