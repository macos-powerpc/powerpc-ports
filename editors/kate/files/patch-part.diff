diff --git a/part/CMakeLists.txt b/part/CMakeLists.txt
index 18082f5d31..1622b35a8a 100644
--- a/part/CMakeLists.txt
+++ b/part/CMakeLists.txt
@@ -1,4 +1,4 @@
-# define project
+# KatePart on plain Qt 4 + qt4compat: one static library, no KParts plugin.
 project(katepart)
 
 # these subdirs have their own CMakeLists
@@ -30,7 +30,7 @@ include_directories(
   ${CMAKE_CURRENT_SOURCE_DIR}/swapfile
   ${CMAKE_CURRENT_SOURCE_DIR}/variableeditor
   ${CMAKE_CURRENT_SOURCE_DIR}/kte5
-  ${KDE4_KIO_INCLUDES}
+  ${CMAKE_CURRENT_BINARY_DIR}
 )
 
 # our sources
@@ -166,6 +166,7 @@ utils/katecmds.cpp
 utils/kateconfig.cpp
 utils/katebookmarks.cpp
 utils/kateautoindent.cpp
+utils/katepcre.cpp
 utils/katetemplatehandler.cpp
 utils/kateprinter.cpp
 utils/kateglobal.cpp
@@ -224,44 +225,21 @@ snippet/editrepository.ui
 snippet/editsnippet.ui
 )
 
-add_definitions ( -DKDE_DEFAULT_DEBUG_AREA=13000 )
+add_definitions ( -DKDE_DEFAULT_DEBUG_AREA=13000 -DKATEPART_STATIC_LIBS )
 
-kde4_add_ui_files(katepart_PART_SRCS ${katepart_PART_UI} )
+kate_add_ui_files(katepart_PART_SRCS ${katepart_PART_UI})
+kate_automoc(katepart_MOC_SRCS ${katepart_PART_SRCS})
 
-kde4_add_library (katepartinterfaces ${LIBRARY_TYPE} ${katepart_PART_SRCS} )
+add_library (katepartinterfaces STATIC ${katepart_PART_SRCS} ${katepart_MOC_SRCS})
 
-target_link_libraries (
-   katepartinterfaces
-   LINK_PUBLIC ${KDE4_KPARTS_LIBS}
-   LINK_PRIVATE ${KDE4_KDECORE_LIBS} ${KDE4_KPARTS_LIBS} ${KDE4_KCMUTILS_LIBS} ${KDE4_KTEXTEDITOR_LIBS} ${QT_QTSCRIPT_LIBRARY} ${KDE_4_4_LIBS_NEEDED} ${KDE4_KNEWSTUFF3_LIBS}
+target_link_libraries (katepartinterfaces
+  ktexteditor
+  qt4compat
+  ${QT_QTSCRIPT_LIBRARY}
+  ${QT_QTNETWORK_LIBRARY}
+  ${QT_QTXML_LIBRARY}
+  ${QT_QTGUI_LIBRARY}
+  ${QT_QTCORE_LIBRARY}
+  ${ZLIB_LIBRARIES}
+  ${PCRE2_LIBRARIES}
 )
-
-set_target_properties(
-  katepartinterfaces PROPERTIES
-  VERSION ${GENERIC_LIB_VERSION}
-  SOVERSION ${GENERIC_LIB_SOVERSION}
-)
-
-# kde 4 final support
-if(KDE4_ENABLE_FINAL)
-   macro_add_file_dependencies(${CMAKE_CURRENT_BINARY_DIR}/katepartinterfaces_final_cpp.cpp )
-else(KDE4_ENABLE_FINAL)
-   macro_add_file_dependencies(${CMAKE_CURRENT_SOURCE_DIR}/script/katescript.cpp )
-endif(KDE4_ENABLE_FINAL)
-
-# install kate part interfaces
-install (TARGETS katepartinterfaces EXPORT kdelibsLibraryTargets ${INSTALL_TARGETS_DEFAULT_ARGS})
-
-# kate part itself just is interfaces + the factory
-kde4_add_plugin (katepart utils/katefactory.cpp)
-
-# link the part, use kate part interfaces + kde stuff
-target_link_libraries (katepart
-  ${KDE4_KDECORE_LIBS} ${KDE4_KPARTS_LIBS}
-  ${KDE4_KCMUTILS_LIBS} ${KDE4_KTEXTEDITOR_LIBS} ${QT_QTSCRIPT_LIBRARY} katepartinterfaces)
-
-# install the part
-install (TARGETS katepart DESTINATION ${PLUGIN_INSTALL_DIR})
-
-# install the snippet stuff
-install( FILES snippet/ktexteditor_codesnippets_core.knsrc DESTINATION  ${CONFIG_INSTALL_DIR} )
diff --git a/part/completion/katecompletionconfig.cpp b/part/completion/katecompletionconfig.cpp
index 9d7dd207af..b875feef80 100644
--- a/part/completion/katecompletionconfig.cpp
+++ b/part/completion/katecompletionconfig.cpp
@@ -24,6 +24,8 @@
 
 #include <kicon.h>
 #include <kglobal.h>
+#include <klocale.h>
+#include <ksharedconfig.h>
 
 #include "katecompletionmodel.h"
 
diff --git a/part/completion/katekeywordcompletion.cpp b/part/completion/katekeywordcompletion.cpp
index 3a720a7a37..07debdea51 100644
--- a/part/completion/katekeywordcompletion.cpp
+++ b/part/completion/katekeywordcompletion.cpp
@@ -25,6 +25,7 @@
 #include "katetextline.h"
 
 #include <KLocalizedString>
+#include <kicon.h>
 
 KateKeywordCompletionModel::KateKeywordCompletionModel(QObject* parent)
     : CodeCompletionModel2(parent)
diff --git a/part/completion/katewordcompletion.cpp b/part/completion/katewordcompletion.cpp
index 32bf90c6a0..5c305f0788 100644
--- a/part/completion/katewordcompletion.cpp
+++ b/part/completion/katewordcompletion.cpp
@@ -34,7 +34,6 @@
 
 #include <kconfig.h>
 #include <kdialog.h>
-#include <kpluginfactory.h>
 #include <klocale.h>
 #include <kaction.h>
 #include <kactioncollection.h>
diff --git a/part/dialogs/filetypeconfigwidget.ui b/part/dialogs/filetypeconfigwidget.ui
index 43f2e5aac0..b3500774ba 100644
--- a/part/dialogs/filetypeconfigwidget.ui
+++ b/part/dialogs/filetypeconfigwidget.ui
@@ -211,30 +211,6 @@
      </layout>
     </widget>
    </item>
-   <item>
-    <layout class="QHBoxLayout">
-     <item>
-      <spacer>
-       <property name="orientation">
-        <enum>Qt::Horizontal</enum>
-       </property>
-       <property name="sizeHint" stdset="0">
-        <size>
-         <width>1</width>
-         <height>0</height>
-        </size>
-       </property>
-      </spacer>
-     </item>
-     <item>
-      <widget class="QPushButton" name="btnDownload">
-       <property name="text">
-        <string>Download Highlighting Files...</string>
-       </property>
-      </widget>
-     </item>
-    </layout>
-   </item>
    <item>
     <spacer>
      <property name="orientation">
diff --git a/part/dialogs/katedialogs.cpp b/part/dialogs/katedialogs.cpp
index bdc9738e92..5182ed6696 100644
--- a/part/dialogs/katedialogs.cpp
+++ b/part/dialogs/katedialogs.cpp
@@ -37,11 +37,9 @@
 #include "kateviglobal.h"
 #include "katevikeyparser.h"
 #include "kateschema.h"
-#include "katesyntaxdocument.h"
 #include "katemodeconfigpage.h"
 #include "kateview.h"
 #include "katepartpluginmanager.h"
-#include "kpluginselector.h"
 #include "spellcheck/spellcheck.h"
 
 // auto generated ui files
@@ -59,10 +57,6 @@
 
 #include <ktexteditor/plugin.h>
 
-#include <kio/job.h>
-#include <kio/jobclasses.h>
-#include <kio/netaccess.h>
-
 #include <kapplication.h>
 #include <kcharsets.h>
 #include <kcolorbutton.h>
@@ -108,15 +102,12 @@
 #include <QtGui/QSlider>
 #include <QtCore/QStringList>
 #include <QtGui/QTabWidget>
+#include <QtGui/QListWidget>
 #include <QtCore/QTextCodec>
 #include <QtCore/QTextStream>
 #include <QtGui/QToolButton>
 #include <QtGui/QWhatsThis>
 #include <QtGui/QKeyEvent>
-#include <QtXml/QDomDocument>
-
-// trailing slash is important
-#define HLDOWNLOADPATH "http://kate.kde.org/syntax/"
 
 //END
 
@@ -1159,25 +1150,23 @@ KatePartPluginConfigPage::KatePartPluginConfigPage (QWidget *parent)
   QVBoxLayout *layout = new QVBoxLayout;
   layout->setMargin(0);
 
-  plugins.clear();
-
-  int i = 0;
+  // qt4compat: plain check list instead of KPluginSelector (no KService)
+  m_list = new QListWidget(this);
   foreach (const KatePartPluginInfo &info, KatePartPluginManager::self()->pluginList())
   {
-    KPluginInfo it(info.service());
-    it.setPluginEnabled(info.load);
-    plugins.append(it);
-    i++;
+    QListWidgetItem *item = new QListWidgetItem(info.service() ? info.service()->name() : info.saveName(), m_list);
+    item->setFlags(item->flags() | Qt::ItemIsUserCheckable);
+    item->setCheckState(info.load ? Qt::Checked : Qt::Unchecked);
+  }
+  if (m_list->count() == 0)
+  {
+    QListWidgetItem *item = new QListWidgetItem(i18n("No editor plugins are available in this build."), m_list);
+    item->setFlags(Qt::NoItemFlags);
   }
 
-  selector = new KPluginSelector(0);
-
-  connect(selector, SIGNAL(changed(bool)), this, SLOT(slotChanged()));
-  connect(selector, SIGNAL(configCommitted(QByteArray)), this, SLOT(slotChanged()));
-
-  selector->addPlugins(plugins, KPluginSelector::IgnoreConfigFile, i18n("Editor Plugins"), "Editor");
+  connect(m_list, SIGNAL(itemChanged(QListWidgetItem*)), this, SLOT(slotChanged()));
 
-  layout->addWidget(selector);
+  layout->addWidget(m_list);
   setLayout(layout);
 }
 
@@ -1187,11 +1176,10 @@ KatePartPluginConfigPage::~KatePartPluginConfigPage ()
 
 void KatePartPluginConfigPage::apply ()
 {
-  selector->updatePluginsState();
-
   KatePartPluginList &katePluginList = KatePartPluginManager::self()->pluginList();
-  for (int i=0; i < plugins.count(); i++) {
-    if (plugins[i].isPluginEnabled()) {
+  for (int i=0; i < katePluginList.count() && i < m_list->count(); i++) {
+    const bool enabled = m_list->item(i)->checkState() == Qt::Checked;
+    if (enabled) {
       if (!katePluginList[i].load) {
         KatePartPluginManager::self()->loadPlugin(katePluginList[i]);
         KatePartPluginManager::self()->enablePlugin(katePluginList[i]);
@@ -1202,179 +1190,30 @@ void KatePartPluginConfigPage::apply ()
         KatePartPluginManager::self()->unloadPlugin(katePluginList[i]);
       }
     }
+    m_list->item(i)->setCheckState(katePluginList[i].load ? Qt::Checked : Qt::Unchecked);
   }
 }
 
 void KatePartPluginConfigPage::reload ()
 {
-  selector->load();
+  const KatePartPluginList &katePluginList = KatePartPluginManager::self()->pluginList();
+  for (int i=0; i < katePluginList.count() && i < m_list->count(); i++)
+    m_list->item(i)->setCheckState(katePluginList[i].load ? Qt::Checked : Qt::Unchecked);
 }
 
 void KatePartPluginConfigPage::reset ()
 {
-  selector->load();
+  reload();
 }
 
 void KatePartPluginConfigPage::defaults ()
 {
-  selector->defaults();
+  const KatePartPluginList &katePluginList = KatePartPluginManager::self()->pluginList();
+  for (int i=0; i < katePluginList.count() && i < m_list->count(); i++)
+    m_list->item(i)->setCheckState(katePluginList[i].isEnabledByDefault() ? Qt::Checked : Qt::Unchecked);
 }
 //END KatePartPluginConfigPage
 
-
-//BEGIN KateHlDownloadDialog
-KateHlDownloadDialog::KateHlDownloadDialog(QWidget *parent, const char *name, bool modal)
-  : KDialog( parent )
-{
-  setCaption( i18n("Highlight Download") );
-  setButtons( User1 | Close );
-  setButtonGuiItem( User1, KGuiItem(i18n("&Install")) );
-  setDefaultButton( User1 );
-  setObjectName( name );
-  setModal( modal );
-
-  KVBox* vbox = new KVBox(this);
-  setMainWidget(vbox);
-  vbox->setSpacing(-1);
-  new QLabel(i18n("Select the syntax highlighting files you want to update:"), vbox);
-  list = new QTreeWidget(vbox);
-  list->setColumnCount(4);
-  list->setHeaderLabels(QStringList() << "" << i18n("Name") << i18n("Installed") << i18n("Latest"));
-  list->setSelectionMode(QAbstractItemView::MultiSelection);
-  list->setAllColumnsShowFocus(true);
-  list->setRootIsDecorated(false);
-  list->setColumnWidth(0, 22);
-
-  new QLabel(i18n("<b>Note:</b> New versions are selected automatically."), vbox);
-  setButtonIcon(User1, KIcon("dialog-ok"));
-
-  transferJob = KIO::get(
-    KUrl(QString(HLDOWNLOADPATH)
-       + QString("update-")
-       + KateGlobal::katePartVersion()
-       + QString(".xml")), KIO::Reload );
-  connect(transferJob, SIGNAL(data(KIO::Job*,QByteArray)),
-    this, SLOT(listDataReceived(KIO::Job*,QByteArray)));
-//        void data( KIO::Job *, const QByteArray &data);
-  resize(450, 400);
-  connect(this,SIGNAL(user1Clicked()),this,SLOT(slotUser1()));
-}
-
-KateHlDownloadDialog::~KateHlDownloadDialog(){}
-
-/// Split typical version string (\c major.minor.patch) into
-/// numeric components, convert 'em to \c unsigned and form a
-/// single value that can be compared w/ other versions
-/// using relation operators.
-/// \note It takes into account only first 3 numbers
-unsigned KateHlDownloadDialog::parseVersion(const QString& version_string)
-{
-  unsigned vn[3] = {0, 0, 0};
-  unsigned idx = 0;
-  foreach (const QString& n, version_string.split("."))
-  {
-    vn[idx++] = n.toUInt();
-    if (idx == sizeof(vn))
-      break;
-  }
-  return KDE_MAKE_VERSION(vn[0], vn[1], vn[2]);
-}
-
-void KateHlDownloadDialog::listDataReceived(KIO::Job *, const QByteArray &data)
-{
-  if (!transferJob || transferJob->isErrorPage())
-  {
-    enableButton( User1, false );
-    if (data.size()==0)
-      KMessageBox::error(this,i18n("The list of highlightings could not be found on / retrieved from the server"));
-    return;
-  }
-
-  listData+=QString(data);
-  kDebug(13000)<<QString("CurrentListData: ")<<listData;
-  kDebug(13000)<<QString("Data length: %1").arg(data.size());
-  kDebug(13000)<<QString("listData length: %1").arg(listData.length());
-  if (data.size()==0)
-  {
-    if (listData.length()>0)
-    {
-      QString installedVersion;
-      KateHlManager *hlm=KateHlManager::self();
-      QDomDocument doc;
-      doc.setContent(listData);
-      QDomElement DocElem=doc.documentElement();
-      QDomNode n=DocElem.firstChild();
-      KateHighlighting *hl = 0;
-
-      if (n.isNull()) kDebug(13000)<<"There is no usable childnode";
-      while (!n.isNull())
-      {
-        installedVersion="    --";
-
-        QDomElement e=n.toElement();
-        if (!e.isNull())
-        kDebug(13000)<<QString("NAME: ")<<e.tagName()<<QString(" - ")<<e.attribute("name");
-        n=n.nextSibling();
-
-        QString Name=e.attribute("name");
-
-        for (int i=0;i<hlm->highlights();i++)
-        {
-          hl=hlm->getHl(i);
-          if (hl && hl->name()==Name)
-          {
-            installedVersion="    "+hl->version();
-            break;
-          }
-          else hl = 0;
-        }
-
-        // autoselect entry if new or updated.
-        QTreeWidgetItem* entry = new QTreeWidgetItem(list);
-        entry->setText(0, "");
-        entry->setText(1, e.attribute("name"));
-        entry->setText(2, installedVersion);
-        entry->setText(3, e.attribute("version"));
-        entry->setText(4, e.attribute("url"));
-
-        bool is_fresh = false;
-        if (hl)
-        {
-          unsigned prev_version = parseVersion(hl->version());
-          unsigned next_version = parseVersion(e.attribute("version"));
-          is_fresh = prev_version < next_version;
-        }
-        else is_fresh = true;
-        if (is_fresh)
-        {
-          entry->treeWidget()->setItemSelected(entry, true);
-          entry->setIcon(0, SmallIcon(("get-hot-new-stuff")));
-        }
-      }
-      list->resizeColumnToContents(1);
-      list->sortItems(1, Qt::AscendingOrder);
-    }
-  }
-}
-
-void KateHlDownloadDialog::slotUser1()
-{
-  QString destdir=KGlobal::dirs()->saveLocation("data","katepart/syntax/");
-  foreach (QTreeWidgetItem *it, list->selectedItems())
-  {
-    KUrl src(it->text(4));
-    QString filename=src.fileName(KUrl::ObeyTrailingSlash);
-    QString dest = destdir+filename;
-
-    KIO::NetAccess::download(src,dest, this);
-  }
-
-  // update Config !!
-  // this rewrites the cache....
-  KateSyntaxDocument doc (KateHlManager::self()->getKConfig(), true);
-}
-//END KateHlDownloadDialog
-
 //BEGIN KateGotoBar
 KateGotoBar::KateGotoBar(KTextEditor::View *view, QWidget *parent)
   : KateViewBarWidget( true, parent )
diff --git a/part/dialogs/katedialogs.h b/part/dialogs/katedialogs.h
index 0971e4ba3d..b2997ae04b 100644
--- a/part/dialogs/katedialogs.h
+++ b/part/dialogs/katedialogs.h
@@ -46,7 +46,6 @@
 #include <QtCore/QStringList>
 #include <QtGui/QColor>
 #include <QtGui/QTabWidget>
-#include <QtGui/QTreeWidget>
 
 struct syntaxContextData;
 
@@ -55,19 +54,11 @@ class KateDocument;
 class KateView;
 class KatePartPluginInfo;
 
-namespace KIO
-{
-  class Job;
-  class TransferJob;
-}
-
 class KComboBox;
 class KShortcutsEditor;
 class KTemporaryFile;
 class KIntNumInput;
 class KIntSpinBox;
-class KPluginSelector;
-class KPluginInfo;
 class KProcess;
 
 class QCheckBox;
@@ -366,32 +357,10 @@ class KatePartPluginConfigPage : public KateConfigPage
     void defaults ();
 
   private:
-    KPluginSelector *selector;
-    QList<KPluginInfo> plugins;
+    class QListWidget *m_list;
 };
 
 
-class KateHlDownloadDialog: public KDialog
-{
-  Q_OBJECT
-
-  public:
-    KateHlDownloadDialog(QWidget *parent, const char *name, bool modal);
-    ~KateHlDownloadDialog();
-
-  private:
-    static unsigned parseVersion(const QString&);
-    class QTreeWidget  *list;
-    class QString listData;
-    KIO::TransferJob *transferJob;
-
-  private Q_SLOTS:
-    void listDataReceived(KIO::Job *, const QByteArray &data);
-
-  public Q_SLOTS:
-    void slotUser1();
-};
-
 /**
  * This dialog will prompt the user for what do with a file that is
  * modified on disk.
diff --git a/part/document/katedocument.cpp b/part/document/katedocument.cpp
index d6b27dd82c..96f538cc1a 100644
--- a/part/document/katedocument.cpp
+++ b/part/document/katedocument.cpp
@@ -58,11 +58,6 @@
 #include <ktexteditor/plugin.h>
 #include <ktexteditor/loadsavefiltercheckplugin.h>
 
-#include <kio/job.h>
-#include <kio/jobuidelegate.h>
-#include <kio/netaccess.h>
-#include <kfileitem.h>
-
 #include <klocale.h>
 #include <kglobal.h>
 #include <kmenu.h>
@@ -79,9 +74,6 @@
 #include <kstandarddirs.h>
 #include <kstringhandler.h>
 
-#include <kservicetypetrader.h>
-
-#include <QtDBus/QtDBus>
 #include <QtGui/QApplication>
 #include <QtCore/QTimer>
 #include <QtCore/QFile>
@@ -98,7 +90,6 @@
 #define EDIT_DEBUG if (0) kDebug()
 #endif
 
-static int dummy = 0;
 
 inline bool isStartBracket( const QChar& c ) { return c == '{' || c == '[' || c == '('; }
 inline bool isEndBracket  ( const QChar& c ) { return c == '}' || c == ']' || c == ')'; }
@@ -150,12 +141,6 @@ KateDocument::KateDocument ( bool bSingleViewMode, bool bBrowserView,
    */
   setProgressInfoEnabled (false);
   
-  QString pathName ("/Kate/Document/%1");
-  pathName = pathName.arg (++dummy);
-
-  // my dbus object
-  QDBusConnection::sessionBus().registerObject (pathName, this, QDBusConnection::ExportAdaptors | QDBusConnection::ExportScriptableSlots);
-
   // register doc at factory
   KateGlobal::self()->registerDocument(this);
 
@@ -2148,18 +2133,9 @@ bool KateDocument::saveFile()
       else
         backupSuccess = true;
     }
-    else // remote file mode, kio
+    else // remote file mode: no KIO in this build, nothing we can back up
     {
-      // get the right permissions, start with safe default
-      KIO::UDSEntry fentry;
-      if (KIO::NetAccess::stat (url(), fentry, QApplication::activeWindow())) {
-        // do a evil copy which will overwrite target if possible
-        KFileItem item (fentry, url());
-        KIO::FileCopyJob *job = KIO::file_copy ( url(), u, item.permissions(), KIO::Overwrite );
-        backupSuccess = KIO::NetAccess::synchronousRun(job, QApplication::activeWindow());
-      }
-      else
-        backupSuccess = true;
+      backupSuccess = true;
     }
 
     // backup has failed, ask user how to proceed
@@ -4876,9 +4852,8 @@ void KateDocument::slotStarted (KIO::Job *job)
       
       /**
        * perhaps some message about loading in one second!
-       * remember job pointer, we want to be able to kill it!
+       * (qt4compat: there is no KIO job to remember or kill in this build)
        */
-      m_loadingJob = job;
       QTimer::singleShot (1000, this, SLOT(slotTriggerLoadingMessage()));
     }
   }
@@ -4947,13 +4922,9 @@ void KateDocument::slotTriggerLoadingMessage ()
   m_loadingMessage->setPosition(KTextEditor::Message::TopInView);
 
   /**
-   * if around job: add cancel action
+   * qt4compat: loading is synchronous and local, there is no job to abort,
+   * so no cancel action is offered
    */
-  if (m_loadingJob) {
-    QAction *cancel = new QAction ( i18n ("&Abort Loading"), 0);
-    connect (cancel, SIGNAL(triggered()), this, SLOT(slotAbortLoading()));
-    m_loadingMessage->addAction (cancel);
-  }
   
   /**
    * really post message
@@ -4964,17 +4935,8 @@ void KateDocument::slotTriggerLoadingMessage ()
 void KateDocument::slotAbortLoading ()
 {
   /**
-   * no job, no work
-   */
-  if (!m_loadingJob)
-    return;
-  
-  /**
-   * abort loading if any job
-   * signal results!
+   * qt4compat: no KIO job, nothing to abort
    */
-  m_loadingJob->kill (KJob::EmitResult);
-  m_loadingJob = 0;
 }
 
 bool KateDocument::save()
diff --git a/part/document/katedocument.h b/part/document/katedocument.h
index f96769d660..305671d7d5 100644
--- a/part/document/katedocument.h
+++ b/part/document/katedocument.h
@@ -54,7 +54,7 @@ namespace KTextEditor {
   class TemplateScript;
 }
 
-namespace KIO { class TransferJob; }
+namespace KIO { class Job; class TransferJob; }
 
 namespace Kate { class SwapFile; }
 
@@ -1236,7 +1236,6 @@ public:
     /**
      * loading job, we want to cancel with cancel in the loading message
      */
-    QPointer<KJob> m_loadingJob;
     
     /**
      * message to show during loading
diff --git a/part/katepartprivate_export.h b/part/katepartprivate_export.h
index 491bc93f18..02e9b1b4b9 100644
--- a/part/katepartprivate_export.h
+++ b/part/katepartprivate_export.h
@@ -25,7 +25,7 @@
 /* needed for KDE_EXPORT and KDE_IMPORT macros */
 #include <kdemacros.h>
 
-#if defined(KDELIBS_STATIC_LIBS)
+#if defined(KDELIBS_STATIC_LIBS) || defined(KATEPART_STATIC_LIBS)
    /* No export/import for static libraries */
 # define KATEPART_TESTS_EXPORT
 #elif defined(MAKE_KATEPARTINTERFACES_LIB)
diff --git a/part/mode/katemodeconfigpage.cpp b/part/mode/katemodeconfigpage.cpp
index 196230fffb..3ed1062870 100644
--- a/part/mode/katemodeconfigpage.cpp
+++ b/part/mode/katemodeconfigpage.cpp
@@ -87,7 +87,6 @@ ModeConfigPage::ModeConfigPage( QWidget *parent )
   connect( ui->btnDelete, SIGNAL(clicked()), this, SLOT(deleteType()) );
   ui->btnMimeTypes->setIcon(KIcon("tools-wizard"));
   connect(ui->btnMimeTypes, SIGNAL(clicked()), this, SLOT(showMTDlg()));
-  connect( ui->btnDownload, SIGNAL(clicked()), this, SLOT(hlDownload()) );
 
   reload();
 
@@ -311,10 +310,4 @@ void ModeConfigPage::showMTDlg()
   }
 }
 
-void ModeConfigPage::hlDownload()
-{
-  KateHlDownloadDialog diag(this,"hlDownload",true);
-  diag.exec();
-}
-
 // kate: space-indent on; indent-width 2; replace-tabs on;
diff --git a/part/mode/katemodeconfigpage.h b/part/mode/katemodeconfigpage.h
index 74dfe14847..684d3a7ea8 100644
--- a/part/mode/katemodeconfigpage.h
+++ b/part/mode/katemodeconfigpage.h
@@ -55,7 +55,6 @@ class ModeConfigPage : public KateConfigPage
     void typeChanged (int type);
     void showMTDlg();
     void save ();
-    void hlDownload ();
 
   private:
     Ui::FileTypeConfigWidget *ui;
diff --git a/part/schema/katecolortreewidget.cpp b/part/schema/katecolortreewidget.cpp
index 86e4d25159..cb9dfbd2da 100644
--- a/part/schema/katecolortreewidget.cpp
+++ b/part/schema/katecolortreewidget.cpp
@@ -34,6 +34,7 @@
 #include <QDebug>
 #include <QEvent>
 #include <QKeyEvent>
+#include <kiconloader.h>
 
 //BEGIN KateColorTreeItem
 class KateColorTreeItem : public QTreeWidgetItem
diff --git a/part/schema/kateschemaconfig.cpp b/part/schema/kateschemaconfig.cpp
index 71f5bc1987..b3cf85dce4 100644
--- a/part/schema/kateschemaconfig.cpp
+++ b/part/schema/kateschemaconfig.cpp
@@ -44,6 +44,8 @@
 
 #include <QtGui/QPushButton>
 #include <QtGui/QProgressDialog>
+#include <kcombobox.h>
+#include <kglobal.h>
 //END
 
 
diff --git a/part/script/katescript.cpp b/part/script/katescript.cpp
index d17770060a..cb7d9dc3f0 100644
--- a/part/script/katescript.cpp
+++ b/part/script/katescript.cpp
@@ -38,6 +38,7 @@
 #include <klocale.h>
 #include <kstandarddirs.h>
 #include <klocalizedstring.h>
+#include <kglobal.h>
 
 //BEGIN conversion functions for Cursors and Ranges
 /** Converstion function from KTextEditor::Cursor to QtScript cursor */
diff --git a/part/script/katescripthelpers.cpp b/part/script/katescripthelpers.cpp
index eb6318f4ba..e53667bf15 100644
--- a/part/script/katescripthelpers.cpp
+++ b/part/script/katescripthelpers.cpp
@@ -34,6 +34,7 @@
 #include <klocale.h>
 #include <klocalizedstring.h>
 #include <kstandarddirs.h>
+#include <kglobal.h>
 
 namespace Kate {
 namespace Script {
diff --git a/part/script/katescriptmanager.cpp b/part/script/katescriptmanager.cpp
index 639f7e5815..f1f59d7db9 100644
--- a/part/script/katescriptmanager.cpp
+++ b/part/script/katescriptmanager.cpp
@@ -35,6 +35,7 @@
 #include <kconfiggroup.h>
 #include <kstandarddirs.h>
 #include <kde_file.h>
+#include <kglobal.h>
 
 #include "kateglobal.h"
 #include "katecmd.h"
diff --git a/part/search/kateregexp.cpp b/part/search/kateregexp.cpp
index 46c90bfcf1..5da8517ec0 100644
--- a/part/search/kateregexp.cpp
+++ b/part/search/kateregexp.cpp
@@ -21,10 +21,22 @@
 
 #include "kateregexp.h"
 
-KateRegExp::KateRegExp(const QString &pattern, Qt::CaseSensitivity cs,
-    QRegExp::PatternSyntax syntax)
-  : m_regExp(pattern, cs, syntax)
+KateRegExp::KateRegExp(const QString &pattern, Qt::CaseSensitivity cs)
+  : m_pattern(pattern)
+  , m_caseSensitivity(cs)
+  , m_multiLine(false)
 {
+  compile();
+}
+
+void KateRegExp::compile()
+{
+  KatePcre::Options options = KatePcre::NoOptions;
+  if (m_caseSensitivity == Qt::CaseInsensitive)
+    options |= KatePcre::CaseInsensitive;
+  if (m_multiLine)
+    options |= KatePcre::MultiLine;
+  m_regExp.setPattern(m_pattern, options);
 }
 
 // these things can besides '.' and '\s' make apptern multi-line:
@@ -196,8 +208,11 @@ int KateRegExp::repairPattern(bool & stillMultiLine)
     }
   }
 
-  // Overwrite with repaired pattern
-  m_regExp.setPattern(output);
+  // Overwrite with repaired pattern; a pattern that still spans lines runs
+  // on the joined text and wants "^"/"$" at every line boundary
+  m_pattern = output;
+  m_multiLine = stillMultiLine;
+  compile();
   return replaceCount;
 }
 
@@ -307,21 +322,12 @@ bool KateRegExp::isMultiLine() const
 
 int KateRegExp::indexIn(const QString &str, int start, int end) const
 {
-  return m_regExp.indexIn(str.left(end), start, QRegExp::CaretAtZero);
+  return m_regExp.indexIn(str, start, end);
 }
 
-
-
 int KateRegExp::lastIndexIn(const QString &str, int start, int end) const
 {
-  const int index = m_regExp.lastIndexIn(str.mid(start, end-start), -1, QRegExp::CaretAtZero);
-
-  if (index == -1)
-    return -1;
-
-  const int index2 = m_regExp.indexIn(str.left(end), start+index, QRegExp::CaretAtZero);
-
-  return index2;
+  return m_regExp.lastIndexIn(str, start, end);
 }
 
 // kate: space-indent on; indent-width 2; replace-tabs on;
diff --git a/part/search/kateregexp.h b/part/search/kateregexp.h
index 4cd02e82c2..6f4cb33b05 100644
--- a/part/search/kateregexp.h
+++ b/part/search/kateregexp.h
@@ -22,42 +22,56 @@
 #ifndef _KATE_REGEXP_H_
 #define _KATE_REGEXP_H_
 
-#include <QtCore/QRegExp>
+#include "katepcre.h"
 
+/**
+ * Regular expression for the document search (search bar, vi mode,
+ * KTextEditor::SearchInterface, the search plugin), on KatePcre. Beyond
+ * plain matching it knows whether a pattern can match across lines and
+ * repairs "." and "\\s" so they do not, as KateRegExpSearch needs.
+ */
 class KateRegExp
 {
   public:
-    explicit KateRegExp(const QString &pattern, Qt::CaseSensitivity cs = Qt::CaseSensitive,
-        QRegExp::PatternSyntax syntax = QRegExp::RegExp2);
+    explicit KateRegExp(const QString &pattern, Qt::CaseSensitivity cs = Qt::CaseSensitive);
 
-    bool isEmpty() const { return m_regExp.isEmpty(); }
+    bool isEmpty() const { return m_pattern.isEmpty(); }
     bool isValid() const { return m_regExp.isValid(); }
-    QString pattern() const { return m_regExp.pattern(); }
-    int numCaptures() const { return m_regExp.numCaptures(); }
+    QString errorString() const { return m_regExp.errorString(); }
+    QString pattern() const { return m_pattern; }
+    int numCaptures() const { return m_regExp.captureCount(); }
     int pos(int nth = 0) const { return m_regExp.pos(nth); }
     QString cap(int nth = 0) const { return m_regExp.cap(nth); }
+    QStringList capturedTexts() const { return m_regExp.capturedTexts(); }
     int matchedLength() const { return m_regExp.matchedLength(); }
 
+    /**
+     * First match in str[offset, end). "^" only matches at 0 (at line
+     * starts too for a multi-line pattern, see repairPattern()).
+     */
     int indexIn(const QString &str, int offset, int end) const;
 
     /**
-     * This function is a replacement for QRegExp.lastIndexIn that
-     * returns the last match that would have been found when
-     * searching forwards, which QRegExp.lastIndexIn does not.
-     * We need this behavior to allow the user to jump back to
-     * the last match.
+     * The last match that a forward search from offset would find. We need
+     * this behavior to allow the user to jump back to the last match.
      *
      * \param str        Text to search in
-     * \param offset     Offset (-1 starts from end, -2 from one before the end)
+     * \param offset     Start of the search
+     * \param end        End of the searched text
      * \return           Index of match or -1 if no match is found
      */
     int lastIndexIn(const QString &str, int offset, int end) const;
 
+    /** true if the pattern matches all of str (QRegExp::exactMatch) */
+    bool exactMatch(const QString &str) const { return m_regExp.exactMatch(str); }
+
     /**
      * Repairs a regular Expression pattern.
-     * This is a workaround to make "." and "\s" not match
-     * newlines, which currently is the unconfigurable
-     * default in QRegExp.
+     * This is a workaround to make "." and "\\s" not match
+     * newlines, which is the default in a Perl regular expression.
+     * A pattern that still matches across lines afterwards is
+     * recompiled in multi-line mode, so "^" and "$" match at every
+     * line boundary of the joined text it is run on.
      *
      * \param stillMultiLine  Multi-line after reparation flag
      * \return                Number of replacements done
@@ -73,7 +87,12 @@ class KateRegExp
     bool isMultiLine() const;
 
   private:
-    QRegExp m_regExp;
+    void compile();
+
+    QString m_pattern;
+    Qt::CaseSensitivity m_caseSensitivity;
+    bool m_multiLine;
+    KatePcre m_regExp;
 };
 
 #endif // KATEREGEXP_H
diff --git a/part/search/katesearchbar.cpp b/part/search/katesearchbar.cpp
index 5357149453..4c191b216b 100644
--- a/part/search/katesearchbar.cpp
+++ b/part/search/katesearchbar.cpp
@@ -44,6 +44,7 @@
 #include <QtGui/QComboBox>
 #include <QtGui/QCheckBox>
 #include <QtGui/QShortcut>
+#include <klocale.h>
 
 // Turn debug messages on/off here
 // #define FAST_DEBUG_ENABLE
@@ -703,7 +704,7 @@ bool KateSearchBar::isPatternValid() const {
         return false;
 
     return searchOptions().testFlag(Search::WholeWords) ? searchPattern().trimmed() == searchPattern() :
-           searchOptions().testFlag(Search::Regex)      ? QRegExp(searchPattern()).isValid() :
+           searchOptions().testFlag(Search::Regex)      ? KateRegExp(searchPattern()).isValid() :
                                                           true;
 }
 
diff --git a/part/snippet/katesnippetglobal.cpp b/part/snippet/katesnippetglobal.cpp
index cdd5b0b0ff..223f481432 100644
--- a/part/snippet/katesnippetglobal.cpp
+++ b/part/snippet/katesnippetglobal.cpp
@@ -22,9 +22,7 @@
 #include "katesnippetglobal.h"
 
 #include <klocale.h>
-#include <kpluginfactory.h>
 #include <kaboutdata.h>
-#include <kpluginloader.h>
 #include <ktexteditor/view.h>
 #include <ktexteditor/document.h>
 #include <ktexteditor/codecompletioninterface.h>
diff --git a/part/snippet/snippet.cpp b/part/snippet/snippet.cpp
index d232f7aebc..0524f8627a 100644
--- a/part/snippet/snippet.cpp
+++ b/part/snippet/snippet.cpp
@@ -31,7 +31,7 @@
 #include "katesnippetglobal.h"
 #include <KActionCollection>
 #include <KAction>
-#include <KParts/MainWindow>
+#include <kparts/mainwindow.h>
 
 Snippet::Snippet()
     : QStandardItem(i18n("<empty snippet>")), m_action(0)
diff --git a/part/snippet/snippetrepository.cpp b/part/snippet/snippetrepository.cpp
index 3a726e4938..2be0370311 100644
--- a/part/snippet/snippetrepository.cpp
+++ b/part/snippet/snippetrepository.cpp
@@ -46,6 +46,7 @@
 
 #include <KAction>
 #include <KShortcut>
+#include <kglobal.h>
 
 #include "snippetstore.h"
 
diff --git a/part/snippet/snippetstore.cpp b/part/snippet/snippetstore.cpp
index cd4d62aaa7..3c4fe7f675 100644
--- a/part/snippet/snippetstore.cpp
+++ b/part/snippet/snippetstore.cpp
@@ -31,6 +31,7 @@
 
 #include <ktexteditor/editor.h>
 #include <ktexteditor/templateinterface2.h>
+#include <kglobal.h>
 
 SnippetStore* SnippetStore::m_self = 0;
 
diff --git a/part/snippet/snippetview.cpp b/part/snippet/snippetview.cpp
index f69c38bebf..23757efc6a 100644
--- a/part/snippet/snippetview.cpp
+++ b/part/snippet/snippetview.cpp
@@ -39,8 +39,6 @@
 
 #include <KGlobalSettings>
 
-#include <KNS3/DownloadDialog>
-#include <knewstuff3/uploaddialog.h>
 
 SnippetView::SnippetView(KateSnippetGlobal* plugin, QWidget* parent)
  : QWidget(parent), Ui::SnippetViewBase(), m_plugin(plugin)
@@ -79,10 +77,6 @@ SnippetView::SnippetView(KateSnippetGlobal* plugin, QWidget* parent)
     connect(m_removeRepoAction, SIGNAL(triggered()), this, SLOT(slotRemoveRepo()));
     addAction(m_removeRepoAction);
 
-    m_putNewStuffAction = new KAction(KIcon("get-hot-new-stuff"), i18n("Publish Repository"), this);
-    connect(m_putNewStuffAction, SIGNAL(triggered()), this, SLOT(slotSnippetToGHNS()));
-    addAction(m_putNewStuffAction);
-
     QAction* separator = new QAction(this);
     separator->setSeparator(true);
     addAction(separator);
@@ -97,12 +91,6 @@ SnippetView::SnippetView(KateSnippetGlobal* plugin, QWidget* parent)
     connect(m_removeSnippetAction, SIGNAL(triggered()), this, SLOT(slotRemoveSnippet()));
     addAction(m_removeSnippetAction);
 
-    addAction(separator);
-
-    m_getNewStuffAction = new KAction(KIcon("get-hot-new-stuff"), i18n("Get New Snippets"), this);
-    connect(m_getNewStuffAction, SIGNAL(triggered()), this, SLOT(slotGHNS()));
-    addAction(m_getNewStuffAction);
-
     connect(snippetTree->selectionModel(), SIGNAL(selectionChanged(QItemSelection,QItemSelection)), SLOT(validateActions()));
     validateActions();
 }
@@ -122,7 +110,6 @@ void SnippetView::validateActions()
     m_addRepoAction->setEnabled(true);
     m_editRepoAction->setEnabled(selectedRepo);
     m_removeRepoAction->setEnabled(selectedRepo);
-    m_putNewStuffAction->setEnabled(selectedRepo);
 
     m_addSnippetAction->setEnabled(selectedRepo || selectedSnippet);
     m_editSnippetAction->setEnabled(selectedSnippet);
@@ -161,7 +148,6 @@ void SnippetView::contextMenu (const QPoint& pos)
         menu.addTitle(i18n("Snippets"));
 
         menu.addAction(m_addRepoAction);
-        menu.addAction(m_getNewStuffAction);
 
         menu.exec(snippetTree->mapToGlobal(pos));
     } else if (Snippet* snippet = dynamic_cast<Snippet*>( item )) {
@@ -178,7 +164,6 @@ void SnippetView::contextMenu (const QPoint& pos)
 
         menu.addAction(m_editRepoAction);
         menu.addAction(m_removeRepoAction);
-        menu.addAction(m_putNewStuffAction);
         menu.addSeparator();
 
         menu.addAction(m_addSnippetAction);
@@ -286,42 +271,6 @@ void SnippetView::slotFilterChanged()
     m_proxy->changeFilter( filterText->text() );
 }
 
-void SnippetView::slotGHNS()
-{
-    KNS3::DownloadDialog dialog("ktexteditor_codesnippets_core.knsrc", this);
-    dialog.exec();
-    foreach ( const KNS3::Entry& entry, dialog.changedEntries() ) {
-        foreach ( const QString& path, entry.uninstalledFiles() ) {
-            if ( path.endsWith(".xml") ) {
-                if ( SnippetRepository* repo = SnippetStore::self()->repositoryForFile(path) ) {
-                    repo->remove();
-                }
-            }
-        }
-        foreach ( const QString& path, entry.installedFiles() ) {
-            if ( path.endsWith(".xml") ) {
-                SnippetStore::self()->appendRow(new SnippetRepository(path));
-            }
-        }
-    }
-}
-
-void SnippetView::slotSnippetToGHNS()
-{
-    QStandardItem* item = currentItem();
-    if ( !item)
-        return;
-
-    SnippetRepository* repo = dynamic_cast<SnippetRepository*>( item );
-    if ( !repo )
-        return;
-
-    KNS3::UploadDialog dialog("ktexteditor_codesnippets_core.knsrc", this);
-    dialog.setUploadFile(KUrl::fromPath(repo->file()));
-    dialog.setUploadName(repo->text());
-    dialog.exec();
-}
-
 bool SnippetView::eventFilter(QObject* obj, QEvent* e)
 {
     // no, listening to activated() is not enough since that would also trigger the edit mode which we _dont_ want here
diff --git a/part/snippet/snippetview.h b/part/snippet/snippetview.h
index 2ce2039b77..806de57071 100644
--- a/part/snippet/snippetview.h
+++ b/part/snippet/snippetview.h
@@ -80,15 +80,7 @@ private slots:
      */
     void slotAddSnippet();
 
-    /**
-     * Slot to get hot new stuff.
-     */
-    void slotGHNS();
 
-    /**
-     * Slot to put the selected snippet to GHNS
-     */
-    void slotSnippetToGHNS();
 
     /**
      * Changes the filter of the proxy.
@@ -113,8 +105,6 @@ private:
     KAction* m_addSnippetAction;
     KAction* m_removeSnippetAction;
     KAction* m_editSnippetAction;
-    KAction* m_getNewStuffAction;
-    KAction* m_putNewStuffAction;
 };
 
 #endif
diff --git a/part/spellcheck/ontheflycheck.cpp b/part/spellcheck/ontheflycheck.cpp
index da9d9fdfd2..768cf238c1 100644
--- a/part/spellcheck/ontheflycheck.cpp
+++ b/part/spellcheck/ontheflycheck.cpp
@@ -26,6 +26,7 @@
 #include "ontheflycheck.h"
 
 #include <QTimer>
+#include <kglobal.h>
 
 #include "kateconfig.h"
 #include "kateglobal.h"
diff --git a/part/spellcheck/spellcheckdialog.cpp b/part/spellcheck/spellcheckdialog.cpp
index 8c51f0a109..d276e9cf2b 100644
--- a/part/spellcheck/spellcheckdialog.cpp
+++ b/part/spellcheck/spellcheckdialog.cpp
@@ -39,6 +39,7 @@
 #include <sonnet/dialog.h>
 #include <sonnet/backgroundchecker.h>
 #include <sonnet/speller.h>
+#include <kglobal.h>
 
 KateSpellCheckDialog::KateSpellCheckDialog( KateView* view )
   : QObject( view )
diff --git a/part/syntax/katehighlight.cpp b/part/syntax/katehighlight.cpp
index fe90a082d8..32f777fa49 100644
--- a/part/syntax/katehighlight.cpp
+++ b/part/syntax/katehighlight.cpp
@@ -401,14 +401,23 @@ void KateHighlighting::doHighlight ( const Kate::TextLineData *_prevLine,
         {
           if (item->customStartEnable)
           {
-              if ( oldContext != context ) {
-                oldContext = context;
-                additionalData = m_additionalData[oldContext->hlId];
+              if (item->hasRuleDeliminators)
+              {
+                // the rule's own delimiters (not cached: differs per rule)
+                if (!item->ruleDeliminators.contains(lastChar))
+                  continue;
               }
-              if (customStartEnableDetermined || additionalData->deliminator.contains(lastChar))
-                customStartEnableDetermined = true;
               else
-                continue;
+              {
+                if ( oldContext != context ) {
+                  oldContext = context;
+                  additionalData = m_additionalData[oldContext->hlId];
+                }
+                if (customStartEnableDetermined || additionalData->deliminator.contains(lastChar))
+                  customStartEnableDetermined = true;
+                else
+                  continue;
+              }
           }
           else
           {
@@ -1021,6 +1030,22 @@ KateHlItem *KateHighlighting::createKateHlItem(KateSyntaxContextData *data,
   if (!colStr.isEmpty())
     column = colStr.toInt();
 
+  // word delimiters for keyword, WordDetect, Int, Float, HlCHex, HlCOct: the
+  // definition's set, which a rule may extend (additionalDeliminator) or
+  // shrink (weakDeliminator) for itself - an upstream extension
+  QString deliminators = m_additionalData[ buildIdentifier ]->deliminator;
+  const QString ruleWeak = KateHlManager::self()->syntax->groupItemData(data,QString("weakDeliminator"));
+  const QString ruleAdditional = KateHlManager::self()->syntax->groupItemData(data,QString("additionalDeliminator"));
+  const bool ruleDeliminators = !ruleWeak.isEmpty() || !ruleAdditional.isEmpty();
+  if (ruleDeliminators)
+  {
+    for (int i = 0; i < ruleAdditional.length(); ++i)
+      if (!deliminators.contains(ruleAdditional.at(i)))
+        deliminators.append(ruleAdditional.at(i));
+    for (int i = 0; i < ruleWeak.length(); ++i)
+      deliminators.remove(ruleWeak.at(i));
+  }
+
   // Create the item corresponding to its type and set its parameters
   KateHlItem *tmpItem;
 
@@ -1028,7 +1053,7 @@ KateHlItem *KateHighlighting::createKateHlItem(KateSyntaxContextData *data,
   {
     bool keywordInsensitive = insensitive_str.isEmpty() ? !casesensitive : insensitive;
     KateHlKeyword *keyword=new KateHlKeyword(attr,context,regionId,regionId2,keywordInsensitive,
-                                             m_additionalData[ buildIdentifier ]->deliminator);
+                                             deliminators);
 
     //Get the entries for the keyword lookup list
     keyword->addList(KateHlManager::self()->syntax->finddata("highlighting",stringdata));
@@ -1041,7 +1066,7 @@ KateHlItem *KateHighlighting::createKateHlItem(KateSyntaxContextData *data,
   else if (dataname=="RangeDetect") tmpItem=(new KateHlRangeDetect(attr,context,regionId,regionId2, chr, chr1));
   else if (dataname=="LineContinue") tmpItem=(new KateHlLineContinue(attr, context, regionId, regionId2, chr));
   else if (dataname=="StringDetect") tmpItem=(new KateHlStringDetect(attr,context,regionId,regionId2,stringdata,insensitive));
-  else if (dataname=="WordDetect") tmpItem=(new KateHlWordDetect(attr,context,regionId,regionId2,stringdata,insensitive));
+  else if (dataname=="WordDetect") tmpItem=(new KateHlWordDetect(attr,context,regionId,regionId2,stringdata,insensitive,deliminators));
   else if (dataname=="AnyChar") tmpItem=(new KateHlAnyChar(attr,context,regionId,regionId2,stringdata));
   else if (dataname=="RegExpr") tmpItem=(new KateHlRegExpr(attr,context,regionId,regionId2,stringdata, insensitive, minimal));
   else if (dataname=="HlCChar") tmpItem= ( new KateHlCChar(attr,context,regionId,regionId2));
@@ -1057,6 +1082,13 @@ KateHlItem *KateHighlighting::createKateHlItem(KateSyntaxContextData *data,
     return 0;
   }
 
+  // rules that start at a word boundary keep their own delimiter set
+  if (ruleDeliminators && tmpItem->customStartEnable)
+  {
+    tmpItem->hasRuleDeliminators = true;
+    tmpItem->ruleDeliminators = deliminators;
+  }
+
   // set lookAhead & dynamic properties
   tmpItem->lookAhead = lookAhead;
   tmpItem->dynamic = dynamic;
diff --git a/part/syntax/katehighlighthelpers.cpp b/part/syntax/katehighlighthelpers.cpp
index 6da4aebbec..22aa0555d5 100644
--- a/part/syntax/katehighlighthelpers.cpp
+++ b/part/syntax/katehighlighthelpers.cpp
@@ -49,6 +49,7 @@ KateHlItem::KateHlItem(int attribute, KateHlContextModification context,signed c
     column (-1),
     alwaysStartEnable (true),
     customStartEnable (false),
+    hasRuleDeliminators(false),
     haveCache(false),
     cachingHandled(false)
 {
@@ -197,30 +198,24 @@ KateHlItem *KateHlStringDetect::clone(const QStringList *args)
 
 //BEGIN KateHlWordDetect
 
-KateHlWordDetect::KateHlWordDetect(int attribute, KateHlContextModification context, signed char regionId, signed char regionId2, const QString& s, bool inSensitive)
+KateHlWordDetect::KateHlWordDetect(int attribute, KateHlContextModification context, signed char regionId, signed char regionId2, const QString& s, bool inSensitive, const QString &delims)
   : KateHlStringDetect(attribute, context, regionId, regionId2, s, inSensitive)
+  , m_deliminators(delims)
 {
 }
 
-inline bool isWordCharacter(const QChar& c)
-{
-  // The Qt docs say for word characters:
-  //      \w  - Matches a word character (QChar::isLetterOrNumber(), QChar::isMark(), or '_').
-  // see also: http://doc.trolltech.com/qregexp.html
-  return c.isLetterOrNumber() || c.isMark() || c.unicode() == '_';
-}
-
 int KateHlWordDetect::checkHgl(const QString& text, int offset, int len)
 {
-  //NOTE: word boundary means: any non-word character.
+  //NOTE: word boundary means: a word delimiter of the definition (or of
+  //      this rule), or the line boundary - the upstream semantics.
 
-  // make sure there is no letter or number before the word starts
-  if (offset > 0 && isWordCharacter(text.at(offset - 1))) {
+  // make sure the word does not continue a word
+  if (offset > 0 && !m_deliminators.contains(text.at(offset - 1))) {
     return 0;
   }
   offset = KateHlStringDetect::checkHgl(text, offset, len);
-  // make sure there is no letter or number after the word ends
-  if (offset && offset < text.length() && isWordCharacter(text.at(offset))) {
+  // make sure the word ends at a delimiter
+  if (offset && offset < text.length() && !m_deliminators.contains(text.at(offset))) {
     return 0;
   }
   return offset;
@@ -235,7 +230,7 @@ KateHlItem* KateHlWordDetect::clone(const QStringList* args)
   if (newstr == str)
     return this;
 
-  KateHlWordDetect *ret = new KateHlWordDetect(attr, ctx, region, region2, newstr, _inSensitive);
+  KateHlWordDetect *ret = new KateHlWordDetect(attr, ctx, region, region2, newstr, _inSensitive, m_deliminators);
   ret->dynamicChild = true;
   return ret;
 }
@@ -365,6 +360,7 @@ KateHlInt::KateHlInt(int attribute, KateHlContextModification context, signed ch
   : KateHlItem(attribute,context,regionId,regionId2)
 {
   alwaysStartEnable = false;
+  customStartEnable = true; // number must start at a word boundary of the definition
 }
 
 int KateHlInt::checkHgl(const QString& text, int offset, int len)
@@ -400,6 +396,7 @@ KateHlFloat::KateHlFloat(int attribute, KateHlContextModification context, signe
   : KateHlItem(attribute,context, regionId,regionId2)
 {
   alwaysStartEnable = false;
+  customStartEnable = true; // number must start at a word boundary of the definition
 }
 
 int KateHlFloat::checkHgl(const QString& text, int offset, int len)
@@ -496,6 +493,7 @@ KateHlCOct::KateHlCOct(int attribute, KateHlContextModification context, signed
   : KateHlItem(attribute,context,regionId,regionId2)
 {
   alwaysStartEnable = false;
+  customStartEnable = true; // number must start at a word boundary of the definition
 }
 
 int KateHlCOct::checkHgl(const QString& text, int offset, int len)
@@ -531,6 +529,7 @@ KateHlCHex::KateHlCHex(int attribute, KateHlContextModification context,signed c
   : KateHlItem(attribute,context,regionId,regionId2)
 {
   alwaysStartEnable = false;
+  customStartEnable = true; // number must start at a word boundary of the definition
 }
 
 int KateHlCHex::checkHgl(const QString& text, int offset, int len)
@@ -565,6 +564,7 @@ KateHlCFloat::KateHlCFloat(int attribute, KateHlContextModification context, sig
   : KateHlFloat(attribute,context,regionId,regionId2)
 {
   alwaysStartEnable = false;
+  customStartEnable = true; // number must start at a word boundary of the definition
 }
 
 int KateHlCFloat::checkIntHgl(const QString& text, int offset, int len)
@@ -629,10 +629,13 @@ KateHlRegExpr::KateHlRegExpr( int attribute, KateHlContextModification context,
   , _insensitive(insensitive)
   , _minimal(minimal)
   , _lastOffset(-2) // -2 is start value, -1 is "not found at all"
-  , Expr (regexp, _insensitive ? Qt::CaseInsensitive : Qt::CaseSensitive)
+  , Expr (regexp, (_insensitive ? KatePcre::CaseInsensitive : KatePcre::NoOptions)
+                | (_minimal ? KatePcre::Minimal : KatePcre::NoOptions))
 {
-  // minimal or not ;)
-  Expr.setMinimal(_minimal);
+  // an invalid pattern never matches; tell the definition's author once
+  if (!Expr.isValid())
+    kWarning(13010) << "invalid regular expression in highlighting rule:" << regexp
+                    << "-" << Expr.errorString() << "at" << Expr.errorOffset();
 }
 
 int KateHlRegExpr::checkHgl(const QString& text, int offset, int /*len*/)
@@ -653,7 +656,7 @@ int KateHlRegExpr::checkHgl(const QString& text, int offset, int /*len*/)
   }
 
   haveCache = true;
-  _lastOffset = Expr.indexIn( text, offset, QRegExp::CaretAtOffset );
+  _lastOffset = Expr.isValid() ? Expr.indexIn( text, offset ) : -1;
 
   if (_lastOffset == -1) return 0;
 
@@ -679,7 +682,7 @@ KateHlItem *KateHlRegExpr::clone(const QStringList *args)
 
   for (QStringList::Iterator it = escArgs.begin(); it != escArgs.end(); ++it)
   {
-    (*it).replace(QRegExp("(\\W)"), "\\\\1");
+    *it = KatePcre::escape(*it);
   }
 
   dynamicSubstitute(regexp, &escArgs);
diff --git a/part/syntax/katehighlighthelpers.h b/part/syntax/katehighlighthelpers.h
index 02ac40e17d..c8fef1f38d 100644
--- a/part/syntax/katehighlighthelpers.h
+++ b/part/syntax/katehighlighthelpers.h
@@ -22,8 +22,8 @@
 #define __KATE_HIGHLIGHTHELPERS_H__
 
 #include "katehighlight.h"
+#include "katepcre.h"
 
-#include <QRegExp>
 
 class KateHlItem
 {
@@ -63,6 +63,15 @@ class KateHlItem
     bool alwaysStartEnable;
     bool customStartEnable;
 
+    /**
+     * Word delimiters of this rule when it carries its own
+     * weakDeliminator/additionalDeliminator attributes (upstream extension
+     * used by ~150 definitions); otherwise the definition's set applies.
+     * Only meaningful when customStartEnable is set.
+     */
+    bool hasRuleDeliminators;
+    QString ruleDeliminators;
+
     // set to true when you cached something
     bool haveCache;
     // internal for doHighlight, don't set it in the items
@@ -163,10 +172,18 @@ class KateHlStringDetect : public KateHlItem
 class KateHlWordDetect : public KateHlStringDetect
 {
   public:
-    KateHlWordDetect(int attribute, KateHlContextModification context, signed char regionId, signed char regionId2, const QString &, bool inSensitive = false);
+    /**
+     * @param delims word delimiters: the string only matches when the
+     *               characters before and after it are delimiters (or the
+     *               line boundary), as in upstream's WordDetect
+     */
+    KateHlWordDetect(int attribute, KateHlContextModification context, signed char regionId, signed char regionId2, const QString &, bool inSensitive, const QString &delims);
 
     virtual int checkHgl(const QString& text, int offset, int len);
     virtual KateHlItem *clone(const QStringList *args);
+
+  private:
+    QString m_deliminators;
 };
 
 class KateHlRangeDetect : public KateHlItem
@@ -287,9 +304,9 @@ class KateHlRegExpr : public KateHlItem
     KateHlRegExpr(int attribute, KateHlContextModification context,signed char regionId,signed char regionId2 ,const QString &expr, bool insensitive, bool minimal);
 
     virtual int checkHgl(const QString& text, int offset, int len);
-    
+
     virtual void capturedTexts (QStringList &);
-    
+
     virtual KateHlItem *clone(const QStringList *args);
 
   private:
@@ -297,14 +314,13 @@ class KateHlRegExpr : public KateHlItem
     QString _regexp;
     bool _insensitive;
     bool _minimal;
-
-    // optimization stuff below
-    /// index of the last match
     int _lastOffset;
-    /// length of the last match
     int _lastOffsetLength;
-    
-    QRegExp Expr;
+    /**
+     * pcre2, as upstream's QRegularExpression: "^" only matches at column 0,
+     * \w and friends are Unicode aware, "minimal" makes quantifiers lazy.
+     */
+    KatePcre Expr;
 };
 
 class KateHlDetectSpaces : public KateHlItem
diff --git a/part/syntax/katesyntaxdocument.cpp b/part/syntax/katesyntaxdocument.cpp
index 6bc0323fbf..0208c5a529 100644
--- a/part/syntax/katesyntaxdocument.cpp
+++ b/part/syntax/katesyntaxdocument.cpp
@@ -18,6 +18,7 @@
 */
 
 #include "katesyntaxdocument.h"
+#include "katesyntaxmanager.h"
 
 #include <sys/types.h>
 #include <sys/stat.h>
@@ -34,6 +35,8 @@
 
 #include <QtGui/QApplication>
 #include <QtCore/QFile>
+#include <kglobal.h>
+#include <kconfig.h>
 
 // use this to turn on over verbose debug output...
 #undef KSD_OVER_VERBOSE
@@ -46,6 +49,12 @@ KateSyntaxDocument::KateSyntaxDocument(KConfig *config, bool force)
   setupModeList(force);
 }
 
+KateSyntaxDocument::KateSyntaxDocument()
+  : QDomDocument()
+  , m_config (0)
+{
+}
+
 KateSyntaxDocument::~KateSyntaxDocument()
 {
   for (int i=0; i < myModeList.size(); i++)
@@ -290,6 +299,56 @@ KateSyntaxContextData* KateSyntaxDocument::getGroupInfo(const QString& mainGroup
 /**
  * Returns a list with all the keywords inside the list type
  */
+/**
+ * Resolves one <include> of a keyword list: "name" is another list of the
+ * same file, "name##Language" a list of another definition (loaded into a
+ * temporary document). Cycles are cut with the guard set.
+ */
+QStringList KateSyntaxDocument::includedKeywords(const QString& mainGroup, const QString& listName, const QString& include)
+{
+  const int sep = include.indexOf(QLatin1String("##"));
+  const QString otherList = sep >= 0 ? include.left(sep) : include;
+  const QString otherLanguage = sep >= 0 ? include.mid(sep + 2) : QString();
+
+  const QString guardKey = (otherLanguage.isEmpty() ? currentFile : otherLanguage) + QLatin1Char('|') + otherList;
+  if (m_includeGuard.contains(guardKey))
+  {
+    kWarning(13010) << "circular <include> of keyword list" << include << "in" << currentFile;
+    return QStringList();
+  }
+  m_includeGuard.insert(guardKey);
+
+  QStringList result;
+  if (otherLanguage.isEmpty())
+  {
+    // same file: finddata() shares m_data, so collect through a copy
+    const QStringList saved = m_data;
+    finddata(mainGroup, otherList, true);
+    result = m_data;
+    m_data = saved;
+  }
+  else
+  {
+    const QString identifier = KateHlManager::self()->identifierForName(otherLanguage);
+    if (identifier.isEmpty())
+    {
+      kWarning(13010) << "keyword list" << listName << "in" << currentFile
+                      << "includes" << include << "but there is no such language";
+    }
+    else
+    {
+      KateSyntaxDocument other;
+      other.m_includeGuard = m_includeGuard;
+      if (other.setIdentifier(identifier))
+        result = other.finddata(mainGroup, otherList, true);
+      m_includeGuard = other.m_includeGuard;
+    }
+  }
+
+  m_includeGuard.remove(guardKey);
+  return result;
+}
+
 QStringList& KateSyntaxDocument::finddata(const QString& mainGroup, const QString& type, bool clearList)
 {
 #ifdef KSD_OVER_VERBOSE
@@ -320,12 +379,24 @@ QStringList& KateSyntaxDocument::finddata(const QString& mainGroup, const QStrin
           
           QDomNodeList childlist = nodelist1.item(l).toElement().childNodes();
 
+          // <include>name</include> / <include>name##Language</include>
+          // pull another list in (upstream extension); resolved below so
+          // the items of this list come first
+          QStringList includes;
+
           for (int i=0; i<childlist.count(); i++)
           {
-            QString element = childlist.item(i).toElement().text().trimmed();
+            const QDomElement child = childlist.item(i).toElement();
+            QString element = child.text().trimmed();
             if (element.isEmpty())
               continue;
 
+            if (child.tagName() == QLatin1String("include"))
+            {
+              includes << element;
+              continue;
+            }
+
 #ifdef KSD_OVER_VERBOSE
             if (i<6)
             {
@@ -340,6 +411,9 @@ QStringList& KateSyntaxDocument::finddata(const QString& mainGroup, const QStrin
             m_data += element;
           }
 
+          foreach (const QString &include, includes)
+            m_data += includedKeywords(mainGroup, type, include);
+
           break;
         }
       }
diff --git a/part/syntax/katesyntaxdocument.h b/part/syntax/katesyntaxdocument.h
index 2a439d3bc5..5e97412b27 100644
--- a/part/syntax/katesyntaxdocument.h
+++ b/part/syntax/katesyntaxdocument.h
@@ -22,6 +22,7 @@
 
 #include <QtCore/QList>
 #include <QtCore/QStringList>
+#include <QtCore/QSet>
 #include <QtXml/QDomDocument>
 
 class KConfig;
@@ -80,6 +81,12 @@ class KateSyntaxDocument : public QDomDocument
      */
     explicit KateSyntaxDocument(KConfig *config, bool force = false);
 
+    /**
+     * A bare document without a mode list, for reading a second definition
+     * file while another one is being built (keyword list includes).
+     */
+    KateSyntaxDocument();
+
     /**
      * Desctructor
      */
@@ -139,6 +146,9 @@ class KateSyntaxDocument : public QDomDocument
      */
     QStringList& finddata(const QString& mainGroup,const QString& type,bool clearList=true);
 
+    /** one <include> of a keyword list, see finddata() */
+    QStringList includedKeywords(const QString& mainGroup, const QString& listName, const QString& include);
+
   private:
     /**
      * Generate the list of hl modes, store them in myModeList
@@ -166,6 +176,9 @@ class KateSyntaxDocument : public QDomDocument
      * last found data out of the xml
      */
     QStringList m_data;
+
+    /** keyword lists being resolved through <include>, "file|list" / "Language|list" */
+    QSet<QString> m_includeGuard;
     
     /**
      * global config, deleted by hlmanager...
diff --git a/part/utils/katecmds.cpp b/part/utils/katecmds.cpp
index 3ca905ed68..9dd88cc794 100644
--- a/part/utils/katecmds.cpp
+++ b/part/utils/katecmds.cpp
@@ -38,7 +38,6 @@
 #include <kdebug.h>
 #include <klocale.h>
 #include <kurl.h>
-#include <kshellcompletion.h>
 
 #include <QDir>
 #include <QtCore/QRegExp>
diff --git a/part/utils/kateconfig.cpp b/part/utils/kateconfig.cpp
index 6ffecd69a4..43f94262a7 100644
--- a/part/utils/kateconfig.cpp
+++ b/part/utils/kateconfig.cpp
@@ -40,6 +40,7 @@
 
 #include <QtCore/QTextCodec>
 #include <QStringListModel>
+#include <kglobal.h>
 
 //BEGIN KateConfig
 KateConfig::KateConfig ()
diff --git a/part/utils/kateglobal.cpp b/part/utils/kateglobal.cpp
index 04348c622a..a2deff4047 100644
--- a/part/utils/kateglobal.cpp
+++ b/part/utils/kateglobal.cpp
@@ -20,6 +20,8 @@
  */
 
 #include "kateglobal.h"
+
+#include <QtCore/QCoreApplication>
 #include "kateglobal.moc"
 
 #include "katedocument.h"
@@ -41,7 +43,7 @@
 #include "snippet/katesnippetglobal.h"
 
 #include <klocale.h>
-#include <kservicetypetrader.h>
+#include <ktexteditor/editorchooser.h>
 #include <kdirwatch.h>
 #include <kdebug.h>
 #include <kdeversion.h>
@@ -52,6 +54,7 @@
 #include <QtCore/QPointer>
 #include <QtGui/QBoxLayout>
 #include <QApplication>
+#include <kglobal.h>
 
 KateGlobal *KateGlobal::s_self = 0;
 
@@ -462,9 +465,52 @@ KIcon KateGlobal::configPageIcon (int number) const
   return KIcon("document-properties");
 }
 
+namespace {
+  /**
+   * qt4compat: KatePart is linked statically, there is no plugin factory.
+   * Register the in-process editor with KTextEditor::EditorChooser.
+   */
+  /**
+   * qt4compat: the editor singleton is reference-counted by its documents
+   * (registerDocument/deregisterDocument); in KDE the KParts factory
+   * (KateFactory, not built here) holds one reference as long as the
+   * library is loaded, so the editor outlives the last document. Hold that
+   * reference here for the life of the process: without it, closing the
+   * last document deleted the editor and Kate crashed creating the
+   * replacement document through its cached pointer. Released from a
+   * post routine, which is when the library would have been unloaded.
+   */
+  void kateReleaseEditorBaseRef ()
+  {
+    KateGlobal::decRef ();
+  }
+
+  KTextEditor::Editor *kateEditorCreator ()
+  {
+    static bool baseRefHeld = false;
+    if (!baseRefHeld) {
+      baseRefHeld = true;
+      KateGlobal::incRef ();
+      qAddPostRoutine (kateReleaseEditorBaseRef);
+    }
+    return KateGlobal::self ();
+  }
+
+  struct KateEditorRegistrar
+  {
+    KateEditorRegistrar ()
+    {
+      KTextEditor::setEditorCreator (kateEditorCreator);
+    }
+  };
+
+  static KateEditorRegistrar s_kateEditorRegistrar;
+}
+
 KateGlobal *KateGlobal::self ()
 {
   if (!s_self) {
+    KTextEditor::setEditorCreator (kateEditorCreator);
     new KateGlobal ();
   }
 
diff --git a/part/utils/kateglobal.h b/part/utils/kateglobal.h
index 29a28370d1..db6b32ba2c 100644
--- a/part/utils/kateglobal.h
+++ b/part/utils/kateglobal.h
@@ -29,11 +29,13 @@
 
 #include <kservice.h>
 #include <kcomponentdata.h>
+#include <ksharedconfig.h>
 #include <kaboutdata.h>
 #include <ktexteditor/commandinterface.h>
 #include <ktexteditor/containerinterface.h>
 #include <ktexteditor/templateinterface2.h>
 #include <QtCore/QList>
+#include <QtCore/QPointer>
 
 class KateCmd;
 class KateModeManager;
diff --git a/part/utils/katepartpluginmanager.cpp b/part/utils/katepartpluginmanager.cpp
index 4d61b44813..f064e6e01d 100644
--- a/part/utils/katepartpluginmanager.cpp
+++ b/part/utils/katepartpluginmanager.cpp
@@ -36,6 +36,7 @@
 
 #include <kservicetypetrader.h>
 #include <kdebug.h>
+#include <kdeversion.h>
 
 KatePartPluginInfo::KatePartPluginInfo(KService::Ptr service)
     : m_pluginInfo(service)
@@ -215,12 +216,12 @@ void KatePartPluginManager::loadPlugin (KatePartPluginInfo &item)
     Q_ASSERT( openDependencies.empty() );
   }
 
-  // try to load, else reset load flag!
-  QString error;
-  item.plugin = item.service()->createInstance<KTextEditor::Plugin>(this, QVariantList(), &error);
-  if ( !item.plugin )
-    kWarning() << "failed to load plugin" << item.service()->name() << ":" << error;
-  item.load = (item.plugin != 0);
+  // qt4compat: there is no plugin loader without KService/ksycoca, so
+  // editor plugins can not be loaded in this build; keep the bookkeeping sane
+  item.plugin = 0;
+  if (item.service())
+    kWarning() << "editor plugins are not available in this build, cannot load" << item.service()->name();
+  item.load = false;
 }
 
 void KatePartPluginManager::unloadPlugin (KatePartPluginInfo &item)
diff --git a/part/utils/kateprinter.cpp b/part/utils/kateprinter.cpp
index 10e5ccd41f..9d00b9c155 100644
--- a/part/utils/kateprinter.cpp
+++ b/part/utils/kateprinter.cpp
@@ -57,6 +57,7 @@
 #include <QtGui/QLayout>
 #include <QtCore/QStringList>
 #include <kvbox.h>
+#include <kglobal.h>
 
 //BEGIN KatePrinter
 void KatePrinter::readSettings(QPrinter& printer)
diff --git a/part/view/kateview.cpp b/part/view/kateview.cpp
index 525defbcb5..d7513c885d 100644
--- a/part/view/kateview.cpp
+++ b/part/view/kateview.cpp
@@ -92,6 +92,7 @@
 #include <QtGui/QKeyEvent>
 #include <QtGui/QLayout>
 #include <QtCore/QMimeData>
+#include <sys/stat.h>
 
 //#define VIEW_RANGE_DEBUG
 
diff --git a/part/view/kateviewhelpers.cpp b/part/view/kateviewhelpers.cpp
index 8ad87710ea..cdeced3f64 100644
--- a/part/view/kateviewhelpers.cpp
+++ b/part/view/kateviewhelpers.cpp
@@ -1703,7 +1703,7 @@ void KateIconBorder::paintBorder (int /*x*/, int y, int /*width*/, int height)
       if (realLine > -1) {
         if (m_viewInternal->cache()->viewLine(z).startCol() == 0) {
           if (m_viRelLineNumbersOn && m_view->viInputMode()) {
-            int diff = abs(realLine - currentLine);
+            int diff = abs(realLine - (int)currentLine);
             if (diff > 0) {
               p.drawText( lnX + m_maxCharWidth / 2, y, lnWidth - m_maxCharWidth, h,
                           Qt::TextDontClip|Qt::AlignRight|Qt::AlignVCenter, QString("%1").arg(diff) );
diff --git a/part/view/kateviewinternal.cpp b/part/view/kateviewinternal.cpp
index a2906f3b45..565286779d 100644
--- a/part/view/kateviewinternal.cpp
+++ b/part/view/kateviewinternal.cpp
@@ -3279,8 +3279,8 @@ void KateViewInternal::clear()
 
 void KateViewInternal::wheelEvent(QWheelEvent* e)
 {
-  // zoom text, if ctrl is pressed
-  if (e->modifiers() == Qt::ControlModifier) {
+  // zoom text, if ctrl is pressed (and the platform wants wheel zooming, see MacPorts patch)
+  if (e->modifiers() == Qt::ControlModifier && KGlobalSettings::wheelMouseZooms()) {
     if (e->delta() > 0) {
       slotIncFontSizes();
     } else {
diff --git a/part/vimode/kateviemulatedcommandbar.cpp b/part/vimode/kateviemulatedcommandbar.cpp
index 907f8f7b49..c8bc6a0cd5 100644
--- a/part/vimode/kateviemulatedcommandbar.cpp
+++ b/part/vimode/kateviemulatedcommandbar.cpp
@@ -34,7 +34,7 @@
 #include <QtGui/QLabel>
 #include <QtGui/QCompleter>
 #include <QtGui/QApplication>
-#include <KDE/KColorScheme>
+#include <kcolorscheme.h>
 #include <algorithm>
 
 namespace
diff --git a/part/vimode/kateviinputmodemanager.cpp b/part/vimode/kateviinputmodemanager.cpp
index c95cd9dbdf..2db0b42ebe 100644
--- a/part/vimode/kateviinputmodemanager.cpp
+++ b/part/vimode/kateviinputmodemanager.cpp
@@ -875,7 +875,7 @@ QString KateViInputModeManager::getMarksOnTheLine(int line) {
   if ( m_view->viInputMode() ) {
     foreach (QChar markerChar, m_marks.keys()) {
       if  ( m_marks.value(markerChar)->line() == line )
-        res += markerChar + ":" + QString::number(m_marks.value(markerChar)->column()) + " ";
+        res += QString(markerChar) + ":" + QString::number(m_marks.value(markerChar)->column()) + " ";
     }
   }
 
