# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem      1.0
PortGroup       cmake 1.1
PortGroup       github 1.0

name            SDL3_mixer
github.setup    libsdl-org SDL_mixer 3.2.4 release-
revision        0
categories      audio devel
license         zlib
maintainers     {jmr @jmroot} openmaintainer
description     Audio mixer library for SDL3

long_description \
    A library to manage audio in SDL3-based games. It offers audio \
    file decoding for several popular formats, such as WAV, MP3 and \
    Ogg Vorbis, and provides sample-perfect mixing of multiple \
    sources of audio, with various effects, and other advanced \
    features.

github.tarball_from releases
distname        ${name}-${version}

checksums       rmd160  be294a0d13a338fbff47914696c640d8f66b19bb \
                sha256  182a07c745375e113dc740d43964ff21b0be29f29f59876c4dbc4db3d32f6901 \
                size    5161139

depends_build-append \
                path:bin/pkg-config:pkgconfig
depends_lib     port:flac \
                port:libgme \
                port:libvorbis \
                port:libxmp \
                port:mpg123 \
                port:opusfile \
                port:SDL3 \
                port:wavpack

configure.args-append   -DSDLMIXER_DEPS_SHARED=OFF \
                        -DSDLMIXER_EXAMPLES_INSTALL=OFF \
                        -DSDLMIXER_INSTALL_MAN=ON \
                        -DSDLMIXER_MIDI_FLUIDSYNTH=OFF \
                        -DSDLMIXER_STRICT=ON \
                        -DSDLMIXER_TESTS_INSTALL=OFF

post-destroot {
    set docdir ${prefix}/share/doc/${name}
    xinstall -d ${destroot}${docdir}
    xinstall -m 0644 -W ${worksrcpath} CHANGES.txt README.md \
        ${destroot}${docdir}
}
