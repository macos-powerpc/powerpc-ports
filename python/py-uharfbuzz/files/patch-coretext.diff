--- harfbuzz/src/hb-coretext.cc	2026-06-04 05:17:28.000000000 +0800
+++ harfbuzz/src/hb-coretext.cc	2026-07-04 12:22:06.000000000 +0800
@@ -403,6 +403,12 @@
 hb_coretext_face_create_from_file_or_fail (const char   *file_name,
 					   unsigned int  index)
 {
+#if !(defined(TARGET_OS_IPHONE) && TARGET_OS_IPHONE) && defined(MAC_OS_X_VERSION_MIN_REQUIRED) && MAC_OS_X_VERSION_MIN_REQUIRED < 1060
+  /* CTFontManagerCreateFontDescriptorsFromURL is only available on macOS 10.6+.
+   * Fall back to the generic (non-CoreText) file-loading implementation on
+   * older deployment targets. */
+  return hb_face_create_from_file_or_fail (file_name, index);
+#else
   auto url = CFURLCreateFromFileSystemRepresentation (nullptr,
 						      (const UInt8 *) file_name,
 						      strlen (file_name),
@@ -437,6 +443,7 @@
   hb_face_set_index (face, index);
 
   return face;
+#endif
 }
 
 /**
@@ -524,6 +531,8 @@
   CFDictionaryRef variations = CTFontCopyVariation (ct_font);
   if (variations)
   {
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wnarrowing"
     HB_SCOPE_GUARD (CFRelease (variations));
 
     hb_vector_t<hb_variation_t> vars;
@@ -547,6 +556,7 @@
       }
       hb_font_set_variations (font, vars.arrayZ, vars.length);
     }
+#pragma GCC diagnostic pop
   }
 
   /* Let there be dragons here... */

--- harfbuzz/src/hb-coretext-font.cc	2026-06-04 05:17:28.000000000 +0800
+++ harfbuzz/src/hb-coretext-font.cc	2026-07-04 12:21:37.000000000 +0800
@@ -301,6 +301,8 @@
 			       hb_glyph_extents_t *extents,
 			       void *user_data HB_UNUSED)
 {
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wnarrowing"
   CTFontRef ct_font = (CTFontRef) (const void *) font->data.coretext;
 
   CGFloat ct_font_size = CTFontGetSize (ct_font);
@@ -317,6 +319,7 @@
   extents->y_bearing = hb_clamp_to<hb_position_t> (y_bearing);
   extents->width = hb_clamp_to<hb_position_t> (round (bounds.size.width * x_mult));
   extents->height = hb_clamp_to<hb_position_t> (round (bounds.origin.y * y_mult) - y_bearing);
+#pragma GCC diagnostic pop
 
   return true;
 }
