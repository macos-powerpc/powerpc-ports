--- pyproject.toml	1970-01-01 08:00:00.000000000 +0800
+++ pyproject.toml	2024-01-25 22:43:53.000000000 +0800
@@ -1,35 +1,27 @@
-[project]
-name = "runs"
-version = "1.3.0"
-description = "🏃 Run a block of text as a subprocess 🏃"
-authors = [{ name = "Tom Ritchford", email = "tom@swirly.com" }]
-requires-python = ">=3.10"
-readme = "README.rst"
-license = "MIT"
-classifiers = ["Programming Language :: Python :: 3", "Programming Language :: Python :: 3.10", "Programming Language :: Python :: 3.11", "Programming Language :: Python :: 3.12", "Programming Language :: Python :: 3.13", "Programming Language :: Python :: 3.14"]
-dependencies = ["xmod"]
-
 [tool.doks]
 auto = true
 
-[dependency-groups]
-dev = [
-    "coverage",
-    "pytest",
-    "tdir",
-    "ruff",
-    "pyupgrade>=3.21.2",
-    "ty>=0.0.14",
-]
-
-[tool.uv]
-
-[tool.uv.build-backend]
-module-root = ""
+[tool.poetry]
+name = "runs"
+version = "1.3.0"
+description = "🏃 Run a block of text as a subprocess 🏃"
+authors = ["Tom Ritchford <tom@swirly.com>"]
+license = "MIT"
+readme = "README.rst"
 
-[build-system]
-requires = ["uv_build>=0.9.0,<0.10.0"]
-build-backend = "uv_build"
+[tool.poetry.dependencies]
+python = ">=3.10"
+xmod = "*"
+
+[tool.poetry.group.dev.dependencies]
+black = "*"
+coverage = "*"
+flake8 = "*"
+isort = "*"
+mypy = "*"
+pytest = "*"
+tdir = "*"
+ruff = "*"
 
 [tool.coverage.run]
 branch = true
@@ -38,7 +30,12 @@
 [tool.coverage.report]
 fail_under = 80
 skip_covered = true
-exclude_lines = ["pragma: no cover", "if False:", "if __name__ == .__main__.:", "raise NotImplementedError"]
+exclude_lines = [
+    "pragma: no cover",
+    "if False:",
+    "if __name__ == .__main__.:",
+    "raise NotImplementedError"
+]
 
 
 [tool.ruff]
@@ -47,3 +44,8 @@
 [tool.ruff.format]
 quote-style = "single"
 
+[tool.mypy]
+strict = true
+[build-system]
+requires = ["poetry-core"]
+build-backend = "poetry.core.masonry.api"
