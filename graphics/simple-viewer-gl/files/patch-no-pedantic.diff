--- CMakeLists.txt	2026-09-01 18:37:23.000000000 +0000
+++ CMakeLists.txt	2026-09-17 13:10:34.019795190 +0000
@@ -63,7 +63,10 @@
 if(CMAKE_BUILD_TYPE STREQUAL "Release")
     pretty_print("*" "Release Build" "*" " " "-")
     add_definitions("-DNDEBUG" )
-    add_definitions("-Wall -Wextra -pedantic -pedantic-errors -Wno-unused-function -O2")
+    # -pedantic-errors turns GCC's Objective-C output into hard errors (an
+    # @interface with no ivars is reported as a struct with no named members),
+    # and -O2 overrides the optimisation level the build system picks.
+    add_definitions("-Wall -Wextra -Wno-unused-function")
     if(CMAKE_CXX_COMPILER_ID MATCHES "Clang")
         add_definitions("-Wno-deprecated-register")
     endif()
