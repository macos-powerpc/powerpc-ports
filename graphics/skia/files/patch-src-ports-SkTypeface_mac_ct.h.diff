--- src/ports/SkTypeface_mac_ct.h.orig	2026-04-26 19:00:00.000000000 +0000
+++ src/ports/SkTypeface_mac_ct.h	2026-04-26 19:00:00.000000000 +0000
@@ -34,6 +34,12 @@

 #include <memory>

+// kCTFontColorGlyphsTrait was introduced in macOS 10.7 / iOS 4.3
+// Define it for older systems to avoid compile errors
+#ifndef kCTFontColorGlyphsTrait
+#define kCTFontColorGlyphsTrait (1 << 13)
+#endif
+
 class SkData;
 class SkDescriptor;
 class SkFontData;
