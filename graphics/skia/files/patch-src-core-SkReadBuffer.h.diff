--- src/core/SkReadBuffer.h.orig
+++ src/core/SkReadBuffer.h
@@ -108,7 +108,7 @@
     }
 
     // peek
-    uint8_t peekByte();
+    uint8_t peekByte(size_t offs = 0);
 
     void readString(SkString* string);
 
