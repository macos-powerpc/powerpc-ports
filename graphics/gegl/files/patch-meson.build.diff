Revert https://github.com/GNOME/gegl/commit/9e536e8180d4e2b0e7d530bfedcd4d951de851fe
Use correct C standard.

--- meson.build	2026-09-07 10:51:56.000000000 +0800
+++ meson.build	2026-09-12 21:49:11.000000000 +0800
@@ -4,7 +4,7 @@
   version: '0.4.72',
   meson_version: '>=0.60.0',
   default_options: [
-    #'c_std=none', # c11 breaks Linux/GNU, gnu11 breaks Windows/MSVC, so set to none
+    'c_std=gnu11', # needed for macOS
     'cpp_std=c++14',
     'buildtype=debugoptimized',
   ],
@@ -703,7 +703,7 @@
 subdir('docs')
 
 
-if host_machine.system() != 'emscripten'
+if not os_osx and host_machine.system() != 'emscripten'
   nm = find_program('nm', required: false)
 
   # Verify .def files for Windows linking (and for macOS too when -Wl,-exported_symbols_list is used).

--- gegl/meson.build	2026-09-07 10:51:56.000000000 +0800
+++ gegl/meson.build	2026-09-12 21:50:40.000000000 +0800
@@ -102,15 +102,17 @@
   simd_extra_def = []
 endif
 
-gegl_def = custom_target('gegl.def',
-  input: [
-    'gegl-generic.def',
-    simd_extra_def,
-  ],
-  output: [ 'gegl.def', ],
-  command: [python, '-c', 'import sys; data="".join(open(f).read() for f in sys.argv[2:]); open(sys.argv[1],"w").write(data)', '@OUTPUT@', '@INPUT@'],
-  build_by_default: true,
-)
+if not os_osx
+  gegl_def = custom_target('gegl.def',
+    input: [
+      'gegl-generic.def',
+      simd_extra_def,
+    ],
+    output: [ 'gegl.def', ],
+    command: [python, '-c', 'import sys; data="".join(open(f).read() for f in sys.argv[2:]); open(sys.argv[1],"w").write(data)', '@OUTPUT@', '@INPUT@'],
+    build_by_default: true,
+  )
+endif
 
 gegl_lib = library(api_name,
   gegl_sources,
