From 11c59c2882e02b618410d1d5ca00a7052138e485 Mon Sep 17 00:00:00 2001
From: Sergey Fedorov <vital.had@gmail.com>
Date: Sat, 31 Jan 2026 10:24:11 +0800
Subject: [PATCH] Fix for Qt4

---
 src/cyan.cpp      | 7 ++++++-
 src/imageview.cpp | 3 ++-
 2 files changed, 8 insertions(+), 2 deletions(-)

diff --git src/cyan.cpp src/cyan.cpp
index 2a691a8..67536f9 100644
--- src/cyan.cpp
+++ src/cyan.cpp
@@ -55,6 +55,11 @@
 #include <QMimeType>
 #include <qtconcurrentrun.h>
 
+#if QT_VERSION < 0x050000
+#include <QFile>
+#define QFileDevice QFile
+#endif
+
 #ifdef Q_OS_WIN
 #include <windows.h>
 #ifdef __MINGW32__
@@ -1510,7 +1515,7 @@ void Cyan::handleReadWatcher()
         exportEmbeddedProfileAction->setDisabled(imageData.iccInputBuffer.size()==0);
         if (!imageData.info.empty()) { parseImageInfo(); }
         getConvertProfiles();
-        if (!monitorProfile->currentData().toString().isEmpty()) { updateImage(); }
+        if (!monitorProfile->itemData(monitorProfile->currentIndex()).toString().isEmpty()) { updateImage(); }
     } else {
         QMessageBox::warning(this, tr("Image error"),
                              QString::fromStdString(image.error));
diff --git src/imageview.cpp src/imageview.cpp
index 75c5cee..7bdb31b 100644
--- src/imageview.cpp
+++ src/imageview.cpp
@@ -35,6 +35,7 @@
 #include <QMimeDatabase>
 #include <QMimeType>
 #include <QSettings>
+#include <QUrl>
 
 ImageView::ImageView(QWidget* parent) : QGraphicsView(parent)
 , fit(false) {
@@ -54,7 +55,7 @@ ImageView::ImageView(QWidget* parent) : QGraphicsView(parent)
 void ImageView::wheelEvent(QWheelEvent* event) {
     setTransformationAnchor(QGraphicsView::AnchorUnderMouse);
     double scaleFactor = 1.15;
-    if(event->angleDelta().y() > 0) { // up
+    if(event->delta() > 0) { // up
         fit = false;
         scale(scaleFactor, scaleFactor);
         emit myZoom(scaleFactor, scaleFactor);
